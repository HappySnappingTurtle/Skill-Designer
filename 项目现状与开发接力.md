# Skill Designer 1.0 - 项目现状与开发接力

> 文档日期：2026-07-30
>
> 实现基线：`2f0545510792e3448c51e8b830d83347d55c33a5` (`feat: establish Skill Designer 1.0 foundation`)
>
> 当前分支：`main`
>
> 用途：供新的开发者、维护者或 Agent 在不依赖原作者口头背景的情况下继续开发。

---

## 1. 先看结论

Skill Designer 是一个面向开发者的本地 Skill 开发工具。它使用 Workspace 管理多个 Skill，帮助用户创建、导入、编辑、可视化、运行、调试、诊断和导出 Skill。1.0 的核心已经形成可运行的业务闭环，但还不是可发布版本：真实 Windows 矩阵、Docker 沙箱、真实 Provider、用户真实 Skill 和外部 Agent 尚未完成最终验收。

当前 `TODOList.md` 共 42 项：

- 26 项 `[x]` 已完成。
- 13 项 `[~]` 已有实现，但缺外部环境、跨平台或更完整的验收证据。
- 3 项 `[ ]` 未开始：T40 用户真实 Skill 端到端验收、T41 外部 Agent 验收、T42 发布包与使用文档。

文档编写时已重新执行 `npm run ci`：边界 Lint、TypeScript 类型检查、28 个测试文件/164 项测试和三个 package 的生产构建全部通过。

当前正确的主攻方向不是扩展新产品范围，而是先收口 T05 逐文件事务恢复验收，再按依赖顺序补齐现有 `[~]` 项的真实证据。

---

## 2. 产品目标与已确认边界

### 2.1 产品定位

- 这是开发 Skill 的通用工具，不是 Claude、Codex 或某一 Agent 的专属编辑器。
- 主用户是开发者；界面是中文桌面式工作台，不是营销站。
- 用户可以自己编辑，也可以使用自己的 Agent，或使用产品内的简化 LLM 对话框。对话框的职责是简化节点、文档、测试用例等操作。
- 工具应能分析错误、解释原因并给出修复建议，但不自动修改用户的 Skill。任何修改都必须由用户通过 ChangeSet 预览和确认。
- Skill 的逻辑错误最终由用户处理。引擎在收到不合法的下一节点时保持当前状态、记录拒绝事件并返回原因，不自动改图，也不替模型选择所谓“回归路线”。

### 2.2 1.0 范围

- Workspace 可同时管理多个 Skill Project，这是后续跨 Skill 联动的必要基础。
- 1.0 每次编辑、运行、调试和导出仍以单个 Skill Project 为边界，不实现跨 Skill 联合 Artifact 或执行编排。
- `workflow` Skill 使用有向图；`content-only` Skill 可表达文档和知识关系，但 1.0 不引入完整的 Skill 元模型。
- 图谱必须是 2D/3D 力导向图，视觉以原始项目 `/Users/hongyuwu/Documents/graph-engine-demo/graph-viewer/index.html` 为基准，不应回退成工作流画布风格。
- 节点行为按类型区分，不按节点名称、显示文案或特定 ID 区分。
- 图的第一次自动解析不要求 100% 准确，但必须经用户审阅和确认后才能进入项目。
- Skill 版本对比使用其 Git 仓库的 diff，界面展示对比结果。
- 先支持中文和单人本地开发；目标平台为 Windows 和 macOS。
- Benchmark 是真实模型、真实 Skill、真实沙箱下的调试工作流，必然消耗 token。工具只能避免重复上下文和无效重试，不应把“零 token”当作目标。
- Benchmark 排在业务能力、Trace、Bug Report 和诊断闭环之后完成。

### 2.3 明确延后到 2.0

- Skill 元文件、元模型和文档间逻辑。
- 跨 Skill 依赖图、版本约束、联合执行和联合 RuntimeArtifact。
- Claude/Codex/其他 Agent 的专属导出和深度适配。1.0 保持通用核心和薄适配层扩展口。
- 多人协作、团队权限和国际化。

---

## 3. 仓库与架构现状

### 3.1 技术栈

- Node.js 20、npm workspaces、TypeScript 5、Vitest 4。
- Web：React 18、Vite 6、`react-force-graph-2d/3d`、Three.js、Lucide React。
- Server：Node HTTP、WebSocket、本地文件持久化、系统密钥存储。
- Engine：纯 TypeScript、零运行时依赖，不允许依赖浏览器或 Server。

### 3.2 Monorepo 分层

| 路径 | 职责 | 依赖约束 |
|---|---|---|
| `packages/engine` | Schema、图、条件 DSL、Runtime、Trace、诊断、报告、Benchmark 契约 | 只允许包内相对导入，零运行时依赖 |
| `packages/server` | Workspace Store、ChangeSet、Snapshot、HTTP、Provider、沙箱、导入导出 | 可依赖 Engine，不得依赖 Web |
| `packages/web` | 中文工作台、2D/3D 图谱、文档、测试、诊断与对话 UI | 通过 HTTP/WebSocket 使用 Server，不得导入 Node 或 Server |
| `scripts` | 开发启动、预览、边界 Lint、可见 Chrome 验收脚本 | 验收脚本是用户流程证据，不是单元测试替代品 |
| `examples/multi-skill-workspace` | 可移植的多 Skill 最小示例 | 同时覆盖 workflow 和 content-only |

`scripts/lint-boundaries.mjs` 会对上述依赖方向执行 AST 级门禁。不要为了方便而让 Web 直接导入 Server，或让 Engine 读取文件系统。

### 3.3 本地数据

- 生产式启动默认使用 `~/.skill-designer`。
- `npm run dev` 和 `npm run preview` 使用仓库内 `.skill-designer-dev` 作为开发数据目录。
- 主要私有数据包括 Workspaces、Projects、Snapshots、Revisions、Transactions、Traces、Reports、Benchmark 运行和 Provider 配置。
- `.skill-designer-dev/` 被 Git 忽略，其中包含大量 Chrome 验收截图和 JSON 证据。这些是本机证据，不是仓库可移植产物。
- 不要删除或重置用户的本地数据目录。需要隔离验收时，使用新的临时目录或独立的 `SKILL_DESIGNER_DATA_DIR`。

---

## 4. 已实现能力

以 `TODOList.md` 中的 `[x]` 为准，当前已完成：

- 基础契约：Workspace/Skill Project 身份、可扩展图 Schema、条件 DSL、ChangeSet、ExecutionTrace 和本地安全边界。
- 核心编辑：多 Skill 生命周期、图加载与确定性运行、声明式项目查询、2D/3D 图谱、图编辑、文档管理、统一 diff/Git 对比/撤销。
- 导入导出：资产清点、静态导入、解析审阅、冲突裁决、本地 Provider 密钥管理、Benchmark 用例编写、Generic Export、Runtime/Debug 对话框。
- Trace 闭环：实时通道、断线续传、状态归约、回放、运行对比、Bug Report 生成/脱敏/导入/图上复现、诊断分类与建议、Report 转夹具和候选 BenchmarkCase。
- 沙箱基础：SandboxRunner 平台契约、固定 digest 镜像约束、指令允许列表和 preflight 模型。

完成项的细节验收条件和证据位置不在本文重复，必须查看 `TODOList.md` 对应任务。

---

## 5. 进行中能力与真实缺口

| 任务 | 已有实现 | 不能宣布完成的原因 |
|---|---|---|
| T01 | Monorepo、边界 Lint、macOS/Windows CI 配置 | 尚未取得真实 Windows runner 证据 |
| T05 | 事务 journal、Snapshot/Revision/Baseline、回滚、撤销、启动恢复 | 逐文件真实强杀验收和跨系统异常退出矩阵未收口 |
| T06 | 不可变 RuntimeArtifact 与内容/输入指纹 | 三系统同字节指纹矩阵和 Artifact 清理策略未完成 |
| T12 | 中文桌面式 Web 外壳和五个主导航 | 缺 Windows 主流浏览器对照 |
| T21 | 设计助手的受限读取、澄清和 ChangeSet 提案 | 缺用户真实 Skill 和真实 Provider 多轮稳定性验收 |
| T22 | LLM 辅助解析、重解析、证据校验和摘要缓存 | 缺用户真实 Skill 和真实 Provider 验收 |
| T27 | Trace 通用图上投影、回放、速度和时间轴 | 缺真实 Benchmark 环境下的同一覆盖层验收 |
| T32 | Diagnosis -> ChangeSet -> 新运行验证 | 缺真实 Docker 下的 post-repair 操作和更多修复类型 |
| T35 | Sandbox 状态机、冻结输入、产物目录、审计与模拟验收 | 本机尚无可用 Docker CLI 和固定 digest runner 镜像，未进行真容器负向验收 |
| T36 | 真实 Provider 决策、单并发队列、断言、指纹和 Trace | 当前只能生成 `blocked` 记录，没有真实模型 token 和沙箱产物证据 |
| T37 | 单用例/批量队列、取消、断言和人工判定历史 | 缺真实 completed Benchmark 的页面端保存/刷新验收 |
| T38 | Benchmark 报告、诊断、重跑和 post-repair lineage | 缺真实 completed/post-repair 比较证据 |
| T39 | macOS Chrome 下 500 节点、100 文档和移动端规模验收 | 缺 Windows 对照、安全/脱敏汇总和异常恢复矩阵 |

T40-T42 尚未开始。不应在完成 T35-T39 的必要验收之前制作 1.0 发布包。

---

## 6. 当前精确开发断点：T05 逐文件异常恢复

这是下一位开发者应首先继续的工作。

### 6.1 已完成但尚未同步到 TODO 细节的实现

- Engine 契约已有 `ProjectFileMutationStep`。
- Transaction journal 已有 `fileMutation` 和 `recoveredFromFileMutation`。
- `WorkspaceStore` 已提供 `afterFileMutation` 故障注入点。
- 文档重命名已分成三个耐久步骤：
  - `document-rename-destination`：写入目标文档。
  - `document-rename-source`：移除源文档。
  - `document-rename-graph`：更新图中的文档引用。
- 启动恢复会记录具体中断的文件步骤，版本历史 UI 会显示对应中文说明。
- `scripts/transaction-crash-worker.mjs` 已支持在指定文件步骤向自身发送真实 `SIGKILL`。
- `packages/server/test/store.test.ts` 已参数化覆盖三个步骤的进程内异常，验证源文档、目标文档、图字节、active revision、revision 数量、ChangeSet 和 journal 状态。

### 6.2 下一步必须新增的脚本

新建 `scripts/chrome-transaction-file-recovery-verify.mjs`，完成以下真实验收：

1. 在独立临时数据目录创建 Workspace 和 managed workflow。
2. 创建并绑定 `docs/transaction-source.md`。
3. 分别为三个文件步骤创建独立 `docs.rename` ChangeSet。
4. 使用 `scripts/transaction-crash-worker.mjs` 在指定步骤真实强杀子进程。
5. 恢复前先核对部分落盘事实：
   - destination 中断：源/目标都存在，图仍引用源。
   - source 中断：源缺失、目标存在，图仍引用源。
   - graph 中断：源缺失、目标存在，图已引用目标。
6. 新建 `WorkspaceStore` 并执行 `initialize()` 触发恢复。
7. 每次验证：active revision 维持稳定基线、源文档恢复、目标文档移除、图引用恢复、ChangeSet 为 `conflicted`、journal 为 `recovered`、`recoveredFromStage === "prepared"`、`recoveredFromFileMutation` 精确匹配。
8. 启动页面，用可见 Google Chrome 验证：
   - “版本与基线”显示“3 次事务已安全回滚”。
   - 三个文件步骤文案全部可见。
   - 文档页只存在源文档。
   - `390 x 844` 无水平溢出。
   - 控制台错误和失败请求均为 0。
9. 将 JSON 报告和桌面/移动截图输出到 `.skill-designer-dev/chrome-artifacts/transaction-file-recovery/`。

### 6.3 T05 收口验证

```bash
npm run ci
node scripts/chrome-transaction-file-recovery-verify.mjs
node scripts/chrome-transaction-recovery-verify.mjs
```

验证通过后更新 `TODOList.md` 的 T05 进度和 `架构设计文档.md` 的 transaction journal 语义。即使 macOS 验收全部通过，T05 仍应保持 `[~]`，直到取得真实 Windows 异常退出矩阵证据。

---

## 7. 不可破坏的核心架构规则

1. **所有项目修改都必须经 ChangeSet。** 手工编辑、导入、LLM 助手、诊断修复和撤销不得建立直写文件的旁路。
2. **ChangeSet 只是提案。** 预览不能修改 active project；只有用户确认且 digest/baseRevision 仍匹配时才能提交。
3. **Workspace 是管理边界，Skill Project 是 1.0 执行边界。** 不要在 1.0 中暗中引入跨 Skill 执行状态。
4. **稳定身份与名称分离。** 逻辑使用 `workspaceId/projectId/skillId/nodeId`，名称只用于展示。
5. **节点行为来自类型注册表。** 不得用节点名称、文案或固定 ID 实现隐式行为。
6. **运行必须冻结 RuntimeArtifact。** 运行开始后的项目编辑、Workspace 切换不能改变已开始的运行。
7. **Trace 是运行事实单一来源。** 实时高亮、回放、Bug Report、诊断和 Benchmark 不得维护互不兼容的事件格式。
8. **工具可建议，不自动修复用户 Skill。** `verified` 必须来自修复后的新 RuntimeArtifact 和新运行，回放不能伪装成验证。
9. **安全边界不得退化。** Server 只监听 loopback，使用会话令牌和 Origin/Host 校验；项目路径、符号链接、上传大小、导入文件和沙箱命令必须继续受限。
10. **未知 JSON 字段要可保留。** 在 Schema 安全上限内，读取、编辑、确认和导出不得静默丢失扩展字段。
11. **通用性不能只是文案。** 核心契约不包含 Claude/Codex 特有概念；Agent 差异留在薄适配层。
12. **图谱视觉以原项目为基准。** 修改 `SkillGraphCanvas.tsx`、`GraphView.tsx` 或相关 CSS 后，必须执行原风格对照验收。

---

## 8. 本地启动与验证

### 8.1 环境

- Node.js 20。文档编写时本机为 `v20.20.2`。
- npm。文档编写时本机为 `10.8.2`。
- 图形验收需要已安装的 Google Chrome，脚本使用 Playwright `channel: "chrome"` 且应保持可见模式。
- 真实 Sandbox/Benchmark 还需要 Docker 和固定 digest 的 runner 镜像。

### 8.2 安装与开发

```bash
npm ci
npm run dev
```

`npm run dev` 启动 Server 和 Vite。默认地址：

- Web：`http://127.0.0.1:5173`
- Server：`http://127.0.0.1:4310`
- 开发数据：`<repo>/.skill-designer-dev`

生产构建预览：

```bash
npm run preview
```

默认打开 `http://127.0.0.1:4310`。

可覆盖的主要环境变量：

- `SKILL_DESIGNER_PORT`
- `SKILL_DESIGNER_DATA_DIR`
- `SKILL_DESIGNER_SANDBOX_IMAGE`：必须使用固定 digest 镜像，否则 Sandbox preflight 应当返回 unavailable。
- `SKILL_DESIGNER_OPENAI_MODEL`：未在界面中明确配置模型时的环境候选。

密钥应通过界面和系统密钥存储管理，不得写入仓库、截图、Trace 或 Bug Report。

### 8.3 全量门禁

```bash
npm run ci
```

该命令依次执行边界 Lint、TypeScript 类型检查、Vitest 和三个 package 的生产构建。交付代码前至少执行一次。

仅运行 Store 定向测试：

```bash
npx vitest run packages/server/test/store.test.ts
```

### 8.4 可见 Chrome 验收

- 用户流程的完成不能只依赖单元测试或 DOM 数量。
- 需要实际点击、输入、切换视图，检查控制台错误、失败请求、桌面/移动端溢出。
- Canvas/3D 场景还必须检查非空像素、实际取景和交互结果。
- 图谱风格基准脚本：

```bash
node scripts/chrome-original-graph-style-verify.mjs
```

- 事务恢复现有验收：

```bash
node scripts/chrome-transaction-recovery-verify.mjs
```

其他流程脚本位于 `scripts/chrome-*-verify.mjs`。多数脚本会使用独立临时数据，但执行前仍应阅读脚本顶部，核对端口、数据目录和清理行为。

---

## 9. 验收纪律

以下情况不得把任务改为 `[x]`：

- 只写了代码，未执行对应测试。
- 只执行了单元测试，未执行跨包或用户流程验收。
- 只检查 DOM 中有 Canvas，未检查实际绘制像素和取景。
- 将模拟 Docker 执行器证据当作真实容器证据。
- Benchmark 没有真实 Provider 调用、token usage、真实沙箱产物和断言结果。
- 使用 macOS 结果宣称 Windows 已支持，或用 CI YAML 存在代替真实 runner 记录。
- 未使用用户提供的真实 Skill，却宣称 T40 完成。
- Generic Export 只在本仓库内部运行，却宣称外部 Agent 验收完成。

不可用“理论上可行”替代证据。证据不足时保持 `[~]`，并在 TODO 中写清真实缺口。

---

## 10. 关键文件索引

| 文件 | 用途 |
|---|---|
| `产品设计文档.md` | 产品定位、边界、用户流程和 1.0 验收标准 |
| `架构设计文档.md` | Schema、核心对象、服务、API、数据流、安全和 2.0 扩展口 |
| `TODOList.md` | 实时任务状态、依赖、验收证据和未完成条件 |
| `专家团评审报告.md` | 早期产品/架构风险和评审背景，不代替后续已确认决策 |
| `设计确认与遗留问题.md` | 早期裁决的来源记录 |
| `packages/engine/src/types.ts` | 大部分跨层公共契约 |
| `packages/engine/src/schema.ts` | 图 Schema 和类型注册 |
| `packages/engine/src/runtime.ts` | 确定性图运行 |
| `packages/engine/src/trace.ts` | Trace 状态归约 |
| `packages/server/src/store.ts` | Workspace/Project/ChangeSet/Revision/Snapshot/Transaction 的主要持久化实现 |
| `packages/server/src/http.ts` | HTTP/WebSocket 路由与本地安全边界 |
| `packages/server/src/benchmark-runner.ts` | 真实 Benchmark 队列和执行编排 |
| `packages/server/src/sandbox-runner.ts` | Docker 沙箱执行契约 |
| `packages/web/src/App.tsx` | 顶层 Workspace/Skill 选择与主导航 |
| `packages/web/src/components/GraphView.tsx` | 图谱工作台、编辑和运行入口 |
| `packages/web/src/components/SkillGraphCanvas.tsx` | 共用 2D/3D 力导向图渲染器 |
| `packages/web/src/components/RevisionHistoryModal.tsx` | Revision/Baseline/撤销/事务恢复 UI |
| `scripts/chrome-original-graph-style-verify.mjs` | 原项目图谱风格对照验收 |
| `scripts/chrome-transaction-recovery-verify.mjs` | 现有 `files-written` 强杀与启动恢复验收 |
| `scripts/transaction-crash-worker.mjs` | 可在事务或逐文件步骤强杀的子进程 |

---

## 11. 建议的后续顺序

1. 收口第 6 节的 T05 逐文件真实 `SIGKILL` 验收，同步 TODO 和架构文档。
2. 继续完成不需外部环境的 T06 Artifact 过期清理策略，同时准备三系统指纹夹具。
3. 在 Windows runner 收口 T01/T05/T06/T12/T39 的跨平台证据，不要重复建立互不相容的测试矩阵。
4. 使用用户提供的真实 Skill 和真实 Provider 收口 T21/T22。
5. 配置固定 digest Docker runner，优先完成 T35 的文件、网络、资源、取消和清理负向验收。
6. 在 T35 真实可用后，使用真实 Skill + 真实 Provider 收口 T36-T38，并同时补齐 T27/T32 的 Benchmark 验收。
7. 汇总 T39 的跨平台、性能、安全、脱敏和异常恢复矩阵。
8. 执行 T40 用户真实 Skill 端到端验收和 T41 外部 Agent 验收。
9. 最后执行 T42：发布包、安装/使用文档、数据迁移/备份说明和发布检查。

---

## 12. 已知工程风险

- `packages/server/src/store.ts` 已经接近 6,000 行，是最大的事务和数据一致性风险点。不要在 T05 收口前进行大规模拆分；收口后如要拆分，先用现有 Store 和强杀验收锁定行为。
- `packages/web/src/App.tsx`、`GraphView.tsx` 和 `SkillGraphCanvas.tsx` 体积较大。拆分必须保留当前稳定尺寸、2D/3D 状态、Trace 覆盖层和移动端布局。
- 大量 Chrome 脚本是独立验收程序，尚未统一进入 `npm run ci`。不应盲目全部并发运行：脚本可能占用不同端口、启动可见 Chrome 并进行本地证据清理。
- `.skill-designer-dev/chrome-artifacts` 存在本机验收证据，但被 Git 忽略。跨机交接不能假定另一台机器能读取这些证据，应重跑关键脚本。
- 当前还没有 T42 使用者 README、安装包和稳定数据迁移策略，不得把开发启动当作发布方案。
- 当前 Schema 版本为 1.0，但项目 npm 版本仍为 `0.1.0`。发布前必须明确两者的版本语义，不要在普通功能提交中随意升级。
- 当前 Web 生产构建的主 JavaScript 产物约 2.15 MB（gzip 约 591 KB），Vite 会给出超过 500 KB 的分包警告。这不阻断当前门禁，但应在 T39/T42 的性能和发布收口中评估路由/重组件动态导入和 Three.js 相关拆包，不要仅调高警告阈值。

---

## 13. 接力者开始工作前的检查清单

1. 阅读顺序：本文 -> `TODOList.md` 当前任务 -> `产品设计文档.md` 相关章节 -> `架构设计文档.md` 相关章节 -> 相关代码和测试。
2. 执行 `git status --short --branch`，确认不覆盖他人的未提交修改。
3. 执行 `npm ci` 和 `npm run ci`，建立本机基线。
4. 核对当前 commit 是否仍以 `2f05455` 为基线；如已有后续提交，必须先读取 diff 和新 TODO，不要按本文盲目重做。
5. 一次只攻一个 TODO 闭环。编码前写清输入、输出、异常、验收用例和不在范围内的内容。
6. 只修改当前任务相关文件，不借机进行无关重构或格式化全仓。
7. 编码后运行定向测试、全量 `npm run ci` 和对应的可见 Chrome 用户流程。
8. 同步更新 `TODOList.md`；如契约、状态机或数据语义变化，同时更新架构文档。产品边界变化需先经用户确认。
9. 提交前再次检查 `git diff --check`、`git status`、凭据扫描和变更文件范围。

---

## 14. 开发交付模板

每个后续任务交付时，至少留下以下信息：

```text
任务：Txx - <标题>
实现：<完成的行为与契约>
未完成：<缺失的环境、证据或范围>
定向测试：<命令与结果>
全量门禁：<npm run ci 结果>
用户流程：<Chrome 脚本、结果和证据路径>
跨平台/外部环境：<实际执行情况，不得推测>
文档：<已同步的 TODO/产品/架构章节>
提交：<commit hash 与 message>
下一断点：<后续人员可直接执行的第一步>
```

这个模板的目的是保留可核对的工程事实，而不是追求形式上的“全部完成”。
