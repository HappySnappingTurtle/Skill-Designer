# Skill Designer 1.0 - 系统架构设计文档

> 架构基线：v1.0
> 日期：2026-07-27
> 依据：《Skill Designer 1.0 - 产品设计文档》

---

## 1. 架构目标与原则

Skill Designer 1.0 以 Workspace 为根对象，以 Skill Project 为执行原子，围绕一条统一主链建设：

```text
Workspace
-> 选择 Skill Project
ChangeSet
-> 用户确认
-> 事务提交
-> RuntimeArtifact
-> ExecutionTrace
-> Diagnosis
-> 修复建议
-> 新 ChangeSet
```

架构原则：

1. **Workspace 只组织、不吞并**：Workspace 管理多个 Skill Project 引用和摘要，不把多个仓库合并为一个文件事实源。
2. **Skill 文件为项目事实源**：每个 Skill 的图、文档、SKILL.md 和测试用例都是可移植文件，服务器不另造一份业务数据库。
3. **Studio 写入归一**：所有客户端写入都经 server 的存储和事务层。
4. **修改先提案**：解析、重载、LLM 设计助手和诊断修复不得直接修改 active 项目。
5. **单 Skill 原子边界**：1.0 的 ChangeSet、revision、RuntimeArtifact、Trace、Benchmark 和导出都明确绑定一个 Skill Project。
6. **运行只读快照**：会话、回放和 benchmark 不直接读取可变工作树，只读 RuntimeArtifact。
7. **Trace 先持久化后广播**：WebSocket 只是传输层，不是事实源。
8. **引擎不含业务域词汇**：引擎只执行通用类型、边和条件协议。
9. **节点行为按类型决定**：节点 ID 只用于标识实例，任何引擎行为不得依赖固定 ID。
10. **1.0 只实现单 Skill 有向流程图**：多层元模型和跨 Skill 编排只保留扩展口，不提前实现。
11. **通用包不绑定 Agent**：1.0 只有 GenericExportProfile，专用 Agent 适配放到 2.0。
12. **基础安全是架构边界**：路径隔离、本地访问控制、密钥隔离和 ChangeSet 白名单不后置。

---

## 2. 系统分层

```text
┌─ web（React）─────────────────────────────┐
│  工作区 / Skill 切换 / 图谱 / 文档 / 测试 / 诊断 / 设计助手 │
│  只依赖 server REST + WebSocket，不访问文件系统    │
├─ server（Node.js）───────────────────────────┐
│  Workspace / SkillProject / ChangeSet / 事务 / 解析 / 导出 │
│  LLM Provider / 运行编排 / Trace 持久化 / ws 广播      │
├─ engine（Node 标准库，零 npm 运行时依赖）─────────┤
│  schema / lint / 图加载 / 走边 / 条件 / 文档切片       │
│  RuntimeArtifact / 运行状态 / 通用 CLI             │
├─ Skill 文件 + Workspace/Studio 私有数据（分离存储）───┤
│  Skill：SKILL.md / skill.json / graph / docs / benchmarks   │
│  私有：workspaces / revisions / runtime / traces / reports  │
└───────────────────────────────────────────┘
```

依赖只能自上而下：

- web 不引入 `fs`/`path` 等 Node 模块；
- engine 不引入 HTTP、UI、LLM Provider 或业务域逻辑；
- server 通过 engine 公共 API 执行图校验和运行，不复制一套规则；
- 导出包复用同一份 engine 代码。

---

## 3. Monorepo 结构

```text
Skill-Designer/
├── package.json
├── packages/
│   ├── engine/
│   │   ├── src/
│   │   │   ├── graph.js
│   │   │   ├── schema.js
│   │   │   ├── condition.js
│   │   │   ├── runtime-artifact.js
│   │   │   ├── runtime.js
│   │   │   ├── lookup.js
│   │   │   ├── slicer.js
│   │   │   ├── trace.js
│   │   │   └── cli.js
│   │   └── test/
│   ├── server/
│   │   ├── src/
│   │   │   ├── auth.js
│   │   │   ├── workspace.js
│   │   │   ├── skill-project.js
│   │   │   ├── revision-store.js
│   │   │   ├── change-set.js
│   │   │   ├── transaction.js
│   │   │   ├── ingest/
│   │   │   ├── assistant/
│   │   │   ├── llm/
│   │   │   ├── trace-store.js
│   │   │   ├── diagnosis/
│   │   │   ├── reports/
│   │   │   ├── export/
│   │   │   ├── git-diff.js
│   │   │   ├── benchmark/
│   │   │   ├── sandbox/
│   │   │   ├── routes/
│   │   │   └── ws.js
│   │   └── test/
│   └── web/
│       ├── src/
│       │   ├── app/
│       │   ├── api/
│       │   ├── workspaces/
│       │   ├── projects/
│       │   ├── graph/
│       │   ├── docs/
│       │   ├── changes/
│       │   ├── assistant/
│       │   ├── tests/
│       │   └── diagnosis/
│       └── test/
├── assets/
│   ├── builtin/skill-parser/
│   └── example-workspace/     # 至少包含两个独立 Skill Project
└── docs/
```

`packages/engine/package.json` 的 `dependencies` 必须为空。server 和 web 可以使用外部依赖。

---

## 4. Workspace 与 Skill Project 结构

对象层级固定为：

```text
Workspace（本地组织与可见性边界）
└── WorkspaceMember
    └── SkillProject（编辑、版本、运行和导出边界）
        ├── Graph
        ├── Documents
        ├── BenchmarkCases
        └── Assets
```

Workspace 可以引用不同目录、不同 Git 仓库中的 Skill Project，不要求共同父目录。

### 4.1 Skill Project 文件

```text
<skill-project>/
├── skill.json
├── SKILL.md
├── graph/
│   └── *.json
├── **/*.md                    # 任意安全相对目录；docs/ 仅为推荐位置
├── benchmarks/*.json
├── scripts/**                 # 保留，沙箱完成前不执行
└── assets/**                  # 导入时原样保留；后续通过资产 ChangeSet 管理
```

Studio 私有数据使用操作系统应用数据目录，不写进 Skill 仓库：

```text
<studio-data>/workspaces/<workspace-id>/
└── workspace.json

<studio-data>/projects/<project-id>/
├── state.json
├── source.json
├── baseline.json
├── snapshots/
├── revisions/
├── changesets/
├── runtime-artifacts/
├── traces/
├── reports/
├── diagnoses/
└── backups/

<studio-data>/imports/<import-id>/       # 待确认上传资产与候选
<studio-data>/exports/<export-id>/       # 导出记录、生成文件与 ZIP
```

`skill.json` 是 1.0 manifest，只保留必要信息：

```json
{
  "skillId": "skill-018f...",
  "name": "example-skill",
  "version": "0.1.0",
  "description": "...",
  "capability": "workflow",
  "entry": "flow.start"
}
```

`content-only` 项目不要求 `entry`，可编辑文档和知识节点，但不可进入流程运行。

`skill.json.skillId` 是可移植稳定身份。导入没有该字段的 Skill 时，由解析 ChangeSet 添加；重命名、移动目录或更换 Git remote 不得自动改变它。

### 4.2 Workspace 私有清单

```json
{
  "id": "workspace-...",
  "name": "My Skills",
  "selectedProjectId": "project-...",
  "members": [
    {
      "projectId": "project-...",
      "skillId": "skill-018f...",
      "displayName": "example-skill",
      "mode": "in-place",
      "status": "ready",
      "order": 0
    }
  ]
}
```

Workspace 清单只存本地组织信息，不进入任何 Skill 仓库或通用导出包。`source.json` 记录 projectId 与单个已授权 Skill 真实路径的关联；成员清单同时持久化 `order` 和最近已知 `sourcePath`，读取时根据真实文件状态派生可展示的 `statusDetail`。成员状态至少包括 `pending-import / ready / missing / error`。外部 Skill 尚无 skillId 时先保存候选 skillId，只有包含该 ID 的导入 ChangeSet 被确认后才转为 ready；pending 成员不可运行或导出。一个 Workspace 不允许存在两个相同 skillId 的 ready 成员，避免未来跨 Skill 引用产生歧义。

in-place 打开必须由显式绝对路径请求触发，服务端保存 realpath 后只授权该 Skill 根。打开前读取并校验三个保留文件、限制文件数与总字节、拒绝项目内符号链接；`.git` 和系统元数据不进入 Snapshot/contentHash。即使工作树随后被外部工具修改，RuntimeArtifact 与通用导出仍从 active revision 的 Snapshot 读取，不能把未确认漂移混入旧 revision。

路径修复端点只接受状态为 `missing/error` 的 in-place 成员。候选目录重新执行 realpath、根目录、格式、文件数量/大小、符号链接和图 lint 校验，并要求 manifest/graph 的 skillId 与成员精确相等；最后计算项目 contentHash，与 ProjectState 指向的 active ProjectRevision.contentHash 比较。只有完全相等时才原子替换 `source.json`，Workspace 清单写入失败则恢复旧 source 映射。删除 Workspace 只递归删除该 Workspace 私有清单目录，不删除 managed project root、in-place 根目录或 `projects/<projectId>` 私有历史。

身份语义：

```text
workspaceId  本机 Workspace 身份，不可用于 Skill 间引用
projectId    本机打开实例和授权身份，不可写入导出包
skillId      Skill 可移植稳定身份，未来跨 Skill 引用的根
nodeId       Skill 内稳定身份；全局定位由 skillId + nodeId 构成
```

Web shell 从当前 `WorkspaceMember` 单向派生业务页身份，不允许图谱、文档、测试或诊断组件按名称、路由文案或各自缓存重新推断目标。共享 `SkillId` 视图负责紧凑显示、完整值提示和稳定 `data-skill-id` 验收标记；API 请求仍使用 `workspaceId + projectId`，并由服务端核对实际 `skillId`。切换成员时 React 组件接收同一份成员对象，失联成员只进入成员级错误边界，不改变其他 ready 成员的加载能力。诊断报告中的 skillId 属于报告事实，必须与 shell 当前成员身份分别渲染和匹配。

Workspace 概览只读取成员摘要，不预加载 50 个成员的图、文档、运行或 Artifact。`scripts/chrome-workspace-scale-verify.mjs` 使用 25 个 workflow、25 个 content-only 的 50 成员矩阵，其中 1 个 in-place 成员路径失联；可见 Chrome 必须操作首/中/末搜索与切换、刷新选中持久化、跨图谱/文档页 skillId、桌面和 390px 布局，并记录首屏、搜索和切换时延。该专项阈值固定为首屏 10 秒、单次搜索 1 秒、单次切换 3 秒，只是当前 1.0 本机回归门禁，不代替 Windows/macOS 对照。

T39 的平台、性能、安全与恢复证据统一登记在 `T39-macOS专项验收矩阵.md`。矩阵只能引用已生成的结构化证据，不把单元测试、模拟平台或 CI 配置写成真实平台通过；Windows 浏览器、Windows 异常终止和远端三系统 CI 未执行时必须保持待验收。

1.0 不持久化跨 Skill 边，但保留规范化引用形式 `skill://<skillId>/node/<nodeId>`，供 2.0 Schema 使用。未来依赖声明至少要包含 targetSkillId、版本约束、目标入口/节点和是否可选；依赖属于可移植 Skill 元文件，Workspace 只负责解析成员，不成为依赖事实源。私有目录不进入 Git diff；`benchmarks/` 是 Skill 项目资产，导出时由用户选择是否包含。

2.0 的解析方向固定为“声明与本地实例分离”：

```text
Source Skill 元文件中的 targetSkillId
-> Workspace Member Index（按 skillId 精确匹配）
-> projectId（仅本机授权与打开实例）
-> 目标 Skill 的入口或节点
```

- 同一个 Skill 可以出现在不同 Workspace，依赖声明不携带 workspaceId 或本机路径；
- Workspace 总览聚合展示 `已满足 / 缺失 / 版本不兼容 / 目标异常`，但不静默改写依赖；
- 同一 Workspace 出现重复 skillId 时必须阻止 ready，不能任选一个目标；
- 缺失依赖不按名称、目录或仓库地址降级匹配；
- 1.0 只保证成员索引和稳定身份可支撑该解析链，不实现依赖加载或联合执行。

### 4.3 导入解析审阅

导入先把允许的原文件逐字节冻结到 `<studio-data>/imports/<import-id>/files/`，候选记录保存逐文件 SHA-256、整体 `sourceDigest`、解析器版本、`reviewRevision` 和随审阅内容变化的确认 digest。`pending-import` 期间不创建 Project source、Snapshot 或 active revision；因此解析、编辑候选和重新解析都不能被其他模块误认为已导入项目。这里的 `SkillImportCandidate` 是项目建立前的 pre-project ChangeSet：它有不可变上传事实、状态、digest、预览与显式确认，但不伪造 project baseRevision，也不复用只适用于 ready 项目的 `ProjectChangeSet`。

`IngestInventory` 只在本地 server 包内运行，YAML/TOML/JSON 解析器不进入共享 engine 根导出或 Web bundle。它接收浏览器已经上传并通过路径/大小校验的内存文件集合，不接收任意宿主路径；输出 frontmatter、格式信号、引用清单和诊断。frontmatter 归一化为 JSON 可序列化值，限制别名展开与 30 层嵌套，识别标准身份字段但保留未知键和值。资产类型只影响清单显示，未知类型仍按原字节冻结。

引用扫描先用 Markdown AST 提取 link、image、definition 和 inlineCode，再对已知 frontmatter 引用字段补充目标；解析器只对上传文件路径集合做 POSIX 相对路径归一化，并用 `indexDocument` 校验 Markdown fragment。link/image/definition/frontmatter 的目标不存在时返回 `missing`；inlineCode 的路径样文本只有精确命中资产时返回 `resolved`，无法命中时返回 `candidate` 和 info 诊断，不计入 Web 的待检查问题数。无论引用种类，HTTP(S)、mailto 等外部目标仅记录为 `external`；绝对路径、反斜杠路径和越过根的 `..` 分别记录为 invalid/escaped，绝不转成磁盘读取或网络请求。Web 摘要分别显示已解析、候选和待检查数量，引用清单最多 2000 项，首屏最多渲染 200 项但 API 保留完整上限内事实。

provenance 是导入候选的一等事实，至少覆盖 detected-format、display-name、description、capability 和 graph。每条记录保存稳定 provenanceId、valueSummary、sourcePath、1-based 行区间、`native-manifest | native-graph | frontmatter | markdown-heading | markdown-paragraph | folder-name | static-inference | conservative-fallback` 方法和置信度。它描述判断来源，不代表用户已接受，更不能触发自动修复。

`static-v2` 解析器只接受三类事实：有效原生 `graph/main.json`、合法 manifest 的显式 `content-only` 声明，或 `SKILL.md` 中名称精确落在允许集合的流程章节；优先级依次为原生图、显式 manifest、Markdown 推断。相较 `static-v1`，允许集合增加带精确`快速 / 标准 / 默认`前缀的`工作流 / 流程 / 步骤`标题，但不使用任意后缀、关键词包含或普通段落做语义猜测。Markdown 流程章节至少需要两个有序子标题，或在子标题不足时至少两个编号/checkbox 列表项；否则返回 `content-only` 单知识节点和未决问题。因此“快速流程”下的明确编号步骤可以形成候选，而写明“本 skill 无工作流”且没有有序步骤的“默认工作流”仍保持内容型。每个候选节点/边包含稳定 candidateId、GraphNode/GraphEdge 值、`accepted | rejected` 决策、`high | medium | low` 置信度、来源路径、1-based 行区间、片段类型和原文摘要。流程边只按文档出现顺序形成初始候选，并明确提出分支、回退和并行关系仍待用户确认。

审阅更新必须携带当前 `reviewRevision`，且只能修改已有 candidateId 的决策和白名单图字段，不能借审阅接口增删候选身份或改写节点/边 ID。服务端重新解析 GraphNode/GraphEdge、按接受项重建有效 `SkillGraph` 并运行 `lintGraph`；candidate digest、Workspace pending 成员能力与 lint 摘要一并更新。Web 的候选画布直接调用与正式图谱页相同的 `SkillGraphCanvas`，在桌面提供 3D/2D 切换，在移动端默认 2D；渲染器不拥有审阅事实。

重新解析只读冻结文件。`manuallyEdited=false` 时，新 snapshot 可替换旧解析 snapshot；`manuallyEdited=true` 时，服务保留人工 snapshot，并把新结果写入 `reparseConflict.parsed`。冲突裁决使用 `reviewRevision` 乐观锁，只允许 `manual | reparse` 两种显式选择，不做自动合并。确认接口同时绑定 candidate digest，并拒绝未裁决冲突、静态诊断 error 或图 lint error；确认成功后才事务性复制原文件，并用已裁决 accepted 候选生成图。原生图只有人工审阅发生变化时才被重写，未修改的原生文件和所有非生成资产保持原字节。

`ImportLLMParserService` 在 `pending-import` 阶段编排有界结构化循环。启动时锁定 `workspaceId + importId + sourceDigest + reviewRevision`，检查 Provider 可用性，并先读取 `SKILL.md` 和当前审阅证据所引用的少量文本；模型随后只能返回 `read | result`。服务端限制 4 次调用、8 次按需读取、每轮 4 项、单文件 20,000 字符、总正文 48,000 字符和 1 次 lint 修正。每次读取都重新比对冻结文件 SHA-256；清单外路径和超限请求作为拒绝反馈回灌，不扩大权限。

模型输出先经过严格 JSON Schema，再由服务端重新解析节点、关系、条件和未决问题。每个候选证据必须指向本轮已读文件，行区间合法且原文片段精确存在；之后重新生成 candidateId 并运行共享图 lint。只有全部校验通过才调用 Store 更新 pre-project candidate；人工编辑存在时写入带 `parserVersion=llm-v1` 的 `manual-vs-reparse` 冲突，不自动覆盖。运行记录持久化状态、模型、聚合 token、读取路径/字符数、上下文模式、缓存状态、修正次数和诊断，不持久化完整正文或模型原始输出；服务重启会把遗留 running 记录标为失败。单次运行按路径复用上下文，跨运行缓存以冻结文件 SHA-256 为唯一键并保存在 Studio 私有目录。首次读取建立最多 6,000 字符、带 `L<原始行号>` 的确定性摘要；后续种子读取命中时先发送摘要，同路径按需读取把上下文提升为最多 20,000 字符的全文。服务端仍逐次从 Store 读取并复验原文件 hash；摘要证据仅允许引用摘要列出的原始行，提升后才允许全文行范围，最终 snippet 始终在冻结全文中再次核验。

---

## 5. 1.0 图 Schema

### 5.1 可扩展类型注册

1.0 图 Schema 只执行核心类型注册表中的确定性行为。Engine 导出节点和边注册表，图加载、Runtime、解析器、Generic Export 和 UI 都读取这一份事实，不在各处复制类型清单，也不按名称或固定 ID 推断行为。

核心节点类型：

| 类型 | 语义 |
|---|---|
| start | 流程起点 |
| end | 流程终点 |
| step | 信息收集或一般步骤 |
| decision | 根据结构化结果选择后续边 |
| gate | 必须等待真实用户回答的确认节点 |
| lookup | 执行声明式图查询或上下文组装 |
| dispatcher | 按队列或结构化路由值调度 |
| action | 表达待执行工具或操作；未完成沙箱时只展示 |
| terminal | 某条子路径结束，可显式引用后续调度节点 |
| knowledge | 通用知识/文档节点，不作为可执行流程步骤 |

1.0 明确定义的节点通用字段包括：

```text
id / kind / title / description / doc / docAnchor /
lookup / position / extensions
```

`extensions` 和导入时未识别的普通 JSON 字段在加载、ChangeSet 编辑、保存和导出往返中不得丢失。它们是惰性数据，不获得运行、工具调用、条件或 UI 组件执行语义；服务端对每个节点或边的未知字段与 `extensions` 合计设置 64 KiB 上限，并拒绝循环引用、非有限数字、非普通对象等有损 JSON 值。图顶层和 manifest 的未知 JSON 字段也随完整对象保留。

1.0 只有核心注册表中的内置行为类型可以进入可执行流程。未注册节点或边类型由 lint 阻止进入 active workflow，即使未知字段中出现同名声明也不能改变这一结果。项目级类型注册、`graph/_extensions.json`、动态展示字段和元模型统一放到 2.0；1.0 只保证未知数据往返不会阻断未来迁移。

拓扑只有一个事实源：图文件中的顶层 `edges[]`。节点不得再持久化 `next/back/edges` 等重复邻接字段；UI 可以在内存中派生出入边索引，但不能回写第二份拓扑。

### 5.2 边与合法跳转

- 每条边至少包含稳定 ID、`from`、`to`、边类型以及可选条件；分支条件属于边，不属于目标节点。
- 运行时合法跳转只使用 `edges[]` 中从当前节点出发的流程边。
- 知识关联边不得被当作运行路径。
- 模型提交了未声明的下一节点时，引擎保持当前节点不变，记录 reject 事件并返回合法出口。
- 1.0 不自动重新规划、不自动改图、不自动接回流程。
- `route(mode=flow)` 只用流程边计算设计/诊断路径；知识关系查询使用独立的 inspect 模式。
- 回退和 terminal 继续调度都使用有类型的显式边，不得写死 `dispatch` 等节点 ID。

### 5.3 条件与变量

条件协议至少支持：

- 布尔值；
- 否定；
- 相等/不相等；
- 数组或字符串包含；
- 安全的嵌套字段访问；
- 与/或组合。

禁止 `eval`、`Function` 或任意脚本条件。条件使用结构化 AST 或限定 DSL。

Condition AST 是封闭协议，不采用“识别 `op` 后忽略其他字段”的宽松解析。`boolean/not/equals/notEquals/contains/and/or` 和 `literal/ref` 各自具有字段白名单；未知字段、未知操作符、非有限数字、超过 100 项的字面量数组、超过 100 个组合子条件或 32 层嵌套均在图加载和 ChangeSet 预演时返回稳定错误码及字段 path。引用只允许 `skill.*` 或 `runtime.*`，逐段使用自有属性访问并拒绝 `__proto__/prototype/constructor`。Studio、Server Runtime 与 Generic Export CLI 必须复用相同校验和求值语义。

变量分为：

```text
runtime.*   # 引擎内部状态，用户 schema 不可声明
skill.*     # skill 定义的业务变量
```

lint 必须防止两个命名空间冲突，状态清理和作废操作分别声明范围。

### 5.4 文档切片

- 优先使用精确标题路径，例如 `Usage/Advanced/Retry`。
- 模糊标题匹配只作降级；多匹配时返回冲突候选，不静默选第一个。
- 页面必须展示真实切片预览；解析确认是审核入口，不是隐藏切片错误的理由。
- 切片算法必须跳过代码围栏内的标题样式文本。

首批实现将 `docAnchor` 定义为完整标题路径或 `#anchor`。标题索引支持 ATX、Setext、显式 `{#id}`、重复标题生成锚点、Frontmatter 与代码围栏排除，并返回 1-based 起止行。只读切片 API 默认只做精确匹配；模糊标题降级必须由调用方显式开启并处理 `ambiguous` 候选。

`ChangeSetService` 在图提案、图确认重算、文档提案和文档确认四个位置校验引用：文档不存在、锚点缺失、锚点不唯一或只有锚点没有文档均为阻断错误。文档列表的引用计数和引用查询均从当前图派生，不维护第二份关系事实源。

节点的 `lookup[]` 使用以下封闭查询联合，不接受任意查询语言或代码：

```text
graph.node(nodeId)
graph.neighborhood(nodeId, direction, edgeKinds?)
graph.search(text, nodeKinds?, limit<=20)
document.slice(path, anchor, fallback=none|title)
```

queryId 在节点内稳定且唯一，单节点最多 20 条。Engine 的 `executeProjectFactQueries` 是 Runtime、Benchmark 和测试共用的纯函数；图查询严格使用冻结 `SkillGraph` 的稳定顺序，文档查询接收调用方从冻结 Snapshot 读取的允许路径内容。结果状态为 `found | empty | missing | ambiguous | degraded`。`document.slice` 先精确求值；只有 `fallback=title` 且精确结果为 missing 时才进行末级标题匹配。唯一降级命中必须携带 `requestedAnchor/resolvedPath`，多命中必须返回候选标题路径和行号。返回节点事实时移除节点自身的 `lookup` 声明，避免查询定义随邻域结果重复注入。

### 5.5 图谱页面规模策略

Web 图谱始终只读取当前 `projectId` 的单 Skill 图，不在客户端合并 Workspace 成员。`GraphProjectionAdapter` 把同一 `SkillGraph` 转成渲染节点与关系边；3D 和 2D 适配器消费完全相同的投影，不复制图语义、筛选状态或 Trace 归约逻辑。参考原始 `graph-engine-demo/graph-viewer`：3D 基于 Three.js/WebGL 的力导向空间图，2D 基于 Canvas 的力导向平面图；两者都以小型类型色节点、标题标签和关系边为主，不渲染工作流卡片。

Web 实现使用独立的 `react-force-graph-3d` 与 `react-force-graph-2d` 入口，禁止引入同时装载 AR/VR/A-Frame 的聚合包。3D 的 `nodeThreeObject` 复用原项目的 CanvasTexture Sprite：在一张超采样纹理中同时绘制类型色圆点、径向辉光、选中环和标题药丸，Sprite 永远面向相机；2D 的 `nodeCanvasObject` 使用相同颜色、半径、辉光和标签规则。`SkillGraphCanvas` 将原版 46px 工具条与渲染平面拆成两个稳定区域：主图谱由路由提供完整工具条，导入候选等嵌入场景由画布提供同风格的标题、210px 搜索、模式、主题和适应控件；两者使用同一渲染组件和参数，不维护第二套图谱 UI。图谱容器暴露模式、稳定状态和投影节点/边计数供 E2E 读取，WebGL 验收启用保留绘图缓冲并直接检查像素，不能用 DOM 占位元素代替场景成功证据。

`position` 只作为 2D 初始种子和用户明确固定布局的坐标，不控制 3D 的 z 轴，也不把力导向图重新排成流程泳道。切换 2D/3D 时保留 nodeId 选择、查询、节点/边类型过滤和两跳聚焦集合，同时清空不应跨投影保留的 hover 状态；渲染器内部坐标是临时视图状态，不写回 active 图。首次进入使用 3D，记忆本机模式；移动端首次进入和 WebGL 初始化失败时显式降级 2D。3D 场景必须铺满浏览器窗口的图谱画布，不放进装饰性预览卡片；图谱路由隐藏 Studio shell，只保留原版 46px 图谱工具条和返回入口。Studio 编辑命令挂在“变更”浮层和只读档案之后，不向原版顶栏注入额外控件。三个及以上节点在首次稳定和用户主动点击“适应”时调用原版参数的 `zoomToFit`；1–2 节点图不把零尺寸或近零尺寸包围盒交给 `zoomToFit`，而是从当前有限坐标计算中心和跨度，以 150 单位为最小观察距离确定性设置相机，避免单知识节点被推到视野外。2D 在动画结束后读取实际 zoom，只有超过 3.2x 时才平滑回落到该上限，防止稀疏小图破坏小型类型色节点语法，常规图布局不受影响。3D 搜索定位沿当前相机到目标节点的视线计算 150 单位观察距离，避免原版从世界原点外推算法在目标接近原点时产生相机与目标重合的空画布。

同一 `SkillGraphCanvas` 也承担 Test 与 Diagnosis 的 Trace 投影，但嵌入式画布的尺寸必须等于实际渲染容器，不能因省略 46px 主图工具条反向增加 Canvas 高度。Trace 面板桌面固定基准高度为 360px，`<=760px` 时为 300px；桌面使用 3D，移动端首次加载使用 2D。3D `zoomToFit` 后允许通过 `fitDistanceScale` 注入嵌入场景相机距离，Trace 当前取 0.72，主图和导入预览继续使用各自默认值。可见 Chrome 验收在运行拒绝、只读回放、诊断分析和修复验证四个桌面 3D 状态，以及 390px 移动 2D 状态，均等待 `data-render-state=settled`、点击适应并读取 Canvas/WebGL 像素；空白、错误尺寸或端点裁切不能只靠 Trace 状态条掩盖。

边方向由业务语义决定：`flow | condition | back | continue` 显示箭头并可被 Runtime 使用，`knowledge` 只显示关系连接及标签，不进入合法跳转集合。节点检查器从当前图确定性计算入边、出边和相邻节点，不增加另一份关系状态。搜索在当前筛选范围建立稳定 ID 序列；2D 居中缩放到节点，3D 将相机飞向节点；双击节点按无向邻接关系生成两跳聚焦子图，但不会改变运行方向或图数据。

节点数超过 200 时进入显式大图模式：两种渲染器均关闭粒子和持续动画、降低远距离标签密度并缩短力模拟冷却时间；节点、边和筛选事实仍保持完整，不以抽样或聚合节点替代真实图。切换 `projectId/activeRevision` 时清空查询、筛选、聚焦、节点/边选择和变更预览，防止前一 Skill 的界面状态泄漏。

`scripts/chrome-graph-scale-verify.mjs` 必须扩展为 500 节点/499 边的 2D 与 3D 双模式可见 Chrome 验收：实际操作首/中/末搜索定位、节点/边类型筛选、两跳聚焦、关系跳转、缩放、平移/旋转和模式往返；除 DOM 与交互断言外，还要检查 Canvas/WebGL 非空像素、桌面/移动端取景和无重叠。环境、阈值、观测时延和截图写入结构化报告；该记录只证明当前环境，不代替 T39 的 Windows/macOS 对照。

### 5.6 图编辑草稿与问题定位

力导向画布不承担流程编排器 handle 语义。新增边由工具栏或节点档案打开端点表单，当前节点可作为预填起点，用户显式选择终点；边 ID 由端点生成可修改的稳定候选。条件边正文先使用 `JSON.parse` 得到结构化值，再调用 engine 的 `validateCondition`，只有完整通过才进入 `draftGraph`。边检查器修改条件采用同一原子表单，非法中间文本不会覆盖已有条件。

批量节点编辑先在弹窗中形成显式 nodeId 集合和有限字段 patch，再一次性映射到 `draftGraph`。Lint 仍由 engine 的 `lintGraph` 计算；Web 只把 `nodes[index]`、`nodes.<id>`、`edges[index]` path 映射回 UI 目标，不复制校验规则。定位动作选中并居中对象，字段级问题进一步聚焦对应控件。快速修复必须是确定性的草稿变换，首批仅允许删除 lint 已指明的自环边，不自动补边、改类型或提交 ChangeSet。

页面草稿的节点/边 CRUD、端点表单、条件和批量操作最终统一经 `graphOperations` 转成白名单操作；server 仍在 ChangeSet 预演和确认阶段重新应用并 lint。图谱 renderer 只读取草稿投影，不能绕过 ChangeSet 写项目。`scripts/chrome-graph-editor-advanced-verify.mjs` 使用可见 Chrome 操作完整链路，并直接读取 store 证明确认前 active 图不变、确认后只出现预览中的修改且刷新可恢复；结构化环境和结果保存为 `graph-editor-advanced.json`。

---

## 6. 核心数据对象

### 6.1 ProjectRevision

```json
{
  "id": "rev-20260727T120102.123",
  "projectId": "project-...",
  "skillId": "skill-018f...",
  "parent": "rev-...",
  "contentHash": "sha256:...",
  "createdAt": "...",
  "source": "manual | import | parse | changeset"
}
```

`contentHash` 覆盖 skill.json、SKILL.md、graph、项目内其他 Markdown 和影响运行的资产内容。已知文本文件先统一 UTF-8、BOM、LF、Unicode NFC 和 POSIX 相对路径；二进制文件按原始字节计算，不能做文本归一化。

### 6.2 ChangeSet

```json
{
  "id": "change-...",
  "workspaceId": "workspace-...",
  "projectId": "project-...",
  "skillId": "skill-018f...",
  "baseRevision": "rev-...",
  "operations": [
    { "op": "graph.node.update", "target": "flow.confirm", "value": {} },
    { "op": "docs.write", "target": "docs/confirm.md", "value": "..." }
  ],
  "reason": "...",
  "source": {
    "kind": "manual | assistant | diagnosis | report | runtime | import | system | legacy",
    "sourceId": "assistant-session-...",
    "label": "设计助手"
  },
  "evidence": [
    { "kind": "user-request", "ref": "assistant-session-...", "summary": "..." },
    { "kind": "graph", "ref": "flow.confirm", "summary": "..." }
  ],
  "validation": { "lint": [], "warnings": [] },
  "previewDiff": {},
  "digest": "sha256:...",
  "status": "proposed | rejected | applied | conflicted",
  "conflict": {
    "code": "revision_conflict",
    "message": "项目版本已变化，原提案不能继续应用",
    "detectedAt": "...",
    "baseRevision": "rev-...",
    "currentRevision": "rev-..."
  },
  "rejectedAt": "...",
  "rejectionReason": "..."
}
```

`source` 和 `evidence` 是审阅追溯元数据，不是授权或可信度证明。内部自动生成路径在服务端写入归属：设计助手使用 sessionId，诊断修复使用 diagnosisId，报告候选使用 reportImportId，运行候选使用 runId，撤销使用当前 revision。通用 ChangeSet API 可以接收调用方声明的元数据，但任何写入权限、项目边界、lint 和确认条件仍只由服务端上下文裁决；界面不得把来源标签渲染成“已验证”。历史记录读取时回填 `legacy + []`，不猜测旧提案依据。

确认摘要 `digest` 对 `workspaceId + projectId + skillId + baseRevision + operations + reason + source + evidence` 的结构化载荷计算 SHA-256。来源或证据的任一变化都会产生新摘要，因此确认请求不会把相同操作但不同依据的提案视为同一份审阅对象。preview 是服务端根据同一操作和基础版本生成的审阅投影，不作为独立客户端输入进入摘要。证据最多 20 条，字段长度和类型由服务端白名单校验；模型给出的事实先通过其本身的有界上下文引用校验，再映射进 ChangeSet。

操作只能使用白名单：

```text
graph.node.create/update/delete
graph.edge.create/update/delete
docs.write/rename/delete
project.restore
skill.update
benchmark.case.write/delete
asset.copy/delete
```

1.0 的首个 `skill.update` 切片固定 `target: skill.json`，value 只允许 `name/version/description`。服务端拒绝任何附带 `skillId/capability/entry` 或未知字段的操作；预演生成完整 manifest 前后状态与 `changedFields`，确认时重新读取 `skill.json` 比对，成功后和其他 ChangeSet 一样原子写文件、捕获 Snapshot、推进 revision。Workspace 成员名称从确认后的 manifest 重新读取，不维护第二份可编辑名称事实。

用户确认绑定 `digest + baseRevision`。确认操作本身就是事务提交入口，不再要求第二次“应用”；提交前如果基础 revision 已变，返回冲突而不是覆盖。

拒绝同样绑定 `changeSetId + digest + baseRevision`，只允许 `proposed -> rejected`。服务写入 `rejectedAt` 和可选 `rejectionReason`，不要求 active revision 仍等于 baseRevision，因为拒绝陈旧提案仍是合法的清理动作；拒绝不进入项目事务、不生成 Snapshot/Revision。`rejected` ChangeSet 不能再次确认或重复拒绝。关闭预览不调用拒绝 API，也不改变持久化状态。

DiagnosisRepair、ReportBenchmarkCandidate 等 Studio 私有来源记录不复制 ChangeSet 状态机的事实源。专用确认/拒绝服务在改变 ChangeSet 后同步写入来源记录；列表读取再根据关联 ChangeSet 派生 `proposalStatus` 或候选状态，以修复通用 ChangeSet 入口、旧版本数据或写入中断造成的展示漂移。拒绝后的来源记录只读保留，重新生成会创建新记录和新 ChangeSet，不复用已终止提案。

所有确认路径在发现事实漂移时持久化结构化 `conflict`，至少包含稳定 code、说明、发现时间、原 baseRevision 和当前 revision；可选 details 只保存已经由服务端计算的结构化 lint/绑定问题。Web 在确认返回错误后重新读取 ChangeSet，只有事实源状态确为 `conflicted` 才进入共用冲突面板。`POST /changesets/:id/repropose` 必须再次校验旧 digest + baseRevision，只接受 conflicted 状态，并以当前 active revision 重新执行原白名单操作、lint 和 diff；撤销提案则重新计算当前 revision 的直接父 Snapshot。响应只返回新的 proposed ChangeSet，不应用项目、不改写旧记录，也不执行自动三方合并。

ChangeSetService 必须验证 projectId 当前解析出的 skillId 与提案一致。1.0 禁止一个 ChangeSet 包含多个 projectId；跨 Skill 修改需要分别确认，不能伪装成原子事务。

### 6.3 Snapshot/Baseline

snapshot 覆盖节点、边、删除项、文档、SKILL.md、扩展 schema 和会影响导出的资产清单。baseline 只表示“上次已阅快照”。

每个确认提交生成一个 `ProjectRevision`，记录 `revisionId / parentRevision / contentHash / snapshotId / changeSetId`；Snapshot Manifest 记录项目内每个文件的规范路径、大小和内容 hash，文件副本与 manifest 一同保存在 Studio 私有目录。Workspace 只展示成员的 active revision，不拥有统一 revision。

最近一次撤销只允许以当前 active revision 的直接 `parentRevision` 为目标。服务生成内部白名单操作 `project.restore`，把目标 `revisionId + snapshotId + contentHash` 和当前到目标的完整文件差异写入 ChangeSet digest。确认时重新校验两个 Snapshot 的身份、逐文件大小/hash、核心文件、live 工作树 contentHash 和文件差异；任一事实变化即把 ChangeSet 标记为 conflicted。提交逐文件恢复目标 Snapshot，失败时用当前 Snapshot 回滚；成功后捕获一个新的 `source: undo` revision，其 parent 指向撤销前 revision，而不是把 active 指针移回旧 revision。项目 `.git` 与系统元数据始终排除，in-place 仓库边界不变。

`ack` 必须携带用户实际审阅的 `revisionId + snapshotId`；任一项与当前事实不一致时拒绝前进 baseline。确认基线不创建新 revision、不修改 Skill 文件，baseline 也不进入导出包。相对基线的差异按文件内容 hash 计算为 `added / modified / deleted`。

### 6.4 RuntimeArtifact

```json
{
  "schemaVersion": "1.0",
  "artifactId": "artifact-...",
  "workspaceId": "workspace-...",
  "projectId": "project-...",
  "skillId": "skill-018f...",
  "revision": "rev-...",
  "contentHash": "sha256:...",
  "initialVariables": {},
  "fingerprint": {
    "schemaVersion": "1.0",
    "algorithm": "sha256",
    "projectContentHash": "sha256:...",
    "inputHash": "sha256:...",
    "value": "sha256:..."
  },
  "createdAt": "...",
  "graph": {}
}
```

RuntimeArtifact 是单 Skill 自包含只读快照。所有运行步骤只读该 artifact，不重新加载可变项目；Workspace 当前选择变化不会改变它。1.0 不创建 WorkspaceArtifact。

`projectContentHash` 直接使用 revision 的内容 hash；`inputHash` 对受限 JSON 输入做确定性序列化后计算 SHA-256，对象键按字典序排列、数组保持顺序，并拒绝循环引用、非有限数字和非 JSON 对象。总指纹为带版本域分隔的 `sha256(projectContentHash + inputHash)`，不包含 artifactId、runId、时间或本机路径。因此同一项目内容与语义相同的输入得到相同指纹，但每次执行仍落到独立 Artifact 文件。普通调试运行与 Benchmark 必须复用同一生成器。

`ProjectRun` 只保存 Artifact 身份、绑定 revision、运行状态和 Trace；详情读取时再按 `projectId + artifactId` 加载冻结事实。旧格式 Artifact 若缺少输入与指纹，读取层以空输入和既有 contentHash 兼容回填，不重写历史文件。active revision 与 run.revision 不同时，Web 只显示版本漂移和显式重建动作；重建会复制用户确认的初始变量并重新调用创建运行接口，绝不修改旧 run 或旧 Artifact。

RuntimeArtifact 采用显式清理，不在服务启动、运行创建或 Workspace 切换时自动删除。Store 以 7 天为固定孤立宽限期，并同时聚合普通 `ProjectRun.artifactId` 与 `BenchmarkRunRecord.fingerprint.runtimeArtifactId` 两类结构化引用；任何仍被引用的 Artifact 永久保护。只有身份与时间字段有效、没有两类引用且早于截止时间的文件可以删除。JSON 损坏、文件名与内容身份不一致、Workspace/Project/Skill 绑定不一致的文件只计入 `invalidCount`，引用存在但文件缺失则计入 `missingReferencedCount`，两者都不由清理操作自动修复或删除。

清理在 Store 写操作串行边界内先扫描，再在每次 `unlink` 前重新读取候选文件，复核 schema、artifact/workspace/project/skill 身份、创建时间与截止时间；候选已变化、消失或转为受保护时跳过。响应同时返回清理前后统计、删除 ID 和释放字节。该操作只维护 Studio 私有缓存，不创建 ChangeSet、Snapshot 或 Revision，也不写 Skill 仓库。固定指纹向量由 Store 测试读取，并在 CI 的 Linux、macOS、Windows Job 上执行；只有三套 Job 对同一项目规范字节、CRLF/中文输入和对象键重排都得到相同预期值，才算跨系统指纹矩阵通过。

### 6.5 ExecutionTrace

TraceStore 在 `<studio-data>/traces/<projectId>/<runId>.ndjson` 保存按运行隔离的追加日志，目录与文件保持 Studio 私有权限。每批事件只有在身份、严格连续 seq 和 Run 绑定一致时才能写入；单批以一次 append 写入并 `fsync`。Trace 查询与 WebSocket 推送从该日志读取，并以严格递增的 `seq` 作为增量游标。

`traceEventRegistry` 是事件类型、domain、默认敏感级别和结构字段白名单的唯一注册表。当前覆盖 engine、condition、document、conversation、llm/model、tool、sandbox、benchmark、assertion 和 review。Reducer 对注册的非引擎观察事件只推进 seq，不改变图状态；未知事件保留完整持久化信封并计入 unknownEventCount。报告 default/strict 模式只导出注册结构字段，未知事件 data 默认为空；off 模式仍递归执行禁 Key/Token/Password/Authorization 规则。

RuntimeDebug 在模型请求前把当前全部条件边结果聚合为单个 `condition.evaluated`，字段仅含 edgeId/to/conditionOp/result 和合法出口；绑定文档存在时写 `document.context`，仅记录 path/anchor/status/字符数/truncated，不记录正文。真实 Benchmark 在每个决策节点执行同一观察协议。

1.0 的 ProjectRun JSON 继续保留完整事件镜像，作为旧数据兼容和双存储异常恢复事实。命令先原子写入 Run，再把缺失后缀同步到 TraceStore，成功后才广播；若日志不存在、尾部被截断、比 Run 超前或任一既有事件不同，读取层用完整 Run 原子重建日志，不能用日志反向覆盖运行状态。该兼容镜像后续只能通过带迁移与故障恢复方案的版本升级移除。

```json
{
  "seq": 42,
  "runId": "run-...",
  "workspaceId": "workspace-...",
  "projectId": "project-...",
  "skillId": "skill-018f...",
  "spanId": "span-...",
  "parentSpanId": "span-...",
  "at": "...",
  "actor": "user | model | engine | tool | server",
  "type": "engine.enter",
  "artifactId": "artifact-...",
  "data": {},
  "sensitivity": "public | project | sensitive | secret"
}
```

事件域：

```text
engine.*        进入节点、结构化结果、拒绝、调度、结束
conversation.*  用户可见发言和模型可见回答
llm.*           请求元数据、结构化输出、错误、token、缓存和延迟
tool.*          工具调用、结果、错误和权限
sandbox.*       工作区、进程、文件产物和网络事件
benchmark.*     断言、人工结论和不完整 fixture
proposal.*      ChangeSet 生成、确认、拒绝和应用
diagnosis.*     候选原因、证据和验证过程
```

不保存、不要求也不展示模型私有思维链。

### 6.6 RuntimeFingerprint

```json
{
  "engine": "...",
  "llmProvider": "...",
  "model": "...",
  "modelParams": {},
  "promptHash": "sha256:...",
  "toolSchemaHash": "sha256:...",
  "exportProfile": "generic/1",
  "sandboxPolicy": "...",
  "platform": "..."
}
```

`contentHash`、RuntimeFingerprint 和 baseline 是三个不同概念，不得统称“skill 版本”。

### 6.7 Diagnosis

```json
{
  "id": "diagnosis-...",
  "runId": "run-...",
  "symptoms": [],
  "candidates": [
    {
      "category": "skill-design | knowledge-document | model-noncompliance | generic-integration | sandbox-environment | designer-tool | insufficient-evidence",
      "evidenceSeq": [12, 18],
      "explanation": "...",
      "confidence": "low | medium | high",
      "alternatives": [],
      "verification": []
    }
  ],
  "proposedChangeSet": "change-..."
}
```

Diagnosis 是可解释推断，不是 Trace 事实。无证据时必须返回 `insufficient-evidence`。

### 6.8 BenchmarkCase

```json
{
  "schemaVersion": "1.0",
  "caseId": "case-...",
  "skillId": "skill-018f...",
  "title": "...",
  "status": "draft | ready",
  "intent": "...",
  "fixture": {
    "initialVariables": {},
    "userReplies": [{ "nodeId": "flow.confirm", "message": "确认" }]
  },
  "expected": {
    "path": { "mode": "subsequence | exact", "nodeIds": [] },
    "terminal": { "status": "completed | stopped", "nodeId": "flow.end" },
    "variables": {},
    "artifacts": [],
    "toolResults": [],
    "forbiddenEffects": []
  },
  "tags": [],
  "notes": "",
  "source": {
    "kind": "bug-report",
    "reportImportId": "report-import-...",
    "reportId": "report-...",
    "sourceRunId": "run-..."
  }
}
```

运行结果状态为 `needs-review / passed / failed / inconclusive`。缺少用户回答时记录 `fixture-incomplete`，不自动算作通过。

用例文件固定为 `benchmarks/cases/<caseId>.json`。首批写入和删除使用 `benchmark.case.write | benchmark.case.delete` 操作并进入单 Skill ChangeSet；确认时重新读取当前图并校验身份、节点、路径可达性和断言结构。列表遇到外部写入的无效 JSON 或 Schema 时保留条目和诊断，不让一个坏文件阻断其他用例。

普通运行候选端点为 `POST /api/projects/:projectId/runs/:runId/benchmark-candidate`，请求必须携带 workspaceId。Store 校验 Workspace 成员、project/skill/run/artifact 身份和终态，只接受 `completed | stopped`；候选读取 RuntimeArtifact.initialVariables、严格按 seq 的 `engine.enter` 路径、最终状态与 skillVariables。返回的 `RuntimeBenchmarkCandidate` 保存来源 run/artifact/revision 供 Studio 页面说明，但其中的 `case` 不包含这些私有身份。候选不落项目、不创建 revision；Web 将 case 装入普通编辑器，后续仍调用 `benchmark.case.write` ChangeSet。因此候选生成、用户审阅和项目写入之间不存在隐藏直写通道。

`BenchmarkHumanReview` 属于 `BenchmarkRunRecord.humanReviews[]`，以追加记录保存人工结论和备注；它不是 BenchmarkCase 字段。自动断言、人工判定和技术运行状态保持三个独立事实，任一层都不能覆盖另两层。

---

## 7. Engine 公共 API

1.0 Engine 公开确定性纯函数，不直接读写目录、调用模型或执行脚本：

```js
lintGraph(graph)
validateCondition(condition)
evaluateCondition(condition, { skill, runtime })
createRuntimeState(graph, skillVariables)
availableTransitions(graph, state)
evaluateTransitionConditions(graph, state)
advanceRuntime(graph, state, requestedNodeId)
pauseRuntime(graph, state)
resumeRuntime(graph, state)
stopRuntime(graph, state)
sliceDocument(markdown, anchor)
reduceTrace(graph, events)
```

运行命令统一返回 `RuntimeCommandResult`：`accepted + state + allowedTransitions + events + rejection?`。拒绝是确定性业务结果，不使用异常表示；图无效或目标事实损坏才抛出带 code 的 `RuntimeEngineError`。Server 负责把 RuntimeArtifact、Run 与 Trace 原子持久化，并把瞬时结果投影为 `ProjectRunView.commandResult`。

通用导出物附带同语义的零依赖 CLI：`inspect`、`transitions` 以及 `run start/status/next/pause/resume/stop`。CLI 状态文件由调用方指定并位于 Skill 目录之外，不属于 Studio 私有 Run，也不冒充 Workspace 会话。

---

## 8. Server 核心服务

### 8.1 WorkspaceService

- 创建、重命名、打开、列出和删除 Workspace 私有清单；
- 添加、移除和排序 WorkspaceMember，维护 selectedProjectId；
- 聚合各成员的 capability、Git、lint、active revision、最近运行和测试摘要；
- 移除成员默认只删除引用，不删除 Skill 文件或 Git 仓库；
- 拒绝重复 skillId，成员失联时保留记录并显示修复入口。

Workspace 生命周期 HTTP 契约为：`POST/GET /api/workspaces`，`GET/PATCH/DELETE /api/workspaces/:workspaceId`，`POST /api/workspaces/:workspaceId/members`，`PUT /api/workspaces/:workspaceId/members/order`，`POST /api/workspaces/:workspaceId/members/:projectId/repair`，`DELETE /api/workspaces/:workspaceId/members/:projectId` 和 `POST /api/workspaces/:workspaceId/select`。排序请求必须是当前成员 projectId 的无重复完整排列；删除响应返回已保留项目清单，便于 UI 明确说明非级联语义。

### 8.2 SkillProjectService

- 创建、复制导入、原地打开和关闭 Skill Project；
- 将每个真实路径单独授权并解析为不透明 projectId；
- 管理 draft/active 状态和 `managed-copy | in-place` 模式；
- 验证 skillId 稳定性与 Workspace 内唯一性；
- 监测外部文件修改，生成新 snapshot 并标记未审变更。

### 8.3 ChangeSetService

- 校验操作白名单；
- 基于 baseRevision 在隔离预演目录应用操作；
- 运行 lint、文档引用检查和影响分析；
- 生成 previewDiff 和 digest；
- 确认时重新比对 baseRevision；
- 拒绝时比对 digest 和 baseRevision，只持久化拒绝状态且禁止后续应用；
- 使用 journal + 临时目录 + 原子替换提交多文件变更；
- 启动时恢复或回滚未完成 journal。

文档生命周期使用白名单操作 `docs.write | docs.rename | docs.delete`，操作名表示文档域而不是固定 `docs/` 目录。目标可为项目内任意 `.md` 相对路径，但必须满足 NFC、POSIX 分隔、500 字符上限、无空段/`.`/`..`、无控制字符和 Windows 非法字符/设备名等三系统共同安全规则。`docs.rename` 的 value 是新 Markdown 路径，服务拒绝已有目标并从当前图确定性改写引用；`docs.delete` 不接受 value，服务确定性清除引用节点的 `doc/docAnchor`。两者都生成 document preview 和 graph preview，确认时重新比较源文档字节、目标不存在状态、整份 graph 和 baseRevision。文档路径、目标文档和 `graph/main.json` 作为同一事务的回滚集合，全部写入成功后才捕获一个 snapshot/revision；根级 `SKILL.md` 受保护。

项目事务 journal 使用 `prepared -> files-written -> revision-captured -> state-committed -> completed` 耐久阶段。对不能作为单次文件系统操作完成的文档重命名，每个子步骤必须先在 `prepared` journal 持久化 `fileMutation`，再执行对应字节变更。当前只允许 `document-rename-destination | document-rename-source | document-rename-graph`，分别表示目标文档写入、源文档移除和图引用更新；进入 `files-written` 后清除活动 `fileMutation`，因为之后的恢复边界由顶层 stage 表达。

启动时遇到未完成 journal，服务必须核对 live 项目、active revision、候选 revision 和 ProjectState：已完成全部耐久事实的事务可补全为 `completed`；其他情况恢复 base Snapshot、移除半成品 revision、恢复原 ProjectState，并将 ChangeSet 标记为 `conflicted`。回滚 journal 进入 `recovered`，保留 `recoveredFromStage`；如中断时存在活动文件步骤，同时保留 `recoveredFromFileMutation`。Web 只将这些字段投影为审计记录，不从文案反推恢复状态。

文档列表 API 递归扫描项目内安全 Markdown，跳过隐藏目录和 `node_modules`，不跟随符号链接，只返回路径、大小、更新时间和引用计数；正文在用户选中后单独读取。节点 `doc` 和 `document.slice.path` 复用 engine 的同一 `isSkillDocumentPath` 校验，避免编辑、lint 与冻结运行形成不同路径口径。首版 Web 对 100 篇规模直接渲染轻量列表并在内存中按规范化路径过滤，不因搜索重新请求全部正文。专项脚本使用 100 篇关联文档加 `SKILL.md` 的真实原地项目，通过可见 Chrome 检查完整列表、首/中/末搜索、打开正文和移动端布局，并把环境、阈值与观测值保存为 `document-scale-100.json`；该数据只证明记录环境，不代替 T39 跨平台验收。

资产域使用 `asset.copy | asset.delete`，每个 ChangeSet 只包含一个资产操作且不能与图、文档、Skill 信息或 BenchmarkCase 混合。目标路径经 NFC 归一化后必须位于 `assets/**`，拒绝反斜杠、绝对路径、空段、`.`/`..`、控制字符、Windows 保留字符和设备名、尾随点/空格以及超限路径；该共同子集确保项目可在 Windows、macOS 和 Linux 往返。`asset.copy.value.contentBase64` 必须是规范 Base64，解码后非空且不超过 2 MiB。服务端从扩展名确定展示 MIME，按原始二进制字节计算 SHA-256，不信任浏览器声明类型。

资产列表只返回路径、大小、MIME、hash、更新时间和引用计数，用户选中后才读取 Base64 正文。引用影响复用结构化 Markdown AST 扫描，记录源路径、1-based 行号、引用类型和原目标；创建时也保留尚缺文件的引用，删除不重写 Markdown。预演只保存前后 `AssetFileFact` 和引用清单，二进制只保留在受 digest 保护的 operation 中。确认重新读取当前字节、重算 hash 和引用清单；任何漂移都把原提案标记为 conflicted。提交使用同目录临时文件原子替换或精确 unlink，捕获完整 Snapshot 后推进 active revision，失败时按原始 Buffer 回滚。图片预览的 CSP 只额外允许 `img-src 'self' data:`，Web 仅渲染 PNG/JPEG/GIF/WebP，SVG 不进入图片预览路径。

### 8.4 IngestService

```text
复制源目录到 Skill 管理项目目录，或授权原地打开
-> 检测 native / foreign
-> 清点全部资产
-> 静态解析或 LLM 解析
-> 生成 ChangeSet
-> 用户确认
-> 转为 active revision
```

静态解析：

- 用结构化解析器读取 SKILL.md 的 YAML/TOML/JSON frontmatter 和明确的 Workflow/Steps 章节；
- 清点 Markdown、JSON、配置、文本、scripts、assets、未知文件和相对引用；
- 为文档和知识内容创建可视节点；
- 只在有明确流程证据时生成流程节点；
- 未提取流程时设为 `content-only`，不构造假流程；
- 格式、身份、能力和图推断记录 provenance 和文档证据；
- 外部、缺失和越界引用只诊断，不发起网络或目录外读取；未命中的 inlineCode 路径样文本仅记录为候选，不冒充结构化缺失引用。

LLM 解析：

- 使用服务端固定解析协议和有限权限的冻结文本读取能力；
- 先提供确定性清点、静态候选和少量种子文件，再按需分批读取；
- 单次运行内复用已读文件；跨运行按冻结文件 hash 复用带原始行号的摘要，证据不足时显式升级完整正文；
- 节点、边、条件和未决问题都记录可复核证据来源；
- 结果只更新 pre-project 导入候选，不直接写入 active 项目。

### 8.5 DesignAssistantService

设计助手仅获得以下受控能力：

```text
workspace.getSummary
project.getSummary
schema.get
graph.getNode / graph.search / graph.getNeighborhood
docs.list / docs.read / docs.slice
benchmark.list / benchmark.get
diagnosis.get
proposal.createChangeSet
```

`workspace.getSummary` 只返回成员摘要。其余工具必须绑定当前 projectId；1.0 的助手一次会话只能为一个 Skill 生成 ChangeSet。不提供任意文件写入、任意路径或任意 shell 工具。

`DesignAssistantService` 使用服务端编排的有界只读循环，而不是向模型暴露开放工具：会话持久化 workspaceId/projectId/skillId，消息时重新复核成员身份和 active revision；图不超过 100 节点时可整体提供，超过时必须由请求明确命中节点；正文首轮只读取需求直接命中的最多三个文档，Benchmark 默认只提供索引，点名 case 才读取正文。其他 Workspace 成员只暴露摘要。

模型结构化动作可为 `read | clarify | propose`。`read` 只允许 `graph.node / graph.neighborhood / graph.search / docs.read / docs.slice / benchmark.get`，由服务强制绑定会话 projectId 并复核文档和 case 索引；最多 3 次模型调用、10 次读取、每次 5 项，工具结果累计限长。索引外请求以拒绝结果回灌，不读取任意路径；active revision 在读取期间变化则终止整个请求。原始读取内容只存在于当次内存输入，持久化消息只保存工具名、ref、状态、字符数和诊断。各次调用 usage 与耗时相加后写入最终消息，页面展示调用次数。项目内容和工具结果被明确标记为不可信数据，不能覆盖系统指令。

会话枚举必须先校验 workspaceId，并只返回最多 100 条摘要；可选 projectId 还要复核它确属该 Workspace。完整消息通过 sessionId 单独读取。进程内为每个正在生成的会话保存唯一 `AbortController`，在注册控制器后才进行异步探测和读取，从而原子拒绝同会话并发请求。取消端点幂等触发控制器；即使 Provider 在 abort 后返回，服务也会在解析输出和创建 ChangeSet 前再次检查信号。取消轮次持久化用户请求和无模型 usage 的 `cancelled` 回复，不创建提案。

Provider 的严格结构化输出固定为 `action/reply/evidence/operations`。操作使用 `op/target/valueJson` 信封，服务解析 JSON 后仍交由 `WorkspaceStore.createChangeSet` 的白名单、项目成员、baseRevision、路径、图 lint、文档绑定和 Benchmark lint 校验。evidence.ref 必须存在于本次服务端上下文的允许集合。`clarify` 禁止携带操作，`propose` 必须同时存在证据和操作。模型无法直接调用 `confirmAndApplyChangeSet`。

Web 抽屉保存当前会话对象而不是读取 Workspace 的 selectedProjectId 作为消息目标；页面切换成员只触发目标漂移提示。图、文档和 BenchmarkCase 页面监听 `skill.activeRevision`，因此设计助手或其他跨页面 ChangeSet 应用后会重新读取事实源，而不是继续展示旧缓存。

### 8.6 LLMProvider

```ts
interface LLMProvider {
  probe(): Promise<ModelProviderCapability>;
  invoke<T>(request: ModelInvocationRequest, signal): Promise<ModelInvocationResponse<T>>;
}
```

Provider 层负责消息、结构化输出、工具调用、token 计量、取消和错误分类，不负责 Agent 包格式或沙箱。

当前首个适配器是宿主侧 `OpenAIResponsesProvider`，但上述接口、Benchmark 记录和 Skill 文件都不绑定 OpenAI。`ModelSettingsService` 实现同一接口并在每次调用时解析项目外配置和凭据，因此设计助手与 Benchmark 无需重启即可使用新模型设置。适配器固定请求官方 Responses endpoint，不接受用户提供的任意 base URL；请求使用 `store: false` 与 `text.format` 严格 JSON Schema。响应遍历所有 message/output_text 项，记录 input/output/total/cached/cache-write/reasoning token；Provider header、Key 和原始请求正文不进入 Trace。默认模型配置为 `gpt-5.6-terra`，实际请求模型与 API 返回的 resolved model 分别冻结。

模型设置 API 位于 `/api/settings/model`，沿用 loopback、Origin 和随机会话令牌保护。GET 只返回 Provider、模型、超时、凭据状态/来源及最近一次连通性诊断；PUT 的 `apiKey` 只在请求内存和系统凭据写入路径中短暂存在，配置 JSON 不含密钥；DELETE 只删除本地凭据，不修改环境变量。连通性端点使用 `GET /v1/models/{model}`，不执行生成，并只对可重试故障重试一次。生成调用具有独立超时和 AbortSignal，但保持零自动重试。

内部测试可以实现 scripted/recorded provider 验证协议，但该结果不得进入用户的 Skill Benchmark 通过率。

### 8.6.1 RuntimeDebugService

`RuntimeDebugService` 以 `workspaceId + projectId + runId + artifactId` 锁定一条简化运行对话。上下文来自 run 绑定的不可变 RuntimeArtifact；当前节点文档和 `lookup[]` 引用文档都通过 run.revision 对应 Snapshot 读取，不读取 active 工作树。单轮输入最多 4,000 字符，冻结节点文档最多 24,000 字符，声明式项目事实序列化结果累计最多 24,000 字符，最近对话最多 12 条且累计 24,000 字符。项目内容和历史对话均标记为不可信数据。

Provider 严格输出 `action: reply | advance | stop`、用户可见 reply、可空 nextNodeId 和 summary。服务端重新解析动作一致性；`advance` 不预先把 allowedTransitions 当作输出白名单，而是把模型真实提交交给 Runtime Engine，使非法决策形成可观察的 `engine.reject`，当前状态不变且不发生自动回归路线。每轮只调用模型一次、零自动重试；页面“重试”创建新的一轮，不覆盖旧消息。

Store 提供受限 `appendRuntimeTrace`，只接受 `condition.evaluated / document.context / context.queried / conversation.user/assistant / llm.request/response/error`，每批最多 6 条、单条 data 序列化后最多 16,000 字符，并连续推进同一 run 的 eventSeq。`context.queried` 只保存查询 ID、类型、状态、结果字符数和可选降级路径，不保存查询结果正文。模型调用前后分别携带预期 artifact/currentNode/eventSeq；期间人工命令造成漂移时返回 `runtime_context_changed`，模型决策不应用。对话正文和展示所需模型 usage 在 Studio 私有 `runtime-dialog/<projectId>/<runId>.json` 持久化；Trace 保存可回放事件，不保存 Key、原始 Provider 请求或思维链。

### 8.7 GenericExportProfile

```ts
interface ExportProfile {
  id: string;
  validate(project, revision): ValidationResult;
  build(project, revision, destination): ExportResult;
}
```

1.0 只实现 `generic/1`：

- 复制上传清单中的全部原文件，并在需要时生成 skill.json 和 graph；
- 附加零 npm 运行时依赖的 engine CLI；
- 生成资产清单和 contentHash；
- 不生成 Claude/Codex 专用 manifest 或安装文件；
- 不修改 skill 业务逻辑；
- 导出前展示文件清单和警告。

实现时导出预览记录必须冻结 `workspaceId + projectId + skillId + revisionId + snapshotId + contentHash + files + digest`。生成 ZIP 时只从 Snapshot 私有副本取文件，不重新读取 active 工作目录；确认阶段再次检查 active revision 与 snapshot，变化则把导出记录标记为 `conflicted`。归档内的 `export-manifest.json` 不包含 workspaceId/projectId、本机路径、Baseline、Trace、RuntimeArtifact 或其他 Studio 私有数据。

生成器在归档中附加 `engine/skill-engine.mjs` 与 `engine/README.md`。CLI 以 Node 20 ESM 单文件交付，不依赖 npm 包、不访问网络、不加载或执行 Skill 中的脚本。每次启动先校验 export manifest/skill manifest/graph 身份、Schema、清单安全相对路径、已注册节点/边类型、端点、入口、可达终点和安全 Condition AST；损坏结构在执行命令前返回结构化错误。`verify` 逐项读取 `export-manifest.json.files[]`，比较实际字节长度和 `sha256:<hex>`，成功返回 `valid + checkedFiles + totalBytes`，缺失或不一致以 `package_integrity_failed` 返回逐文件 code、预期/实际大小与 hash。清单不包含自身，避免自引用 hash；CLI 文件本身作为普通声明文件参与校验，因此该机制用于传输和落盘完整性检查，不替代发布签名或对恶意替换 CLI 的信任证明。`inspect` 读取结构，`transitions` 使用与 Studio 完全相同的 `skill.* / runtime.*` 命名空间和 Condition 比较语义计算可用出口；`run start/status/next/pause/resume/stop` 读写调用方指定的外置状态文件。状态至少冻结 `schemaVersion + skillId + status + currentNodeId + step + variables + visitedNodeIds + eventSeq + events + timestamps`，采用临时文件加 rename 原子替换并限制为当前用户读写。非法推进先追加 `engine.reject` 再持久化，但不改变节点、step 或变量；合法到达终点追加 `engine.complete`。CLI 不负责自动改图、选择替代路线或修复用户逻辑。

导出记录 API 包含按 `workspaceId + projectId` 列表、创建预览、确认生成、下载和删除。每个入口都重新校验 Workspace 成员与 Skill 身份；列表只返回该成员的记录。删除仅递归移除 `<studio-data>/exports/<export-id>` 下的私有记录、生成副本和 ZIP，不能触及项目根、Revision 或 Snapshot。Web 打开导出面板时优先复用 active revision 的未冲突记录，并允许查看、重下或清理旧 revision 记录。

导出输入必须是一个 skillId + revision。1.0 不提供 Workspace 聚合导出。

### 8.8 GitDiffService

GitDiffService 只提供只读比较：

- 发现项目所属 Git 仓库并验证其仍位于用户授权路径内；
- 默认比较工作树与 `HEAD`，可比较已有 commit/tag；
- 使用固定参数调用 Git，禁用外部 diff、pager、hook 和交互提示；
- 返回结构化文件状态、文本 diff 和二进制变更摘要；
- 不提供 add、commit、checkout、reset、clean 或其他 Git 写操作；
- 仓库不存在、基准无效或 Git 不可用时返回明确能力状态。

实现从项目 realpath 执行并计算仓库根到授权项目的前缀。`git/refs` 只枚举最多 50 个触及当前 Skill 的近期 commit 与最多 100 个 tag，tag 先剥离并验证为 commit；`git/diff` 只接受 `HEAD` 或 40/64 位十六进制 object ID，再以 `rev-parse <oid>^{commit}` 规范化，拒绝任意 ref 文本或选项注入。相对旧基准的结构化文件状态由 `diff --name-status` 与当前 `status --porcelain` 合并，因此覆盖已提交差异、暂存区、工作树和未跟踪文件；文本 patch、`numstat` 二进制识别和大小摘要使用同一 base OID。

所有结构化路径返回前剥离且复核授权前缀，patch 使用 `--relative -- <project-pathspec>`。二进制摘要最多返回 200 项并显式声明截断；基准大小通过只读 `cat-file -s` 获取，当前大小来自授权 Skill 根内的文件状态。子进程不使用 shell，清除所有继承的 `GIT_*` 环境变量后重新设置只读环境，并强制 `core.fsmonitor=false`、`submodule.recurse=false`、`--no-ext-diff`、`--no-textconv`、无 pager、无可选锁。任何归档或 Git warning 不得静默降级成成功。

ChangeSet previewDiff 与 Git diff 是不同对象，前者比较提案预演结果，后者比较仓库事实。

### 8.9 SandboxRunner

1.0 选择 Docker Desktop `desktop-linux` Linux VM 作为 Windows/macOS 统一后端。原生 macOS `sandbox-exec` 已弃用；Windows Sandbox 偏交互且缺少本产品所需的稳定无界面生命周期和产物协议；二者均记录为 `unsupported`，不得回退为裸宿主进程。能力探测只接受本机 unix socket 或 `dockerDesktopLinuxEngine` named pipe，清除 `DOCKER_*` 覆盖并拒绝远程 context。

```ts
interface SandboxRunner {
  probe(): Promise<SandboxCapabilityReport>;
  prepare(input: SandboxPrepareInput, policy: SandboxPolicy): Promise<SandboxHandle>;
  run(handle, request: SandboxExecutionRequest, signal): AsyncIterable<SandboxAuditEvent>;
  cancel(handle): Promise<void>;
  collect(handle): Promise<SandboxCollectionResult>;
  cleanup(handle): Promise<void>;
}
```

T34 已实现接口、默认策略、严格 Docker 命令计划和只读能力 API。T35 已实现持久化 Handle 状态机、私有冻结输入副本、独立 output、启动/输出/终止审计、超时与强制取消、普通文件产物收集、特殊文件拒绝和工作副本清理。命令计划要求 sha256 digest 镜像、`network=none`、只读 root/input、`cap-drop ALL`、`no-new-privileges`、UID/GID 65532、受限 tmpfs/PID/内存/CPU；不使用 shell。网络 allowlist 在受控代理实现前直接拒绝。固定协议自检只有在真实 Docker Desktop 上同时通过输入只读、网络禁用、正常退出、产物收集、源文件不变、取消无残留和工作副本清理后，才把顶层 `readyForBenchmark` 置为 true。当前开发机没有 Docker CLI，因此这里只完成了代码、模拟执行器负向测试和页面 unavailable 路径，T35 的真实容器验收仍待完成。

最终沙箱契约必须定义：

- 隔离工作区和 fixture；
- 允许工具和命令参数；
- 文件、网络、环境变量和密钥权限；
- setup/teardown；
- 超时、取消、输出上限和技术性最大步数；
- 文件 diff、命令结果、结构化产物和禁止副作用断言；
- 清理失败后的恢复。

---

## 9. REST API 契约

统一前缀 `/api`，除启动认证交换外，全部请求验证访问令牌。Workspace 使用 workspaceId，Skill Project 使用不透明 projectId；请求不得提交任意根路径。响应统一为：

```json
{ "ok": true, "data": {} }
```

或：

```json
{ "ok": false, "error": { "code": "...", "message": "...", "details": {} } }
```

| 域 | 主要端点 |
|---|---|
| Workspace | `GET/POST /workspaces` `GET/PATCH/DELETE /workspaces/:id` `POST /workspaces/:id/select` |
| Workspace 成员 | `GET/POST /workspaces/:id/members` `DELETE /workspaces/:id/members/:projectId` |
| Skill Project | `POST /projects` `POST /workspaces/:id/in-place` `GET /projects/:id` |
| Skill 导入 | `POST /workspaces/:id/imports` `GET /imports/:id` `PATCH /imports/:id/review` `POST/GET /imports/:id/llm-parse` `POST /imports/:id/llm-parse/cancel` `POST /imports/:id/reparse` `POST /imports/:id/reparse/resolve` `POST /imports/:id/confirm` `POST /imports/:id/cancel` |
| 状态与 revision | `GET /projects/:id/revisions` `GET /projects/:id/revision-status` `POST /projects/:id/undo-changesets` |
| schema/图/文档 | `GET /schema` `GET /projects/:id/graph` `GET /projects/:id/docs` `GET /projects/:id/docs/file` `GET /projects/:id/docs/slice` `GET /projects/:id/docs/references` |
| ChangeSet | `POST /projects/:id/changesets` `GET /changesets/:id` `POST /changesets/:id/confirm-and-apply` `POST /changesets/:id/reject` `POST /changesets/:id/repropose` |
| 基线 | `POST /projects/:id/baseline` |
| 解析 | `POST /projects/:id/parse` `POST /projects/:id/reparse` `GET /tasks/:id` `POST /tasks/:id/cancel` |
| 设计助手 | `GET /assistant/capabilities` `GET /workspaces/:id/assistant/sessions` `POST /projects/:id/assistant/sessions` `GET /assistant/sessions/:id` `POST /assistant/sessions/:id/messages` `POST /assistant/sessions/:id/cancel` |
| 测试用例 | `GET /projects/:id/benchmark-cases` `GET /projects/:id/benchmark-cases/:caseId`；写入/删除通过项目 ChangeSet |
| 运行 | `GET/POST /projects/:id/runs` `GET /projects/:id/runs/:runId` `POST /projects/:id/runs/:runId/{next,pause,resume,stop}` `GET/POST /projects/:id/runs/:runId/dialog` `POST /projects/:id/runs/:runId/dialog/cancel` `GET/POST /projects/:id/runtime-artifacts/storage` |
| Trace | `GET /projects/:id/traces/:runId/events?afterSeq=N` `WS /projects/:id/traces/:runId/stream?afterSeq=N`；回放端点后续补充 |
| Bug Report | `POST /projects/:id/runs/:runId/reports` `GET /projects/:id/reports?workspaceId=` `GET/DELETE /reports/:id` `POST /reports/:id/confirm` `GET /reports/:id/download?format=json\|markdown` `GET/POST /workspaces/:id/report-imports` `DELETE /workspaces/:id/report-imports/:reportImportId`；导入报告下提供 `GET/POST fixtures`、`GET/POST benchmark-candidates`、候选 `changeset`、`confirm-and-apply` 与 `reject` |
| Diagnosis | `POST /diagnoses` `GET /diagnoses/:id` `POST /diagnoses/:id/propose-change`；导入报告下的 repairs 提供创建、`confirm-and-apply`、`reject` 与 `verify` |
| LLM | `GET /llm/config` `PUT /llm/config` `POST /llm/test` |
| 导出 | `POST /projects/:id/exports` `GET /exports/:id` `POST /exports/:id/confirm` `GET /exports/:id/download` |
| Git 对比 | `GET /projects/:id/git/refs` `GET /projects/:id/git/diff?base=HEAD|<commit-oid>` |
| Benchmark | `POST /projects/:id/benchmark-runs` `GET /benchmark-runs/:id` `POST /benchmark-runs/:id/verdict` |

所有 Skill 业务 API 必须显式绑定 projectId，并在服务端复核对应 skillId，不能读取 Workspace 的 selectedProjectId 作为隐式上下文。所有 Skill 文件变更 API 最终都要生成或应用单 projectId ChangeSet。不允许单独暴露“LLM 直接写文件”端点。Workspace 成员排序、选择和显示名属于 Studio 私有状态，不修改 Skill 文件，因此不进入 Skill ChangeSet。

---

## 10. Trace 与 WebSocket

首个有向图版本使用按运行隔离的 WebSocket 端点 `/api/projects/:projectId/traces/:runId/stream?afterSeq=N`。连接必须通过 Host、Origin、本地 session token 和 `skill-designer.trace.v1` 子协议校验；token 通过 WebSocket 子协议传递，不进入 URL。服务端从 projectId/runId 对应的持久化运行中复核 workspaceId、skillId 和 artifactId，不依赖 Workspace 当前选择。

```json
{
  "type": "trace.page",
  "data": {
    "schemaVersion": "1.0",
    "traceId": "run-...",
    "afterSeq": 41,
    "latestSeq": 44,
    "events": [],
    "projection": {}
  }
}
```

服务端处理次序：

```text
生成事件
-> 原子持久化 ProjectRun 兼容事实
-> 追加并 fsync TraceStore
-> 校验连续序号和完整身份
-> 广播给订阅者
```

TraceStore 日志缺失或损坏时，在广播或补拉前从 ProjectRun 的完整事件镜像重建；恢复只修复事件日志，不推进、回退或改写运行状态。

客户端维护已接收的最大 seq，断线后以该值作为 `afterSeq` 自动重连；服务端首次推送当前增量页，之后只在新事件已持久化时推送。前端亮灯分为：

1. 纯状态层：Trace 事件 -> 节点/边视觉状态，可单元测试；
2. 渲染适配层：把同一状态覆盖到 2D Canvas 或 3D WebGL 图谱，知识边不参与运行高亮；
3. 播放队列：处理运行毫秒级产生事件、界面秒级展示的速度差（T27 后续）；
4. 快进：允许直接查看最终状态（T27 后续）。

实时亮灯和事后回放消费同一 Trace，不各自记录过程。

回放不复制或改写 Trace。`projectTraceAt(graph, events, throughSeq)` 只选择 `seq <= throughSeq` 的事件并调用同一状态 reducer，因而时间轴、步进、播放和实时最终态具有同一归约语义。回放模式是只读视图，必须禁用暂停、继续、停止和下一节点等运行命令；切换回实时视图后显示订阅期间已持久化的最新事件。

运行对比按稳定 nodeId 的 `engine.enter` 序列保存左右完整路径，并计算共同前缀和首个偏差，不按节点名称做模糊匹配。revision 是否相同、artifact 是否相同与路径是否相同分别展示，不从“路径一致”推导“输入一致”，也不从“artifact 不同”推导“存在故障”。比较器同时返回左右运行的 `status/currentNodeId/step/eventSeq/skillVariables/eventTypeCounts` 快照；变量按顶层键列出存在性和值差异，值使用对象键排序的确定性比较，数组保持顺序。事件差异按完整 type 统计左右次数，`llm.*`、`tool.*` 等域不在比较器中解释语义或根因。

对比运行完整路径中存在、但当前运行 RuntimeArtifact 图中不存在的 nodeId，作为“对比图缺失”传给通用 Trace 图谱；Canvas/WebGL 使用虚线缺失态和稳定 ID 占位，不生成虚假边，也不映射到相似名称节点。报告自身事件相对其图投影缺失时仍使用“原图缺失”，两种缺失来源在状态条中明确区分。

---

## 11. Bug Report 和脱敏

Bug Report 是 Trace 的可分发投影，不是原始 Trace 的直接拷贝。

默认导出使用同一文件 stem 生成 `<project-slug>-<YYYYMMDD-HHmmss>.report.json` 和 `<project-slug>-<YYYYMMDD-HHmmss>.report.md`。时间戳使用用户本地时间并在报告内容中另存带时区的 ISO 时间。该命名用于可读性，不承担并发唯一性职责。

```json
{
  "reportVersion": "1.0",
  "skill": { "skillId": "skill-018f...", "name": "...", "contentHash": "sha256:..." },
  "runtime": {
    "artifactFingerprint": { "schemaVersion": "1.0", "algorithm": "sha256", "projectContentHash": "sha256:...", "inputHash": "sha256:...", "value": "sha256:..." }
  },
  "coverage": {
    "engine": true,
    "conversation": true,
    "tools": false,
    "externalAgentMayBypass": true
  },
  "trace": [],
  "graphProjection": { "nodes": [], "edges": [] },
  "symptoms": [],
  "userNote": "...",
  "sanitized": true
}
```

`graphProjection` 包含本次路径涉及节点的标题、类型和必要边，以支持原图已改变时的自包含回放。

首个版本的报告创建端点为 `POST /api/projects/:projectId/runs/:runId/reports`，输入 workspaceId、`off | default | strict` 脱敏模式和用户说明；只接受 `completed | stopped` 运行。服务端从该运行绑定的不可变 RuntimeArtifact 与 Snapshot 清单构建预览，保存 `report + digest`。用户通过 `POST /api/reports/:reportId/confirm` 确认同一 digest 后，服务端从该 BugReportDocument 同时生成冻结 JSON 与 Markdown；Markdown renderer 只接收已经脱敏的 BugReportDocument，在可读摘要之后内嵌同一完整 JSON，不访问原始 Trace、Run 或 active 工作树。`GET /api/reports/:reportId/download?format=json|markdown` 只读取对应冻结文件。报告生成不创建 Skill revision，也不进入项目 Git diff。

`GET /api/projects/:projectId/reports?workspaceId=` 只列出同时匹配 Workspace 成员、projectId 和 skillId 的报告。`DELETE /api/reports/:reportId` 必须复核 workspaceId 归属，且删除边界严格限制为 `<studio-data>/reports/<reportId>/`；不级联删除 report-imports 中的独立副本、来源运行、Trace、RuntimeArtifact、Benchmark 记录或 Skill 文件。

`off` 只关闭可选内容脱敏，不能关闭禁 Key 规则。三种模式都递归屏蔽 API Key、session/access/refresh token、Authorization、Cookie、Password、Secret 和 Private Key 等字段，并屏蔽常见密钥/Bearer 文本。`default` 对 engine 结构字段保留可回放数据，对 conversation/llm/tool/sandbox 等自由数据按严格规则缩减；`strict` 只保留回放所需结构字段，并移除用户说明。每次预览记录实际移除字段数。

导入报告时优先按 skillId 查找 Workspace 成员，并继续校验 contentHash/RuntimeFingerprint。找不到精确 skillId 时要求用户明确选择或仅用自包含 projection 查看；禁止按相似名称自动绑定到另一个 Skill。

导入端点校验 `reportVersion`、全部稳定 ID、Trace 事件身份、严格递增唯一 seq、图 skillId、边端点，以及存在时的 RuntimeArtifact/Benchmark 指纹内部身份。新生成报告总是写入完整 RuntimeArtifact 指纹；Benchmark 来源另写入 Provider、请求与解析模型、prompt 版本、runner 镜像和沙箱策略指纹。为兼容开发期早期 1.0 报告，缺少新增指纹字段仍允许导入，但前端必须显示“未记录”，不能回填当前环境。报告保存于 Workspace 私有 `report-imports`，不写入任何 Skill 项目。匹配状态分为精确匹配、内容指纹不同、Skill 缺失和目标不可用；后三者继续使用报告自带 graphProjection 回放并显示明确告警。回放默认定位首个事实症状，并允许按每条症状的 seq 独立跳转；未知/缺失节点按原 nodeId 占位，不尝试名称相似映射。

浏览器多选与拖放单次最多接收 10 个 JSON 文件、合计 10 MiB，每个文件仍受 2 MiB 上限；客户端逐份调用同一导入 API，因此某份校验失败不回滚其他成功项。服务端按 workspaceId + reportId 检查幂等性：完整 BugReportDocument 相同则返回既有副本，内容不同则返回 `report_id_conflict`，不能静默覆盖。

删除导入副本必须先复核 workspaceId 与 reportImportId 身份，再清理 `report-imports` 中的副本及 `diagnoses`、`report-fixtures`、`report-benchmark-candidates` 下同一 Workspace/reportImportId 的私有派生目录。ChangeSet、已写入项目的 BenchmarkCase、来源报告、Run、Trace 和 RuntimeArtifact 不属于该删除事务，不能级联删除或反向回滚。

首个诊断 API 绑定导入报告而不是当前页面所选 Skill：`POST /api/workspaces/:workspaceId/report-imports/:reportImportId/diagnoses` 读取已校验报告、确定性生成 DiagnosisRecord 并原子保存；同路径 `GET` 按生成时间返回历史记录。记录存于 Studio 私有 `diagnoses/<workspaceId>/<reportImportId>/`，不写入 Skill 仓库、不创建 revision。前端选择报告时只加载该报告最新诊断，使用请求序号防止快速切换报告时旧响应覆盖新选择。

脱敏覆盖：

- 用户发言和意图；
- 模型请求/回答；
- 结构化结果和终态变量；
- 文件 diff 和工具输出；
- 命令参数、环境变量和路径；
- lookup 轨迹与文档切片；
- 所有标记为 `sensitive/secret` 的事件字段。

导出前页面同时展示不下载的本地原始投影和脱敏后预览。1.0 不接受任意附件，避免未定义文件绕过报告脱敏；后续版本只有在建立类型白名单、数量/大小上限、路径边界和逐类型内容清洗后才能加入附件协议。API Key 永不可导出。

Bug Report 转换产生两种不同资产：

1. 确定性引擎回归夹具：验证引擎面对相同输入的行为，不称为 Skill Benchmark；
2. 候选 BenchmarkCase：提取用户输入、回答、fixture 和期望产物，经用户确认后使用真实模型重新运行。

`ReportFixture 1.0` 固定 `kind: engine-regression` 和 `benchmarkEligible: false`，包含报告身份、来源 artifact/revision/contentHash、报告自带图、初始变量、重放命令、期望事件签名和限制说明。创建时必须调用同一 Runtime Engine 重放并逐项比较签名；不一致返回 `fixture_not_replayable`，不得保存。当前 Bug Report 尚未记录初始变量，因此夹具把它列为显式限制，不伪造变量、模型或工具结果。夹具保存于 Studio 私有 `report-fixtures/<workspaceId>/<reportImportId>/`，不进入 Skill 项目、revision 或导出包。

`ReportBenchmarkCandidate 1.0` 独立保存于 `report-benchmark-candidates/<workspaceId>/<reportImportId>/`。状态从 `draft` 进入 `changeset-created`，再根据关联 ChangeSet 进入 `applied / rejected / conflicted` 之一。caseId、skillId 和 `source` 来源身份在补充过程中不可修改；服务端以当前项目图重新 lint，存在 error 时不生成 ChangeSet。确认前候选只存在于 Studio 私有目录；确认后复用 `benchmark.case.write` 事务写入项目，并记录 changeSetId 和 appliedRevision。拒绝或冲突后的候选保留来源审计，只能重新创建候选与 ChangeSet；候选始终以 draft 创建，不能把报告中的一次历史运行直接认定为 Benchmark 通过。

---

## 12. 诊断与修复编排

```text
Trace/Bug Report
-> 提取症状
-> 检查 RuntimeArtifact 的图、文档和规则
-> 关联上游事件、模型输出、工具结果和文件 diff
-> 生成多个候选原因
-> 给出置信度、其他解释和验证方式
-> 可选生成 ChangeSet
-> confirm-and-apply 事务提交
-> 重新运行用例
```

工具不识别原因时必须显示“证据不足”，不使用第一个 reject 臆测根因。

`DiagnosisRecord 1.0` 绑定 `diagnosisId + workspaceId + reportImportId + reportId + skillId`。每个候选包含稳定 candidateId、分类、陈述、`high | medium | low` 置信度、证据数组、建议数组和结构化 `verification.method/steps/successEvidence`；每条证据必须注明来源 `trace | graph | report`，并可附 seq、nodeId 和字段路径。limitations 独立保存未观测 conversation/tool 和外部 Agent 绕过引擎等边界，不能藏在候选结论中。旧记录缺少 verification 时读取层不改写历史，Web 只显示保守的补充证据方案。

当前确定性规则把 `engine.reject` 本身解释为“提交目标不符合当前合法出口”，这是可由 Trace 直接证明的近因；提交目标不在 graphProjection 时增加一个中等置信图引用候选。两者都不证明提交方身份或最终责任。只有先行 `condition.evaluated` 中同目标边结果为 false，且随后发生同目标 reject，才增加 condition-evaluation 候选。只有 `document.context.status` 为 missing/ambiguous 才增加 document-context 候选。`engine.stop` 只证明运行在完成前停止。规则引擎按失败域归并事件：`llm/model error` 映射 model-output，`tool error/failed` 或非零结果映射 tool-execution，`sandbox failed/timed-out` 与 Provider 不可用映射 environment；同域多事件合并为一个候选并保留所有 seq。Benchmark failureCategory 使用同一映射，避免内容重复的“技术失败”和领域失败候选。

结构化修复使用可选 `DiagnosisRepairOption`，当前注册 `graph.add-edge | graph.remove-condition | graph.remove-document-binding`。`graph.add-edge` 的生成条件必须同时满足：导入报告的 skillId/contentHash 与当前 active revision 精确一致；requestedNodeId 在报告图中存在；当前没有同起终点的非知识边。回退到此前已进入节点时生成 `back` 边，否则生成 `flow` 边。`graph.remove-condition` 只在同一节点、目标和 edgeId 的先行 `condition.evaluated=false` 与随后 `engine.reject` 都被 Trace 证明，且报告冻结图中的该边确实带 condition 时生成 `graph.edge.update`；提案只移除 condition，不猜测新的表达式或测试变量。`graph.remove-document-binding` 先按 `nodeId + path + anchor` 分组 `document.context=missing/ambiguous` 证据，再由 Store 读取当前 source/graph 并调用文档绑定 lint；只有报告精确匹配、节点仍绑定完全相同的路径与锚点、对应 `nodes[index].doc/docAnchor` 当前仍报错时，才附加移除 `doc/docAnchor` 的 `graph.node.update`。生成 ChangeSet 前再次执行同一事实复核，防止诊断后文档已恢复或节点已变化却继续应用旧建议。三种修复都调用现有 ChangeSetService 预演和 lint，不直接写图，页面必须展示 Diff 并等待用户确认。

修复端点包括 `GET/POST /api/workspaces/:workspaceId/report-imports/:reportImportId/repairs`、同级 `/:repairId/confirm-and-apply`、`/:repairId/reject` 和 `/:repairId/verify`。RepairRecord 保存 diagnosis/candidate/report/skill/project/sourceRun/sourceRevision/changeSet 身份、appliedRevision、提案状态 `proposalStatus`、验证状态 `status`、验证证据、`round` 和可选 `lineage`，位于 Studio 私有 diagnosis 目录。确认与拒绝都继续绑定 ChangeSet digest + baseRevision；拒绝只把 proposalStatus 置为 rejected，不得把验证状态伪造成 failed。当前 runtime 验证要求 run 的 workspaceId/skillId/revision 与修复记录一致。边修复要求 Trace 实际经过本次 create/update 的 edgeId 且运行 completed；同一起终点再次 reject，或已经经过修复边后在下游发生新的 reject 且运行未完成，均记为 failed，并保存修复边经过 seq 与下游拒绝 seq。文档绑定修复还要求新 Artifact 的目标节点已无 `doc/docAnchor`、Trace 实际进入该节点且运行 completed；进入后同节点再次出现 missing/ambiguous 记为 failed，未进入、未完成或只有无关下游失败返回 inconclusive。任何类型都不能用回放补足。

多轮修复按真实验证运行串联，不按时间邻近推断。若新导入报告的 source runId/benchmarkRunId 精确等于上一 RepairRecord 的 `verification.runId`，新记录写入 `round = parent.round + 1` 和 `lineage={ relation: follow-up, parentRepairId, parentReportImportId, sourceVerificationRunId }`；否则从第 1 轮开始。页面展示上一轮到当前轮并可跳回父报告，但每轮仍有独立 Diagnosis、ChangeSet、确认和验证状态。读取旧 RepairRecord 时缺失或无效的 round 保守回填为 1，不虚构 lineage。

报告匹配状态在每次列表读取时根据当前 Workspace 成员和 active contentHash 重算，而不是永久使用导入瞬间结果。因此修复应用后原报告会从精确匹配变为指纹不同，仍可用自包含图回放和既有 RepairRecord 验证，但不能继续生成基于旧指纹的新修复提案。

修复结果状态：

```text
proposal-created -> user-approved -> applied -> pending-verification -> verified
                                                              \-> failed
```

回放不能改变验证状态。修复后必须生成新 RuntimeArtifact，并用与原问题相同或更强的执行方式重新运行。验证记录包含 `runtime | benchmark` 级别；原问题来自 Benchmark 时，只有新的真实沙箱 Benchmark 运行和用户判定才能标记为 `verified`。

---

## 13. Benchmark 架构

### 13.1 测试用例编写与运行分离

BenchmarkCase 编辑器、schema、ChangeSet 和人工审核属于前期业务能力，不依赖沙箱。

真实 BenchmarkRunner 的当前实现链：

```text
冻结 RuntimeArtifact
-> SandboxRunner.prepare
-> 启动真实 LLM Provider
-> 根据当前节点要求交回结构化结果或向用户提问
-> 记录 Trace
-> 收集文件、命令和结构化产物
-> 运行自动断言
-> 用户审阅并给出 passed/failed/inconclusive
```

`BenchmarkRunnerService` 使用单并发 FIFO；服务重启会把遗留 queued/preparing/running 记录明确标记为技术失败。单用例入口建立一条记录；批量入口先一次性校验 1 到 50 个不重复 ready case，再按调用方顺序建立独立记录并进入同一 FIFO，不提高并发上限。每次执行先同时检查 Provider 配置、T35 真实沙箱自检与固定 digest runner 镜像，任一不满足即持久化 `blocked`，不会冻结 Artifact、调用模型或生成 token。通过后从 active revision 的 snapshot 读取 case、图与节点引用文档，先由真实容器读取 snapshot，再按节点调用宿主 Provider。模型只能在引擎给出的合法出口中选择或停止，不能生成命令；action 命令只能来自用户已确认图中的 `extensions.benchmarkCommand`，仍经过 `SandboxPolicy.allowedCommands`。

真实 Skill preflight 验收使用临时 `WorkspaceStore`、真实 `BenchmarkRunnerService`、`ModelSettingsService`、`SandboxControlService` 和独立本地 HTTP 端口；Provider 凭据存储在验收进程中显式为空，避免读取宿主密钥。`mdd-backend-extend-develop` 的 10 节点/9 边图生成 `exact` ready BenchmarkCase，页面和 Store 双重验证 ChangeSet 确认前后持久化边界。缺少 Provider、Docker 生命周期自检和固定 digest 镜像时，持久化记录为 `blocked/not-run`，`modelCallCount=0`、`usage.totalTokens=0`、无 `runtimeArtifactId`、无 sandbox handle，源目录逐文件 hash 不变。证据位于 `.skill-designer-dev/chrome-artifacts/real-skill-mdd-benchmark-preflight/verification.json`；这不是沙箱执行成功证据。

模型调用不自动重试，最多 40 个节点步骤；节点文档按绑定路径/标题切片且单次限制 24,000 字符，节点声明的项目事实查询使用同一 Engine 原语并共享 24,000 字符结果上限，工具回灌只保留最近两项并截断 stdout/stderr，避免重复上下文。只有至少一次模型调用且 API usage 总 token 大于 0，运行才可进入自动断言；scripted Provider 只用于协议测试，其结果不会写入用户项目的 Benchmark 通过率。

### 13.2 判定规则

- 路径 diff 默认 `subsequence`，同时显示多走、漏走和换序；用户可切换为 `exact`；
- 路径是诊断证据，真实产物和禁止副作用是主要断言；
- `fixture-incomplete` 不进入通过率；
- 每个默认或模拟用户回答都记录来源；
- 单次结果与多次稳定性评估分开展示；
- `automaticVerdict` 和 `humanReviews[]` 分开持久化；人工记录不得修改自动断言或运行技术状态；
- `humanReviews[]` 是只追加审计序列，每条记录使用稳定 reviewId，并写入独立 `benchmark.review.recorded` Trace 事件；
- review API 只接受 `completed` 运行，`blocked/failed/cancelled` 必须拒绝，避免人工结论掩盖沙箱或 Provider 故障；
- 不设金额红线，但记录 token 消耗并实施取消、最大步数、断点与缓存。

Benchmark Report 复用 Bug Report 1.0 的报告记录、脱敏、digest 确认、下载和导入存储，但在来源中增加 `kind=benchmark`、真实 benchmarkRunId/caseId，并以兼容的 `run-<同 UUID>` 作为报告内 Trace 身份。`ExecutionTraceEvent` 是通用事件信封，`RuntimeTraceEvent` 是其引擎运行子类型；报告因此可同时保存 engine、model、tool、sandbox、assertion 和 review 事件。生成服务必须从已持久化 RuntimeArtifact 读取精确图与 revision，缺少 Artifact 的 blocked 记录不得退回 active 项目图生成报告。

重跑 lineage 存放在子 `BenchmarkRunRecord.lineage`。普通 `relation=rerun` 要求父记录属于同一 project、Workspace 和 case，并已处于终态；父子对比读取明确 ID 对，不做时间邻近匹配。`relation=post-repair` 只能由 DiagnosisRepair 专用服务入口建立，必须绑定报告来源 parentBenchmarkRunId、repairId、changeSetId 和 appliedRevision，调用方不能通过普通启动接口自行声明。

`WorkspaceStore.preparePostRepairBenchmark` 在入队前校验报告来源、已应用修复、ready case 和当前 active revision；`BenchmarkRunnerService` 从私有记录读取报告指定的父运行并建立 lineage。验证入口允许 active 项目在冻结后继续演进，但会重新读取父子两份 RuntimeArtifact，逐项校验 Workspace/project/skill/case、来源 revision/artifact、子 appliedRevision、contentHash 和独立 Artifact ID。子运行必须为 completed，且 `automaticVerdict` 与最新 `humanReviews[]` 都不能是缺失或 inconclusive；两者均 passed 才写 verified，任一 failed 写 failed。blocked 和技术失败不进入修复判定。

Engine 的纯函数 `compareBenchmarkRuns(before, after)` 生成版本化 `BenchmarkRunComparison`，供页面和后续导出复用。lineage 只在 after 明确引用 before 时返回 `rerun/post-repair`，不按时间或名称推断；Artifact 对比读取两条记录各自持久化的 ID、revision 和 contentHash。实际路径从按 seq 排序的 `engine.enter` 提取，并返回共同前缀及首个偏差。断言以稳定 assertionId 对齐为 added/removed/changed/unchanged，Trace 按完整事件 type 计数，usage 覆盖 input/output/total/cachedInput/reasoning/cacheWrite token，另保留模型调用数和最新人工判定。比较器不读取 active 项目、不修改运行或修复状态，也不将任何差异解释成根因；缺失字段保持缺失事实，只有专用验证服务能够更新 repair verification。

---

## 14. 基础安全架构

### 14.1 本地服务器

- 只监听 `127.0.0.1`/本机 loopback；
- 每次启动生成高强度随机令牌；
- 页面通过带一次性交换码的本地 URL 进入，后续请求使用会话令牌；
- 校验 Host 和 Origin；
- 不在 URL、日志或 Trace 中记录长期令牌。

### 14.2 项目路径隔离

- Workspace 每个成员根目录都要单独授权、解析真实路径并分配 projectId；
- Workspace 成员关系不授予共同父目录或其他成员目录的访问权；
- 对已存在文件使用 realpath 校验；
- 对尚未存在的写入目标校验最近已存在父目录的 realpath；
- in-place 根目录保存 realpath 后，打开时拒绝项目内任意符号链接，三个核心文件还要单独 `lstat`；后续每次读取文档、图、用例、ChangeSet 输入或 Workspace 摘要前，从授权根到目标逐级 `lstat`，遇到符号链接立即返回 `project_symlink_unsupported`，不依赖首次扫描结果；
- Markdown 目录遍历遇到符号链接必须整体失败，不能静默跳过，也不能跟随到根目录之外；RuntimeArtifact 和通用导出继续只从不可变 Snapshot 取数；
- 拒绝绝对路径、`..`、符号链接逃逸、Windows 保留名和大小写冲突；
- ChangeSet 中的路径统一是 POSIX 相对路径。

### 14.3 密钥与外来内容

- LLM API Key 优先读取 `OPENAI_API_KEY`；用户在界面写入时，macOS 使用 Keychain，Windows 使用当前用户 DPAPI 加密并保存到权限最小化的项目外应用数据文件；其他平台不落盘明文，只接受环境变量；
- 前端只能写入/替换 Key，GET 只返回是否配置、来源和凭据后端，不返回明文、后缀或可识别掩码；
- 日志、Trace、诊断、Bug Report 和导出包经过禁 Key 检查；
- Markdown 默认禁止原始 HTML，或在展示前清洗；
- 导入 JSON/Markdown/其他资产设置单文件和总量上限；
- 导入脚本只读展示，沙箱完成前不存在执行路径。

### 14.4 命令与工具

- action 节点使用结构化 `command + args[]`，不拼接 shell 字符串；
- 变量只能填充允许参数，不能修改可执行程序或工作目录；
- 沙箱上线前，Studio 不执行 action 节点命令；
- 沙箱上线后，命令、工作目录、权限和网络范围进入 Trace。

---

## 15. 三系统一致性

Studio 正式验收 Windows 和 macOS；engine、导出包和 hash 规则同时兼容 Linux。

统一规则：

1. 项目内部路径使用 `/` 分隔，不存储绝对路径。
2. 文本编码统一 UTF-8，hash 前移除 BOM，换行统一 LF，Unicode 统一 NFC。
3. 文件名校验 Windows 保留名、尾随点/空格和大小写不敏感冲突。
4. Windows 原子替换遇到文件占用时采用有界重试和明确错误，不静默失败。
5. 多文件提交使用 journal 恢复，不假设多次 rename 具有整体原子性。
6. 子进程使用参数数组，不拼接 shell 命令。
7. 启动方式提供跨平台 Node 命令，可额外提供 macOS/Windows 快捷入口，但快捷入口不是唯一启动方式。

### 15.1 发布包结构与校验协议

`npm run release` 先执行全仓构建，再由 `scripts/release/build-release.mjs` 为 macOS 和 Windows 各生成一个 stage 目录与 ZIP，并写出 `dist/releases/SHA256SUMS.txt`。stage 结构固定为：

```text
skill-designer-<版本>-<平台>/
├── release-manifest.json        # 发布清单，不包含自身
├── app/server/dist              # 已构建的 server
├── app/web/dist                 # 已构建的静态前端
├── node_modules/                # server 生产依赖闭包 + @skill-designer/engine
├── bin/                         # skill-designer.mjs / doctor.mjs / install.mjs / uninstall.mjs / release-common.mjs
├── docs/                        # 用户指南与已知限制
├── README.md
└── start.* / diagnose.* / install.* / uninstall.*   # 平台快捷入口
```

协议要点：

1. `release-manifest.json` 声明 `schemaVersion/product/version/targetPlatform/releaseChannel/signing/minimumNode/entry/dataDirectoryPolicy` 和逐文件 `path + size + sha256`。清单不含自身，避免自引用 hash。
2. **完整校验协议是“实际普通文件集合与清单声明集合精确相等，且声明文件 100% 校验大小和 SHA-256”，不是某个固定文件数量。** 文件数量随构建缓存类文件变化，不能写成长期契约。清单外文件返回 `undeclared_file`；符号链接、目录或特殊文件返回 `unsupported_file_type`，校验不跟随符号链接读取内容。
3. 生产依赖按 `dependencies` 与 `optionalDependencies` 递归解析真实 realpath 后去重复制，缺失的必需依赖直接失败；发布包内不允许符号链接或非普通文件。安装器在 `cp` 前先对解压源执行完整校验，复制后再校验 staging，避免只验证复制结果而接受不受支持的源文件类型。
4. 当前 `0.1.0` 只接受 `releaseChannel=development-preview`，且 `signing` 必须逐项声明 `status=unsigned`、`trustLevel=integrity-only`、`distribution=local-development`、macOS `appleCodeSigned=false/notarized=false` 与 Windows `authenticodeSigned=false`。缺字段、未知值、自相矛盾状态或只有任意文本的 `signed` 声明均由 `readReleaseManifest` 拒绝；后续正式签名发布必须引入可验证证明和新的受支持契约，不能放宽当前分支。
5. 完整性校验为传输与落盘检查，不替代代码签名，也不构成对被恶意替换的 `bin/` 自身的信任证明。合法的未签名开发预览可以运行，但 `doctor.releaseTrust.publisherTrustEstablished` 必须为 `false` 并输出明确警告。

### 15.2 启动器、安装、诊断与卸载语义

- **启动器** `bin/skill-designer.mjs` 先校验 Node 版本、`targetPlatform` 与 `process.platform` 的唯一映射，以及三个核心文件（server 入口、web 入口、engine 入口）的普通文件类型和 SHA-256，再以 `SKILL_DESIGNER_DATA_DIR` 和 `SKILL_DESIGNER_PORT` 启动子进程 server。macOS 只接受 `targetPlatform=macos`，Windows 只接受 `targetPlatform=windows`，Linux 不作为 1.0 发布包运行平台。启动器等待 `GET /api/session` 就绪后，通过 `release-common.mjs` 的共享探测规则只打开 Google Chrome；未找到时明确失败，不调用系统默认浏览器。端口默认在 `4310-4399` 内探测空闲值，显式 `--port` 被占用时直接失败而不静默改端口；`--no-open` 仅跳过自动打开并保留本地服务运行，不改变页面实际使用 Chrome 的前置条件。
- **安装** `bin/install.mjs` 拒绝错平台包及安装目标与解压目录互相包含，先完整校验解压源，再 `cp` 到同级隐藏 staging 目录，对副本重新执行完整清单校验，随后把旧程序 `rename` 为 backup、staging `rename` 为目标，最后删除 backup。任一步失败都清理 staging 并把 backup 恢复回目标位置；数据目录不参与安装事务。
- **诊断** `bin/doctor.mjs` 输出结构化结果：包身份与 `package.releaseChannel/package.platformMatchesRuntime`、`releaseTrust`（签名状态、信任等级、分发范围、平台签名事实、是否建立发布者信任及警告）、运行时平台/架构/Node 版本与是否满足最低要求、清单精确文件集合及全量完整性、数据目录写探针（写后立即删除探针文件）、共享规则探测到的 Chrome 路径，以及 Benchmark 前置条件是否配置。Provider 密钥和 runner 镜像只报告布尔状态，绝不读取或回显密钥值。合法的未签名开发预览警告不是硬失败；错平台、manifest 非法、Node 不支持、完整性失败、数据目录不可写或未找到 Chrome 时退出码为 1。
- **卸载** `bin/uninstall.mjs` 默认只删除程序目录。删除前要求 TTY 交互输入 `UNINSTALL` 或显式 `--yes`；非 TTY 且无 `--yes` 时视为取消。只有同时给出 `--delete-data` 才删除数据目录，且 `removeExactDirectory` 拒绝删除文件系统根目录或用户主目录。

### 15.3 快捷入口的退出码与暂停策略

`scripts/release/entrypoints.mjs` 是四个双击入口的唯一事实源，被构建脚本和单元测试共同引用，避免生成物与断言各写一份。约束：

1. macOS `.command` 只启用 `set -u`。**不能启用 `set -e`**：`node` 失败时 errexit 会立即终止脚本，`status=$?` 与暂停提示都不会执行，用户在 Finder 双击时看不到任何原因。
2. Windows `.cmd` 必须在 `node` 之后立即 `set "status=%errorlevel%"`，再执行 `pause`，最后 `exit /b %status%`。`pause` 会覆盖 `%errorlevel%`，直接 `exit /b %errorlevel%` 会丢失 Node 的真实退出码。
3. `start` 是常驻服务，只在非零退出时暂停；`diagnose`/`install`/`uninstall` 是一次性命令，无论成功失败都暂停，确保结果可读。
4. 两个平台都原样返回 Node 的退出码，并通过 `"$@"` / `%*` 透传参数，快捷入口不改变命令语义。

---

## 16. 核心数据流

### 16.1 Workspace 与 Skill 选择

```text
打开 Workspace
-> WorkspaceService 读取成员引用
-> SkillProjectService 独立校验各 projectId/skillId/授权路径
-> 聚合轻量摘要
-> 用户选择当前 Skill
-> 图谱/文档/测试/诊断进入该 projectId 上下文
```

某个成员失联、无权限或 lint 失败不得阻断其他成员加载。切换当前 Skill 不会终止已在后台运行的任务，运行归属由 Trace 身份字段决定。

### 16.2 编辑与 ChangeSet

```text
当前 skillId/projectId + web 表单或 Design Assistant
-> ChangeSetService 生成提案
-> 隔离预演 + lint + diff
-> 用户确认 digest，或明确拒绝
-> 确认：事务应用 -> 生成 revision + snapshot -> ws 广播 Skill 变更
-> 拒绝：仅持久化 rejected，项目与 active revision 不变
```

### 16.3 导入与重载

```text
选择目标 Workspace
-> 复制源 skill 或授权原地打开
-> 资产清点
-> 静态/LLM 解析到暂存区
-> ChangeSet
-> 审阅/修改/重解析
-> 用户确认
-> active revision
```

### 16.4 运行与 Trace

```text
选中 skillId + active revision
-> RuntimeArtifact
-> engine 和 LLMProvider 运行
-> TraceStore 追加事件
-> ws 广播可选亮灯
-> 结束/失败/取消封存
-> 回放/诊断/报告投影
```

### 16.5 诊断与辅助修复

```text
Trace + RuntimeArtifact
-> DiagnosisService
-> 候选原因 + 证据 + 置信度
-> 可选 ChangeSet
-> 用户确认
-> 事务应用
-> 候选 BenchmarkCase
-> 重新验证
```

### 16.6 真实沙箱 Benchmark

```text
ready BenchmarkCase + active revision
-> RuntimeArtifact
-> SandboxRunner
-> 真实 LLMProvider + 受控工具
-> ExecutionTrace
-> 产物断言
-> 独立保存 automaticVerdict
-> 只追加用户 passed/failed/inconclusive 复核历史
```

---

## 17. 架构级约束清单

1. engine 的 npm 运行时依赖为空。
2. engine 不包含业务域词汇、HTTP、UI 或 LLM Provider 代码。
3. web 不直接读写文件。
4. Studio 项目写入最终都由 ChangeSetService + TransactionService 提交。
5. 用户确认必须绑定 ChangeSet digest 和 baseRevision。
6. 校验失败、revision 冲突或提交失败时，不得产生部分生效的工作树。
7. 会话和 benchmark 从开始到结束只读同一 RuntimeArtifact。
8. Trace 事件先持久化后广播，断线可以按 seq 续传。
9. 亮灯、回放、Bug Report 和诊断不各自建立运行事实源。
10. Diagnosis 推断与 Trace 事实分离，无证据时返回证据不足。
11. 节点行为不依赖固定节点 ID，流程跳转不使用知识边。
12. 未识别字段和资产在导入-保存-导出往返中不得丢失。
13. `content-only` 项目不得被引擎当作虚假流程运行。
14. 导入脚本在沙箱上线前不存在执行路径。
15. API Key 不进入项目、Trace、日志、Bug Report 或导出包。
16. 1.0 只实现 GenericExportProfile，不产生 Claude/Codex 专用格式。
17. 内部确定性测试不得计入 Skill Benchmark 结果。
18. snapshot/baseline 用于人工已阅比较，不替代 ChangeSet 批准和 RuntimeArtifact 版本冻结。
19. 1.0 不实现通用元模型或专用 Agent 适配；但类型注册、未知字段保留和 Trace 事件信封不得阻断 2.0 扩展。
20. 流程拓扑只以顶层 `edges[]` 为事实源，节点内不得再保存重复邻接关系。
21. revision、Trace、报告和 RuntimeArtifact 等 Studio 私有数据存放在项目目录外，不污染 Skill 仓库。
22. GitDiffService 只读，不通过 UI 或 API 暴露任何 Git 写操作。
23. Workspace 是多 Skill 组织边界，不是跨 Skill 事务或运行边界；1.0 所有可变更和可执行对象都绑定单一 projectId + skillId。
24. skillId 不依赖名称、路径、projectId 或 workspaceId；同一 Workspace 不允许活动成员 skillId 冲突。
25. Workspace 每个成员目录独立授权，任何成员失败不得导致跨目录访问或阻断其他成员。

---

## 18. 2.0 扩展口（不实现）

1.0 只保留以下扩展空间：

- 全局稳定字符串 ID；
- 节点/边类型注册；
- 通用 `nodes + edges` 渲染输入；
- 未知字段与资产往返保留；
- Trace 事件域扩展；
- ExportProfile 扩展点。
- 稳定 skillId、版本约束和规范化 `skill://<skillId>/...` 引用形式；

2.0 才引入 `skill.meta.json`、文档关系图、知识语义图、工具图、多入口、多流程、跨 Skill 关系边、WorkspaceArtifact/联合运行和 Agent 专用导出。
项目级 `graph/_extensions.json` 与自定义节点/边类型注册也在 2.0 引入；1.0 不读取该文件赋予行为。

---

*Skill Designer 1.0 系统架构基线，2026-07-27*
