# 设计确认与遗留问题 v1.0

> **这份文档是什么**：你对专家评审的四条回复已经采纳，写进了《产品设计文档 v0.5》的决策记录（第 8–12 条）。本文档做三件事：
> ①核对你的四条回复分别解决了评审中的哪些问题；
> ②列出由这四条回复**引出的连带细节**，需要你确认；
> ③列出评审中**你还没有表态的遗留问题**——每条都附了建议方案，你可以整体回复"都按建议办"，也可以单独挑出不同意的条目。

---

## 一、你的四条回复解决了什么（核对）

### 回复 1：不承诺强制走流程，产品定位是"更可视化、更流畅地开发和维护 skill"

**解决了**：评审主题一（skill 包发出去后无法强制对方的 AI 按流程走）。这个问题因为定位调整而消解——我们不再把"强制"当卖点，引擎的校验和拦截依然存在并且在配合的模型面前有效，只是对外说法改为如实的"过程记录与辅助约束"。产品文档措辞已相应修改。

**顺带的效果**：评审建议的"AI 配合率测试"从必做项降级为可选项（既然不承诺，就不必专门度量承诺兑现率）；但 skill 包里那份说明文件的措辞质量依然值得打磨——写得越清楚，对方的 AI 配合的概率越高，这是免费的收益。

### 回复 2：提问机制必做；测试用例必须考虑提问；解析结果必须人工确认；对话组装归本地服务器

**解决了**：评审主题二的大部分——
- 2.1（对话是两层，设计里只画了一层）→ 由"提问机制升级为一等设计"解决：引擎有了"等待用户回答"状态，模型每一步可以"交回答"或"先提问"，对话记录进入事件流。
- 2.2（自动测试里没有"用户"角色）→ 由"测试用例预设用户回答；没有预设就按默认回答推进，结束时提示用例不完整"解决。方案很好：测试不会卡死，缺口也不会被掩盖。
- 2.5（解析质量没有把关手段）→ 由"解析结束必须人工确认、用户调整到满意为止"解决了**质量把关**的问题；"用户可提供需求文档指导构建"又给解析提供了方向依据。
- 2.4（提示词如何组装没有归属）→ 归属定了：本地服务器统一处理。

**没有完全解决的**：2.3 里有一半问题与质量无关、与**容量和费用**有关——一个几十篇文档的大 skill，把所有内容一次性塞给模型会超出它一次能读的上限，费用也不可控。人工确认解决不了"塞不下"。这一条列入下面第三部分的遗留问题（编号 B-7），和费用预算一起等你定。

### 回复 3：安全过滤措施保留，但留到系统功能完成后再做

**解决了**：评审主题三的执行时机问题——安全整体作为发布前的专项任务，不阻塞功能开发。我会在任务清单里增加一个"安全加固专项"，放在发布之前。

**有一个例外想请你确认**（见下面第二部分第 4 条）：有两个小的安全动作，现在做几乎不花时间、以后补反而费劲，建议不延后。

### 回复 4：所有写入统一归本地服务器

**解决了**：评审主题四的大部分——客户端里图谱、文档、会话、测试记录、备份、导出全部走服务器的同一个写入口。单一入口意味着：版本比对（防止两个窗口互相覆盖）、写入前检查、操作日志（将来做撤销的基础）都只需要在一个地方实现。

**没有完全解决的**：两个边缘情况需要你表态（见第二部分第 5、6 条）。

---

## 二、由你的回复引出的连带细节（需要你确认，共 6 条）

**1. 测试里的"默认回答"从哪来？**
建议：允许 skill 设计者在图里给每个会提问的环节写一个"测试用默认回答"；没写的，普通提问按"确认/同意"处理，需要用户点头的关卡按"同意"放行。凡是用了默认回答的地方，测试报告里逐条列出来（不只是一句"用例缺少内容"，而是列明"第几步的什么提问用了什么默认回答"）。
→ 你的裁决：＿＿＿＿

**2. "需求文档"是什么格式？**
建议：不设格式要求，自由书写的文本/Markdown 都行，解析时作为"这个 skill 想做成什么样"的指导性输入。将来有需要再考虑模板。
→ 你的裁决：＿＿＿＿

**3. 解析后的人工确认，以什么方式进行？**
背景：大 skill 解析出来可能有几百个节点，一个一个点确认不现实。
建议：确认界面 = 一份"变更清单"（新增了哪些节点、每个节点绑定了什么文档）+ 图上对应标注，用户整体浏览、想改哪就改哪，最后一键"全部确认"。确认之前，解析结果放在暂存状态，不影响现有图谱。
→ 你的裁决：＿＿＿＿

**4. 安全延后的两个例外，建议现在顺手做：**
（a）既然写入统一走服务器一个入口，"写入位置必须落在项目文件夹之内"的检查只需要在这个入口写一次——这是防止任何功能（包括将来的新功能）把文件误写或被诱导写到项目外的总闸，现在做是几行代码，以后到处补是大工程；
（b）界面上显示外来文档（导入的 skill、别人发来的 bug 报告）时，过滤掉里面可能藏的网页脚本——渲染库的一个配置开关。
其余安全项（访问口令、请求来源校验、恶意内容审计、防篡改等）全部延后到发布前专项。
→ 你的裁决：＿＿＿＿

**5. 客户端开着某个项目的同时，在终端里对同一个项目文件夹跑引擎命令——支持吗？**
建议：允许（这本来就是顺手的用法），服务器监控项目文件变化，发现外部改动就自动刷新界面并提示"文件已在外部被修改"；不做强制锁。用代码编辑器直接改文件也走同一套监控。
→ 你的裁决：＿＿＿＿

**6. 一个确认项：导出的 skill 包在别人电脑上运行时，那边没有我们的服务器**，引擎只能自己把会话记录写在 skill 包目录里——"写入统一归服务器"这条只适用于客户端场景，skill 包场景维持引擎自写。
→ 你的裁决（确认即可）：＿＿＿＿

---

## 三、评审中你尚未表态的遗留问题

### A 组：动工前必须定的技术修正（建议全部采纳；成本都不高，但错过时机就贵了）

| # | 问题（白话） | 建议方案 |
|---|---|---|
| A-1 | 引擎代码里写死了一个节点名字，别人的图不用这个名字流程就断 | 改成从图里声明，引擎不写死任何名字 |
| A-2 | 条件判断太弱：引擎明明自动查出了"不允许"的结论，流程却读不到这个结论、拐不了弯 | 小幅升级条件语法（支持读嵌套结果、不等于、包含判断） |
| A-3 | 引擎内部变量和 skill 作者定义的变量在同一个空间，重名就互相污染 | 分成两个空间，检查工具查重名 |
| A-4 | 模型走丢后，引擎给它的"回归路线"其实是走不通的（会被自己拒绝） | 算路线时区分"流程可走的边"和"知识关联边"；设计一个合法的重新入轨口 |
| A-5 | 按标题模糊匹配截取文档段落，英文文档标题重复时会**悄悄截错段落**、把错误知识喂给模型，而且没有任何环节能发现 | 支持精确定位（标题路径），模糊匹配只作备用并留下记录 |
| A-6 | 所有存盘文件（事件记录、会话、测试用例等）都没有格式版本号，产品将来一升级，旧文件怎么读没有答案 | 统一加版本号 + "读到不认识的内容跳过并计数，不许崩溃" + 提供升级转换命令 |
| A-7 | 图谱指纹的计算没统一换行符和编码，Windows 用户生成的 bug 报告会被集体误判为"版本不符" | 计算前统一换行符和编码，三个系统交叉验证同一指纹 |
| A-8 | 大模型接口只有真实调用一种，自动化测试没法稳定跑、还费钱 | 做成三种可替换实现：真实调用 / 照剧本回答（测试用）/ 录制回放；日常测试全用剧本版 |
| A-9 | "节点依次亮起"直接画在画布上，永远无法自动化测试 | 拆成两层：纯逻辑层（输入事件、输出每个节点该什么状态——可完整自动测试）+ 渲染层 |
| A-10 | 会话编号用弱随机数（可撞车）；生成 bug 报告要用户报会话编号（对方多半记不住） | 换安全随机数；生成报告时默认取最近一次会话并列出候选 |

→ 你的裁决（可整体回复"A 组全部同意"）：＿＿＿＿

### B 组：需要你提供的信息（都附了建议默认值，接受默认就很快）

| # | 问题 | 建议默认 |
|---|---|---|
| B-1 | 第一版给中文用户还是全球用户？ | 第一版中文；但引擎的提示消息底层用"错误码+参数"结构（界面层翻译成文字），给将来翻译留门——这一步必须现在做，以后补不了 |
| B-2 | Windows 第一版要支持吗？浏览器范围？ | 第一版以 macOS 为主，Chrome 系浏览器；Windows 作为发布前专项验证（文件保存的兼容写法从一开始就采用，避免返工） |
| B-3 | skill 包第一版只针对 Claude Code？ | 是，只针对 Claude Code；其他 AI 助手环境以后再说 |
| B-4 | 团队多人协作要考虑吗？ | 第一版明确"单人使用"；多窗口防覆盖已由"写入归一服务器"解决 |
| B-5 | 图的规模目标定多大？ | 以"500 节点、100 篇文档"为性能设计目标（大图的聚焦视图、增量刷新按这个规模验收） |
| B-6 | 用哪些真实 skill 做验收材料？ | 需要 3–5 个风格不同的真实 skill（英文的/纯文档的/带脚本的/大型的）。如果你手头没有，我从公开的 skill 里挑选后给你过目 |
| B-7 | 大模型的费用红线：单次解析最多花多少可接受？ | 需要你给一个数（比如每次解析不超过多少元）；另外大文档采取"先清点摘要、再分批细读"的省钱策略，超预算自动停下并保留进度 |
| B-8 | 导入 skill 的方式？ | 第一版：填本机文件夹路径（客户端和文件在同一台电脑，最自然）；上传压缩包以后再说 |
| B-9 | 对话面板主要给谁用？ | 以"设计者的调试台"为主（第一版的主用户是 skill 设计者），聊天体验为辅 |
| B-10 | 你的目标 skill 里，有没有"同时做几件事再汇合""对一批东西逐个执行同一套流程"这类需求的具体例子？ | 如果没有，第一版文档里明确声明不支持这些形态，并写清楚遇到时的绕行办法 |
| B-11 | skill 包要不要防篡改？ | 第一版不做签名；导出时附一份文件清单和校验值，文档里提醒接收方可以核对 |

→ 你的裁决（可整体回复"B 组按默认"，个别条目单独说）：＿＿＿＿

### C 组：会照做、无需你确认（通报一下）

以下评审意见没有争议、也不需要你提供信息，我会直接落进设计文档和任务清单：

- 文档里增加"模型能力边界"一节，诚实写明第一版不支持的流程形态和绕行办法；
- 通用性验收不再用 demo 自己证明自己（改用 B-6 的真实材料）；
- 静态解析的改进：识别脚本目录、步骤识别收紧（只认特定标题下的列表）、解析结果界面标注"收录了多少、没解析出什么、建议配模型后重新解析"；
- 测试评分算法换成能算出"多走/漏走/换序"的序列对齐算法（原方案算不出"换序"）；
- 实时通信补断线重连和续传；跑测事件进"动画播放队列"，保证肉眼能看到节点依次亮起（并提供"直接看结果"的快进）；通信消息里带项目标识；
- 导入向导界面、解析冲突处理界面补进任务清单（原来只有后端没有前端）；
- 过大的任务拆分（引擎、通信、解析、发布四个任务各拆小）；
- 新增"新用户首次使用体验"任务（首屏三入口：打开示例/导入/新建；示例项目第 5 分钟就能跑测看亮灯）；
- 产品自身的日志、长任务的取消按钮、"导出诊断包"；
- 编辑操作可撤销；删除节点时对相连边的处理规则；
- 会话/备份/测试记录的自动清理策略；
- 状态标识用"颜色+形状"双重编码（照顾色盲用户）、动画尊重系统"减少动效"设置。

---

## 四、你确认之后，我将按顺序执行

1. 把二、三部分的裁决回写《产品设计文档》《架构设计文档》（含新增"服务器安全模型（延后执行）""模型能力边界""对话双层设计"等章节）；
2. 新增一份《大模型交互协议设计文档》（提示词组装、结构化回答的强制手段、提问机制的消息格式、长对话截断——都在本地服务器一侧）；
3. 修订《TODOList》：任务拆分、新增任务（安全加固专项、首启体验、解析确认界面等）、验收材料落实；
4. 修订完成后给你过目，然后进入第一个任务的设计阶段。

*v1.0 · 2026-07-24 · 待你逐项裁决（二、三部分可分别整体回复）*
