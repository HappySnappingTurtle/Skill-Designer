# Skill Designer — 专家团评审报告 v1.1

> **这份报告是什么**：在正式开发前，四位不同领域的专家（系统架构与安全、产品通用性、大模型应用工程、测试与使用体验）各自独立审查了三份设计文档（《产品设计文档 v0.4》《架构设计文档 v0.2》《TODOList v1.1》），本报告是四份意见的汇总去重。
>
> **一句话总结论**：图引擎这一侧（图谱格式、事件记录、回放、不依赖大模型的解析兜底）设计得比较扎实；但有 **8 个"不解决就不能开工"的阻断级问题**，最集中的两个薄弱地带是：**所有和大模型打交道的环节都还没有真正设计**，以及**安全防护完全缺失**。

## 阅读须知：本报告的用词约定

设计文档里的一些内部叫法，本报告统一换成白话：

| 设计文档里的叫法 | 本报告的说法 | 意思 |
|---|---|---|
| 形式 A / Studio | **客户端** | 我们的桌面产品：图形界面 + 本地服务器 + 大模型接入，可以设计、测试、诊断、直接对话使用 skill |
| 形式 B | **skill 包** | 导出的一个文件夹，放进 Claude Code 这类 AI 编程助手里使用；里面带一个小引擎程序 |
| 宿主 / 宿主 agent | **对方的 AI 助手** | skill 包使用者自己环境里的那个 AI（比如他的 Claude Code）——不归我们控制 |
| 发卡 / 卡片 | **任务卡** | 引擎发给大模型的"当前这一步该做什么"的说明（含要求、参考文档、答复格式） |
| 回执 / produce | **答卷 / 答卷格式** | 大模型交回给引擎的结构化回答；"答卷格式"是每个节点预先声明的必填项与取值范围 |
| gate 节点 / 闸门 | **确认关卡** | 流程中必须拿到**用户本人真实答复**才放行的节点 |
| 严重度 P0 / P1 / P2 | **🔴 阻断 / 🟠 重要 / 🟡 建议** | 阻断=不解决不能开工；重要=对应任务开工前必须解决；建议=做了更好 |

---

## 主题一（🔴 阻断）：skill 包发出去之后，没人能强制对方的 AI 按流程图走——而文档把"能约束"当成了核心卖点

**问题是什么**：skill 包的工作方式是——对方的 AI 助手读到说明文件（SKILL.md），"按照说明的建议"去调用包里的引擎，一步一步领任务卡、交答卷。注意：这只是**建议**，没有任何机制能强制。对方的 AI 完全可能：读完第一张卡就自己把活干了、跳着调用、被引擎退回几次答卷后干脆不再理会引擎。

**会发生什么**：
- 产品文档宣传"把大模型约束在流程图的轨道内"、bug 报告是"完整的事实记录"——这两句话在 skill 包场景下都不成立。
- 更危险的是误导诊断：用户报障，bug 报告显示"流程停在第 2 步"，排查的人会以为流程图或引擎出了问题，而真相是对方的 AI 根本没继续用引擎。
- 现有的测试方案测的都是我们客户端里"程序强制驱动"的链路，对"对方的 AI 到底会不会配合"这个真实风险，**测试覆盖率是零**。

**建议怎么办**：
1. 产品文档诚实声明：skill 包的约束力来自说明文件的措辞质量，是"大概率有效"而不是"机制保证"。
2. 把 SKILL.md 说明文件当成核心工程资产来做：专门设计措辞模板（明确"必须经引擎推进、禁止绕开引擎直接读文档"的红线）、版本管理、持续打磨。目前任务清单里它只有"编辑与预览"，远远不够。
3. 新增一类测试：在真实的 AI 助手环境里跑任务，统计"实际领卡数 ÷ 应领卡数"，度量对方 AI 的配合率。
4. bug 报告里增加自检信息（比如每张卡从发出到交卷隔了多久、流程结束时间和用户报障时间差多远），并在报告开头标注"以下只是引擎看到的部分，对方的 AI 可能有引擎之外的行为"。

## 主题二（🔴 阻断，共识最强）：所有和大模型打交道的环节，设计上都只有一句话——五个具体缺口

架构文档里，大模型相关的模块（接口代理、解析器、带大模型的测试模式、对话面板）都只有一行字，但每一处都藏着会返工的深坑：

### 2.1 对话其实是"两层"，文档画成了"一层"

真实使用时同时存在两层交流：**用户和大模型聊天**（说需求、回答问题）+ **大模型和引擎交答卷**（结构化数据）。文档里的流程是"用户说一句需求 → 引擎发卡 → 大模型交卷 → 下一张卡"——用户只在第一步出现，之后是大模型和引擎的封闭循环。可是"确认关卡"节点必须有**用户本人的真实答复**，走到那里怎么办？要么大模型伪造用户答复（引擎无法识别伪造），要么流程卡死。

缺的东西：引擎需要一个"**等待用户发言**"的状态；大模型每一步的输出要允许两种选择——"交答卷"或"我需要先问用户一个问题"；事件记录里要有"用户说了什么""大模型说了什么"这两类事件（目前 11 种事件全是引擎视角）；对话界面要拆成"聊天区 + 流程卡片区"两块。**这必须在实时通信协议定稿（任务 T12）之前想清楚，否则协议要推倒重来一次。**

### 2.2 带大模型的自动测试里，没有"用户"这个角色

测试用例里写着"大模型模式：不预设答卷，由大模型实时作答"。但只要流程里有确认关卡（正经 skill 几乎都有），测试走到关卡必然失败——因为没有人扮演用户来给答复。这是测试框架自身的缺陷，却会被记成 skill 的失败；更糟的情况是大模型学会编造用户答复，测试全绿、真实风险被掩盖。

建议：测试用例增加"预设的用户答复剧本"字段（便宜、结果稳定）；进阶方案是引入"**模拟用户**"——用第二个大模型按角色设定扮演用户，这也是检验"skill 能不能通过多轮对话把信息问全"的唯一办法。

### 2.3 让大模型解析一个旧 skill 时，没设计"文档怎么喂给它"

解析的对象是别人 skill 里的几十篇文档，这些是每次运行才知道的动态输入。而引擎发卡机制只能携带"图谱里预先绑定的文档"，没有通道把待解析的文件塞进任务卡。若简单粗暴全部塞入：一个 50 篇文档的 skill 一次就是十几万字，解析流程有六个阶段还要循环修错——要么超出模型的上下文容量，要么费用失控。全部文档中**没有任何一处估算过费用**。

建议：先清点并生成每篇文档的摘要，后续阶段按需一批批喂；或给解析用的大模型配一个受控的"读文件"工具；设定单次解析的花费上限，超限自动降级为不依赖大模型的静态解析并说明原因；解析中断后要能从断点续跑；抽完流程骨架后先停下来让用户确认，再继续细化（既省钱又保质量）。

### 2.4 任务卡如何变成给大模型的提示词，完全没设计

包括：卡片以什么格式进入对话上下文；答卷怎么保证拿回合法的结构化数据（正确做法：把答卷格式直接翻译成 API 的工具调用参数约束，让接口层在生成时就管住格式，比"生成完再校验、打回重写"省一整个来回）；退回答卷时错误信息怎么拼给大模型；长对话里历史任务卡（每张带几千字文档）无限堆积怎么截断。建议在动工前补一份**《大模型交互协议设计文档》**，把 2.1–2.4 一起定稿。

### 2.5 解析质量没有评判标准

"通过格式检查"只能保证图谱结构合法，不能保证内容正确。最危险的是**答卷格式的"幻觉"**：大模型给某个节点编造出文档里根本不存在的必填项——它会变成运行时的硬关卡，之后真实使用中合法答卷被反复退回，用户完全想不到根因在解析环节。

建议：人工精修 3–5 个真实 skill 的"标准答案图谱"作为评测集，定义可计算的指标（流程节点找全率、确认关卡位置对不对、文档绑定对不对、答卷格式每一项能否在原文档中找到出处）；要求解析产物中每个必填项都附"文档出处"，给不出出处的自动降为选填。

## 主题三（🔴 阻断）：本地服务器是"裸奔"状态，缺乏最基本的安全防护

**问题是什么**：客户端的本地服务器没有设计任何身份校验，而它的接口能读写文件。这里有一个反直觉的常识：**你在浏览器里打开的任何网页，都可以偷偷向你电脑上的本地服务器发请求**（浏览器只拦"读取回应"，不拦"发送"，而写文件只需要发送就够了）。

**会发生什么**：你开着客户端时随手浏览了一个恶意网页，它就能调用"写文档"接口，把内容写到项目之外的位置——比如改写你的终端启动脚本，等于在你电脑上执行任意命令。

**建议怎么办**（全部列入服务器任务的验收标准）：服务器只监听本机（127.0.0.1）；启动时生成一个随机口令，界面通过带口令的链接打开，之后每个请求都验口令；校验请求来源；所有涉及路径的参数，先解析成真实路径、再强制检查落在项目文件夹之内（防"../"逃逸和符号链接逃逸）。

**同一主题的其他问题**：
- 🟠 **导入的东西都是不可信内容**：别人发来的 bug 报告文件、导入的外部 skill 文档，都可能藏恶意内容——文档里嵌脚本（渲染时要过滤 HTML）、超大文件（要设大小上限）、压缩包里带逃逸路径的文件名。甚至有更隐蔽的一招：恶意 skill 的说明文档可以**反过来指挥解析用的大模型**，生成带后门的图谱（比如把某个确认关卡设成永远自动放行、在命令模板里埋危险命令）。因此解析产物里"命令、自动放行条件"这类有执行力的字段，必须强制人工确认。
- 🟠 **API 密钥的存放两份文档说法矛盾**（一处说存前端、一处说存服务器）。应拍板：只存在服务器端、且放在项目文件夹**之外**（避免随导出打包泄露）；前端只能写入不能读回。
- 🟠 **命令模板是注入口**：节点上的命令模板会用变量填充，变量来自大模型答卷和用户输入，目前没有任何转义——用户输入里带一段 `"; 危险命令` 就能混进最终命令。需要改为参数化拼接。另外客户端对话中走到"执行命令"节点时**到底谁来执行**（不执行则流程断，服务器代执行则打开风险面）——文档完全没写。

## 主题四（🔴 阻断）：几个入口同时改同一份数据时，会互相覆盖且毫无提示

**问题是什么**：同一个项目文件夹可能同时被三个入口写：客户端界面、命令行引擎（这两个同时用恰恰是产品鼓励的）、用户直接拿代码编辑器改文件。目前的防冲突设计只覆盖了客户端内部。

**会发生什么**：
- 命令行里跑着一个会话，你同时在客户端里改了图——会话后半程跑在新图上，最后 bug 报告里记的图谱指纹对不上实际运行的图，整个"版本对齐回放"功能被污染。
- 开两个浏览器标签页编辑同一节点，后保存的把先保存的**无声覆盖**——而且这次误覆盖还会被标记成"人工编辑"，从此受到重解析时的保护，错误被永久固化。
- 用编辑器直接改文件后，客户端里的检查结果、指纹、变更角标全部悄悄过期，没有任何机制发现。

**建议怎么办**：项目加锁文件（一边占用时另一边写入前先警告）；每个会话记录开始时的图谱指纹，图变了就给会话打上"图已变更"标记；保存请求带上"我基于哪个版本改的"，版本对不上就返回冲突提示而不是覆盖；服务器监听文件变化，发现外部改动就重新检查。另外"所有写盘只在三个模块"这条纪律，和自家功能清单冲突（备份、导出、测试报告都在清单之外）——需要改写成一张"写操作分类表"，每类写操作指定专属模块和各自的规矩。

## 主题五（🟠 重要）：引擎里的七个具体技术缺陷——修起来便宜，但必须趁早（都在引擎任务 T3/T4 动工前定）

1. **有个节点名字被写死在引擎代码里**（队列调度节点 `dispatch`）：只要别人的图里这个节点不叫这个名，流程走到那一步就断。这与"引擎不含任何具体业务"的纪律直接矛盾。
2. **条件表达式太弱，读不到查询结果**：引擎能自动做知识查询并给出"通过/不通过"的裁决，但条件表达式语法上写不出"如果裁决是不通过就走另一条边"——于是"机制上强制拦截"退化成了"指望大模型自觉"。需要小幅升级条件语法（支持多层字段访问和不等于/包含判断）。
3. **变量名会撞车**：引擎自己依赖几个内部变量（队列、查询结果等），但 skill 作者声明的答卷字段可以叫同样的名字，一撞车就污染引擎内部状态；还有一个"清空状态"机制，白名单少写一项就把待办队列悄悄清空了。需要把引擎内部变量和用户变量分成两个空间，加检查规则。
4. **走丢之后没有回头路**：引擎宣传"迷路了可以搜索图谱、计算路径接回主线"，但路径计算会把"知识关联边"也当成可走的路——照它给的路径走，下一步就会被引擎以"非法跳转"拒绝。等于给了一张不能用的地图。需要区分"流程可走的边"和"知识关联边"，并设计一个合法的"重新入轨"机制。
5. **文档切片可能悄悄切错段落**：按标题模糊匹配取文档章节，匹配规则是"互相包含就算命中、取第一个"。英文文档里 Usage、Examples 这类标题重复出现是常态，很容易切到错误章节——**把错误的知识喂给大模型，而且全链路没有任何环节能发现**（这是唯一无法被现有机制观测到的失效方式）。需要支持精确定位（标题路径），模糊匹配只作降级并留痕。
6. **队列内容没有检查规则**：待办队列由大模型答卷写入、调度节点按路由表消费，但"答卷允许的取值"和"路由表的键"是否一致没有校验——不一致时引擎会走到一个不存在的节点，行为未定义。
7. **会话编号用弱随机数生成**（6 位、可预测、可撞车），而它同时是文件名和接口参数；另外生成 bug 报告要求填会话编号，但对方的 AI 长对话后很可能记不住编号——应默认取最近一次会话并列出候选让用户确认。

## 主题六（🔴 阻断）："通用"两个字还没有经过真正的检验

**问题一：模型的能力边界没有向用户交代**。现在的流程引擎是"单线程走图 + 一个待办队列"，以下常见形态放不进去：并行分支（同时做两件事再汇合）、子流程复用（两条路线共用一段确认流程）、逐项迭代（"对每个文件执行同一套子流程"）、中途打断（用户变卦）、以及**纯知识型 skill**（只有参考资料没有流程——真实世界里占比很高，对这类 skill 本产品的流程约束能力完全无用武之地）。第一版不支持可以，但文档必须诚实列出"不支持什么、遇到了怎么绕、以后怎么演进"——现在一个字都没有。

**最扎心的发现**：我们自己内置的"解析 skill"（清点文件 → 逐篇文档处理）恰恰就是"逐项迭代"形态——**用自己的引擎跑自己的解析器，可能第一个撞上自己模型的墙**。评审建议：动工前先用图模型把解析 skill 在纸面上画出来，画不出来就说明模型有缺陷，趁没写代码赶紧改。

**问题二：通用性的验收在"自己证明自己"**。任务清单里用来验收通用性的测试数据，是当初孕育这套模型的那个 demo 的 29 个节点——用模型的出生地证明模型的普适性，逻辑上是循环的。必须换成 3–5 个**风格迥异的真实外部 skill**（英文的、没有规范头信息的、纯文档型的、带脚本工具的、几十篇文档的大型的）作为固定验收集。

**问题三：静态解析对真实 skill 的效果存疑**。真实的 skill 常带 scripts 脚本目录（这是"命令节点"的最强线索，解析规则里却完全没有）；英文文档里的有序列表大多是功能清单而非步骤，会被误展开成假流程链。用户导入一个成熟 skill，第一眼看到"一条假流程 + 一团无结构的文档云"，多半直接弃用——**产品的第一印象恰好由它最弱的路径承担**。需要：步骤识别只认特定标题（Workflow / Steps / 步骤）下的列表；增加脚本目录的识别规则；解析结果界面诚实标注"已收录 X 篇文档为知识节点；未能提取流程结构，建议配置大模型后重新解析"。

**问题四：测试评分算法与承诺的功能对不上**。已拍板"路径不完全一致时要逐项提示：多走了什么、漏走了什么、顺序换了什么"——但设计里的算法是简单的子序列判断，只能给出"命中/没命中"，根本算不出"换序"。需要换成序列对齐类算法（类似文本 diff），这个规格现在不补，实现时才会发现写不出来。

## 主题七（🟠 重要）：版本升级和跨平台问题——现在不设计，将来无法补救

1. **文件格式都没有版本号**：事件记录、会话文件、测试用例、扩展声明——全都没有格式版本字段。产品一旦迭代（比如事件增加新类型），已经发出去的旧 skill 包会持续产出旧格式的 bug 报告寄回来，新客户端打开时是崩溃还是错乱，未定义。所有落盘格式统一加版本号 + "读到不认识的内容要跳过并计数，不许崩溃"写进纪律 + 提供升级迁移命令。**必须在定事件格式的任务（T3）里一并定死，事后补是伤筋动骨。**
2. **图谱指纹在 Windows 上会集体失真**：指纹计算没有规定换行符和文件名编码的统一规则。Windows 上 git 默认会把换行符改成另一种，Mac 的文件名编码方式也和其他系统不同——结果就是：Mac 上导出的包，Windows 用户生成的**每一份 bug 报告都会被误判为"版本不一致"**，版本对齐回放这个核心功能形同虚设。计算指纹前统一换行符、统一编码，三个系统交叉算出同一指纹进测试用例。
3. **对 Windows 只字未提**："分发给任何人"的产品，文档里却处处是 Mac 假设：文件保存用的"重命名替换"手法在 Windows 上会因为文件被杀毒软件或编辑器占用而报错（需要重试机制）；启动脚本是 Mac 专属格式；建议启动方式改成跨平台单命令。
4. **界面语言没有决策**：引擎的提示语、检查消息、任务卡、解析产物目前全是中文，和"分发给任何人"矛盾。不必第一版就做多语言，但有一件事必须现在做：**引擎对外的所有消息改成"错误码 + 参数"的结构化形式，由界面层翻译成文字**——这个架构决策事后无法补做。另外"第一版只做中文"如果是决策，就写进文档，别让它以疏忽的形式存在。
5. **缺一个"运行环境指纹"**：图谱指纹只能锚定"图变没变"，但"用的哪个模型、提示词哪个版本、解析器哪个版本"都没有记录——"同一张图昨天能跑今天不能跑"时，这三个维度无法归因。

## 主题八（🔴 阻断）：按现在的设计，好几个核心功能将来没办法自动化测试

这个项目自己的开发规则是"先写测试用例文档再写代码"，但两处设计让这条规则在后期会执行不下去：

1. **大模型相关功能没有"替身"**：会话、解析、大模型测试模式的验收全都只能靠真实调用大模型 API——结果不稳定、花钱、有些分支（比如"连续退回答卷直到放弃"）根本无法故意复现。解决办法是把大模型接口做成**可替换的三种实现**：真实调用 / 照剧本回答（从文件读"每一步回什么"，包括故意答错触发纠错分支）/ 录制回放。日常测试全用剧本版，真实调用只留最终验收时冒烟一次。
2. **"节点亮灯"画在画布上，无法断言**：亮灯直接渲染在 WebGL 画布里，没有可检查的页面元素——现在不管，以后想补测试都补不进去。解决办法是把亮灯层拆成两半：**纯逻辑的状态机**（输入事件流，输出"每个节点现在该是什么状态"的表格——可以完整自动测试）+ 薄薄的渲染壳。
3. 其他配套缺口：实时通信没有断线重连和"从第 N 条事件续传"的设计（跑测中途切个窗口回来就断了，只能重跑）；脚本测试毫秒级跑完但亮灯动画是秒级的，事件到达速度远超动画播放速度，前端需要一个"动画播放队列"（否则用户只能看到最后一帧全亮，"依次亮起"的卖点不存在）；通信协议里没有"属于哪个项目"的字段（同时开两个项目就乱了）；导入向导界面和重解析冲突裁决界面**在任务清单里没有归属**（后端做了、前端没人做，最后验收时才发现）；几个任务的体量严重超标应当拆分（引擎任务 T3 实际是三个任务的量，解析任务 T19、通信任务 T12、发布任务 T20 各是两个）。

## 主题九（🟠 重要 / 🟡 建议）：使用体验与日常运维的缺失

1. **大图会卡死体验**（🟠）：静态解析"每篇文档一个节点"，导入真实大型 skill 直接产出几百个节点；而现在"每次保存 → 整张图刷新重排"，用户刚记住的图形位置全部作废。需要：节点位置持久化；变更只推送增量；提供"以某节点为中心看周边几层"的聚焦视图（这比 3D 全景重要得多）；向产品负责人要一个明确的规模目标来定性能预算。
2. **新用户第一小时没有任何设计**（🟠）：首次打开是空白界面加一堆陌生概念。建议：首屏三个入口（打开示例 / 导入已有 skill / 新建）；示例项目预装一套测试用例，让用户**第 5 分钟就能一键跑测看亮灯**——这是产品最打动人的时刻，不该藏到第 5 天。
3. **产品自己出了问题没法排查**（🟠）：没有日志设计、长任务（如大模型解析）**没有取消按钮**、界面白屏时用户说不清也导不出任何诊断信息。讽刺的是：产品用"发布即黑盒"批评传统 skill，自己却复刻了同样的黑盒。需要：结构化日志、统一的"任务状态查询/取消"接口、"导出诊断包"命令。
4. **没有撤销功能**（🟡）：误删一个连着十几条边的关键节点，唯一的恢复手段是手工重建。写入口是唯一的，顺手记一份操作日志就能实现撤销，成本很低；删除节点时"连的边怎么办"（拒绝删/一起删/留着报错）也没定义。
5. **数据只增不减**（🟡）：会话、备份、测试记录都没有清理策略，长期使用目录会膨胀到几个 GB；会话文件每一步都全量重写，越写越慢——改成追加式写入。
6. **脱敏没盖全**（🟡）：目前只对答卷字段脱敏，但最终变量、查询轨迹、填充后的完整命令、用户原话同样带敏感信息。
7. **色盲用户不可用**（🟡）：拒收标红、对比用红绿双色——红绿恰是最常见色盲（约 8% 男性）的盲区。每种状态改用"颜色 + 形状/线型"双重编码；动画尊重系统的"减少动效"设置。这些必须在渲染器设计时定死，事后翻改很贵。

---

## 需要你回答的 12 个问题

这些是文档里推断不出来、必须由你拍板的信息：

| # | 问题 | 为什么需要答案 |
|---|---|---|
| Q1 | **给谁用**：第一版面向中文用户，还是全球分发？ | 决定界面/引擎提示语的语言策略，以及多语言改造是现在做还是声明不做 |
| Q2 | **什么系统**：Windows 第一版必须支持吗？浏览器只保 Chrome 系还是都要？ | 影响文件保存实现、启动方式、换行符处理一整串问题 |
| Q3 | **skill 包给谁的 AI 用**：只针对 Claude Code，还是也要兼容其他 AI 编程助手？ | 说明文件（SKILL.md）按谁家的规范写 |
| Q4 | **几个人用**：第一版接受"一个人、一个窗口"吗？开多个浏览器标签页、团队多人共用（git 协作）要不要考虑？ | 决定要不要做防覆盖锁和文件组织方式 |
| Q5 | **多大规模**：预期支持的最大图规模（几百节点？几千？）、最大文档量？ | 大图视图、性能预算、解析分批、费用上限全依赖这个数 |
| Q6 | **拿什么验收**：能否选定 3–5 个风格不同的真实 skill（英文的/纯文档的/带脚本的/大型的）作为固定测试材料？有没有人力给它们做"标准答案图谱"？ | 不然"通用"永远是自己证明自己 |
| Q7 | **大模型的预算和权限**：一次解析可接受花多少钱？自动化测试里允许真实调用大模型吗？模型要不要锁定具体版本？允许给大模型配"读文件"工具吗？ | 决定解析架构、测试策略、成本护栏的取值 |
| Q8 | **导入怎么进来**：用户导入 skill 是"填一个本机文件夹路径"，还是"上传一个压缩包"？ | 两种方式的安全设计完全不同 |
| Q9 | **同时用两个入口吗**：客户端开着某项目的同时，在终端里对同一项目跑命令行——这是要支持的用法，还是明确禁止？ | 决定加锁方案 |
| Q10 | **对话面板主要给谁**：是给 skill 使用者的"聊天窗口"，还是给设计者的"调试台"？ | 决定双层对话界面里聊天区和调试区谁为主 |
| Q11 | **有没有并行需求的实例**：你目标中的 skill，有没有具体案例需要"同时做两件事再汇合"、"复用同一段子流程"、"对一批东西逐个执行"？ | 决定模型边界声明的措辞和后续演进的优先级 |
| Q12 | **信任问题**：skill 包里带着可执行的引擎代码，接收的人要不要能验证"这个包没被篡改"（签名/校验清单），还是文档里提醒一句就行？ | 决定导出器要不要做完整性校验机制 |

---

## 建议的后续动作（待你对以上裁决后执行）

1. **回写设计文档**（动工前必须完成）：架构文档新增「服务器安全模型」「模型能力边界」「对话双层模型」「写操作分类表」四节；修订事件格式（加对话类事件与格式版本号）、指纹计算（换行符/编码归一 + 运行环境指纹）、测试评分（换序列对齐算法）、通信协议（断线续传/项目字段/动画队列）。
2. **新增一份《大模型交互协议设计文档》**：统一解决主题二的五个缺口，在通信协议和大模型接入任务动工前定稿。
3. **修订任务清单**：拆分 T3/T12/T19/T20；新增「首次使用体验」「AI 配合率测试」「SKILL.md 措辞模板工程」；补导入向导与冲突裁决的前端任务归属；验收材料集（Q6）作为前置采购项。
4. **三个便宜但必须趁早的引擎修复**记入引擎任务的设计文档：去掉写死的节点名、升级条件表达式、隔离引擎内部变量。

*v1.1 · 2026-07-24 · 按可读性要求全文重写（内部代号全部替换为白话，术语首次出现即解释）· 待逐项裁决 Q1–Q12*
