# Skill Designer 1.0 - 产品设计文档

> 设计基线：v1.0
> 日期：2026-07-27
> 产品主线：设计 -> 确认 -> 调试 -> 诊断 -> 修复 -> 导出

---

## 1. 产品定位

Skill Designer 1.0 是一个面向 **Skill 开发者和维护者** 的本地开发工具。Workspace 是产品的根对象，一个 Workspace 可以同时组织多个 Skill Project；每个具有流程逻辑的 Skill Project 以有向图建模。用户能够在同一个工具中完成：

- 创建 Workspace，并添加、导入或切换多个 skill；
- 编写节点、边和具体文档；
- 使用页面或 LLM 设计助手修改 skill；
- 编写测试用例并反复调试；
- 通过 ExecutionTrace 和 Bug Report 回放可观测过程；
- 分析问题、生成修复建议，经用户确认后辅助落地；
- 导出不绑定 Claude、Codex 或其他 Agent 的通用 skill 包。

Skill Designer 是工具，不是替用户接管 skill 设计的自动决策系统。工具可以检测、分析、解释、建议并生成修改方案，但用户 skill 的最终设计和修复决策归用户。

### 1.1 要解决的问题

| 问题 | 1.0 的解决方式 |
|---|---|
| 多个 skill 分散、无法同时掌握状态 | Workspace 汇总多个 Skill Project 的类型、Git、lint、revision、运行和测试状态 |
| 图和文档难维护 | 图形化编辑节点与边，文档编辑与节点绑定，保存前校验 |
| 修改过程不可见 | ChangeSet 展示文件和图的 diff，用户确认后才生效 |
| skill 效果难调试 | 用户、外部 Agent 或 Studio 助手编写测试用例，反复运行并判定结果 |
| 故障难复现 | 持久化 ExecutionTrace，导出脱敏 Bug Report，在图上回放 |
| 只看到失败点，不知道原因 | 用证据驱动的 Diagnosis 区分 skill、模型、文档、环境和工具问题 |
| 修复建议与真实修改脱节 | Diagnosis 生成 ChangeSet，用户确认后由本地服务器原子提交 |
| 导出物过度绑定某个 Agent | 1.0 只导出通用包，专用 Agent 适配延后到 2.0 |

### 1.2 核心价值

1. **所见即所改**：页面编辑和 LLM 设计助手最终落到同一份项目文件。
2. **所改须确认**：解析、重载、设计助手和诊断修复都先生成 ChangeSet。
3. **所行有证据**：运行过程、对话、校验、工具调用和文件变化通过 Trace 关联。
4. **所报可回放**：Bug Report 是 Trace 的脱敏导出视图，可以导入回放。
5. **所修可验证**：建议修复经用户确认后落地，再通过测试用例重新验证。
6. **多 Skill 可组织**：Workspace 统一呈现多个 Skill，但保持各自文件、版本和运行边界。

---

## 2. 1.0 能力边界

### 2.1 支持的 skill

1.0 主要支持具有明确流程逻辑的 skill：

- 线性步骤；
- 条件分支；
- 用户确认节点；
- 回退边；
- 知识节点和文档绑定；
- 声明式查询或上下文组装；
- 工具或脚本的描述与展示。

导入的 skill 如果没有识别出可执行流程，仍可以作为文档和知识内容打开、搜索和编辑，但必须显示：

> 当前版本未提取到可执行流程，仅作为文档/知识内容展示。

系统不得为了“构造即合法”而臆造虚假流程。

1.0 同时支持在一个 Workspace 中加入多个 Skill Project，查看其摘要状态并切换当前 Skill。图谱、文档、ChangeSet、运行、Benchmark 和导出仍以单个 Skill Project 为作用域。

节点可以在 `lookup[]` 中声明精确节点、节点邻域、图谱搜索和文档切片四类项目事实查询。查询只作用于本 Skill 图和本次运行冻结的 revision，不跨 Workspace 成员猜测事实。文档切片默认使用完整标题路径或 `#anchor`；按末级标题匹配必须由用户显式开启降级。唯一降级命中、多匹配歧义、缺失和成功空结果是四种不同事实，界面和 Trace 都不得把它们折叠成普通成功。

这里的“查看多个 Skill”不是只有切换下拉框：工作区首页必须同时列出全部成员及其类型、可用状态、revision、Git、lint、最近运行和测试摘要。进入图谱、文档、测试或诊断后，界面才收窄到明确选中的一个 Skill，避免把多项目内容混成一个事实源。

### 2.2 1.0 不承诺的能力

- 并行分支后汇合；
- 通用子流程复用；
- 对集合逐项执行动态子流程；
- 跨 Skill 引用边、联合图谱、联合运行和跨 Skill 原子 ChangeSet；
- 文档关系图、工具图、多入口和多图联动元模型；
- 未经用户确认的自动改图和自动修复；
- 对所有 Agent 的自动安装、自动发现和完全一致运行；
- 回放外部 Agent 中未经引擎或适配层观测的行为。

### 2.3 2.0 路线

2.0 规划引入 `skill.meta.json` 和多层 skill 元模型，覆盖文档关系图、知识语义图、工具图、多入口、多流程，以及带稳定 skillId、版本约束和目标入口的跨 Skill 引用与编排；Claude/Codex 等专用适配也放在 2.0。2.0 不进入 1.0 的开发范围。

---

## 3. 目标用户与界面定位

### 3.1 主用户

1.0 主用户是 Skill 开发者和维护者：

- 从零创建有流程逻辑的 skill；
- 导入现有 skill 并继续维护；
- 为 skill 编写节点、文档和测试用例；
- 定位问题并修复；
- 导出给其他 Agent 环境使用。

### 3.2 主导航

1.0 主导航只保留：

```text
工作区 / 图谱 / 文档 / 测试 / 诊断
```

工作区页展示多个 Skill Project 的摘要和当前选择；其余页面始终显示当前 Skill 切换器。LLM 设计助手是跨页面侧边工具，不另做一个与业务页面平行的聊天产品。

### 3.3 对话的两种模式

| 模式 | 用途 | 权限 |
|---|---|---|
| 设计助手 | 添加/修改节点、边、文档、测试用例和修复方案 | 只能生成结构化 ChangeSet，不能直接写文件 |
| 运行/调试 | 不接入外部 Agent 时，在 Studio 内使用和调试 skill | 不得修改项目文件，只生成 Trace 和运行结果 |

界面必须明确显示当前模式，不得只依靠提示词区分权限。

---

## 4. 产品形态

### 4.1 Skill Designer Studio

1.0 以本地服务器 + 浏览器页面交付：

- React + Vite 前端；
- Node.js 本地服务器；
- 零业务域逻辑的图引擎；
- 可配置的 LLM Provider；
- 本地 Workspace、多个 Skill Project、Trace、诊断与测试记录。

Studio 1.0 正式支持 Windows 和 macOS，第一版界面语言为中文。

### 4.2 通用 skill 包

1.0 导出一种中立格式，不针对 Claude、Codex 或其他 Agent 生成专用文件。

```text
<skill-name>/
├── SKILL.md
├── skill.json
├── graph/
├── docs/
├── benchmarks/
├── scripts/             # 原 skill 存在时保留；未完成沙箱前不执行
└── engine/
    ├── skill-engine.mjs # 零 npm 运行时依赖的 CLI
    └── README.md        # 中立格式的接入与命令说明
```

通用包要求目标 Agent 至少能够读取文件、执行本地 Node CLI 并处理结构化结果。1.0 不承诺自动安装、自动发现或所有 Agent 中完全一致的使用体验。

CLI 负责读取图、计算条件分支并推进一个外置 JSON 状态文件，不执行 `scripts/` 中的业务脚本，也不发起网络请求。用户或外部 Agent 可以检查结构、查询合法出口，或执行开始、查看、推进、暂停、恢复和停止；提交非法下一节点时保持原状态并返回可解释的拒绝事件。状态文件应放在导出包之外，避免污染不可变包内容。

---

## 5. 核心对象与状态

### 5.1 Workspace

Workspace 是用户在 Studio 中组织工作的根容器，包含一个或多个 Skill Project 引用。1.0 的 Workspace 提供：

- 创建、重命名、打开和删除本地 Workspace 清单；删除清单默认不删除成员 Skill 文件；
- 添加新 Skill、导入副本或原地打开已有 Skill 仓库；
- 同时查看成员 Skill 的类型、Git 状态、lint、active revision、最近运行和测试摘要；
- 按 Workspace 私有顺序排列成员并按名称或 skillId 检索；
- 选择当前 Skill，并在图谱、文档、测试和诊断页面快速切换；
- 从 Workspace 移除 Skill 引用，但默认不删除其仓库或管理工作区文件。

成员状态至少区分 `pending-import / ready / missing / error`。没有已确认 skillId 的导入项只能审阅，不能运行、Benchmark 或导出。

Workspace 本身不把多个 Skill 文件合并成一个仓库，也不拥有统一 active revision。1.0 中任何编辑、运行和导出都必须明确指向一个 Skill Project。

Workspace 也是未来 Skill 联动的本地解析上下文，但不是依赖关系的事实源。2.0 的跨 Skill 依赖必须由发起方 Skill 的可移植元文件声明，Workspace 只负责把 `targetSkillId` 解析到已加入的成员、展示依赖是否满足，并引导用户补充缺失 Skill；不得按显示名称或目录名猜测目标。

“原地打开”要求用户显式输入并确认 Skill 根目录绝对路径。服务端解析 realpath，只接受已有有效 `SKILL.md + skill.json + graph/main.json` 的完整项目；打开时不写用户目录，只在 Studio 私有目录建立 source 映射、Revision、Snapshot 和 Baseline。后续经用户确认的 ChangeSet 会写回该目录，因此界面必须持续显示 `原地打开` 模式。

原地项目移动后保留成员记录并标记 `missing`，用户可显式选择新根目录修复映射。修复只适用于 `in-place`，新目录必须精确保持原 `skillId`、通过格式与图校验，并与当前 active Revision 的内容 hash 一致；否则拒绝关联。工具不按名称猜测目录，不自动覆盖目录内容。异常成员的原因和修复入口在 Workspace 展示，但不影响其他 ready 成员继续工作。

工作区顶栏以及图谱、文档、测试、诊断业务页必须同时显示当前 Skill 的名称和稳定 `skillId`。长 ID 可以在紧凑区域省略中段，但完整值必须可查看；名称仅用于识别，不得作为切换、报告匹配或请求作用域。诊断页还需把“当前操作 Skill”与报告内记录的 Skill 身份分开显示，避免用户把导入报告的目标误认为当前选择。切换成员后所有业务页从同一个 WorkspaceMember 事实刷新身份，内容型空状态也不能丢失该上下文。

### 5.2 Skill Project 与稳定身份

每个 Skill Project 在 `skill.json` 中保存稳定 `skillId`。`skillId` 不依赖显示名称、目录路径或 Git 仓库位置；重命名和移动目录不会改变它。节点 ID 只要求在所属 Skill 内稳定，未来跨 Skill 引用使用 `skillId + nodeId`，不得依赖名称猜测目标。

项目内容只有两个业务状态：

```text
draft --用户确认并原子提交--> active
```

- `draft`：创建、导入、解析、重载或修改后的待审提案。
- `active`：已确认、可以生成 RuntimeArtifact 和导出的 revision。

ChangeSet 自身使用 `proposed / rejected / applied / conflicted` 状态。导出是绑定某个 active revision 的独立记录，不是项目状态，也不会改变 active 内容。

工作区详情允许编辑 Skill 的名称、版本和说明，保存统一形成 `skill.update` ChangeSet。稳定 `skillId`、能力类型和入口不在该操作的可编辑范围；需要改变流程能力时应进入图和项目迁移设计，不能借信息表单同时改写身份与执行语义。

### 5.3 ChangeSet

ChangeSet 是所有工具辅助修改的唯一批准边界：

- 所属 workspaceId、projectId 和 skillId；
- 基于哪个 revision；
- 将新增、修改或删除哪些节点、边和文件；
- 修改理由与证据；
- lint 和影响分析；
- 可视 diff；
- 确认、拒绝或编辑后重新提交；
- 确认后原子应用，失败时不得部分生效。

确认界面统一展示提案来源、稳定来源 ID、修改原因和逐条证据。手工编辑默认标记为 `manual`；设计助手绑定会话 ID，并同时保存用户请求与模型引用的项目事实；诊断修复绑定 diagnosisId 和候选证据；报告候选绑定 reportImportId、reportId 与来源运行；从冻结运行生成的候选用例绑定 runId 和 RuntimeArtifact。来源和证据只回答“这份提案从哪里来、基于什么事实”，不授予权限，也不表示提案已经验证或可信；用户仍须结合 diff 明确确认。

`digest` 同时覆盖操作、理由、来源和证据。即使文件操作完全相同，只要依据发生变化，也必须形成不同摘要并重新确认。旧数据缺少来源时只标记为历史记录，不反推或伪造证据。

1.0 的一个 ChangeSet 只能修改一个 Skill Project。需要修改多个 Skill 时分别提案和确认，不承诺跨仓库原子事务。

确认界面的三个动作语义必须分开：关闭预览或“返回编辑”只回到当前草稿，不代表拒绝；“拒绝提案”把用户实际审阅的 `changeSetId + digest + baseRevision` 持久化为 `rejected`，不写项目文件、不推进 revision，且该提案不能再应用；用户继续调整保留在页面中的草稿时，应生成新的 ChangeSet 再审阅。1.0 的拒绝原因由具体入口写入简短来源说明，不要求用户额外填写表单。

确认时若项目 revision、目标文件、图、文档引用、测试用例或撤销目标与预览事实不同，原 ChangeSet 进入只读 `conflicted` 终态。确认界面不得关闭后只留一条瞬时错误，而要保留原 diff，并展示冲突类型、原 base revision、当前 revision 和服务端事实说明。1.0 不自动合并；用户只能关闭并保留当前项目，或显式选择“基于当前版本重新预演”。重新预演创建新的 ChangeSet、新 digest 和基于当前事实的新 diff，不写项目；用户必须重新审阅并再次确认。旧冲突记录永久保留，不被新提案覆盖。

诊断修复和报告候选用例等 Studio 私有来源记录必须同步展示其关联 ChangeSet 的 `proposed / rejected / applied / conflicted` 状态，不能在 ChangeSet 已拒绝后继续显示“待确认”。ChangeSet 是提案状态的事实源；专用拒绝入口同时更新来源记录，列表读取还要按 ChangeSet 重新校准，以覆盖通用拒绝入口或旧数据。拒绝只终止本次提案，不删除诊断证据或报告来源；用户选择继续时创建新的修复记录或候选记录及新的 ChangeSet，旧记录保留审计链。

每次确认应用后，工具为该 Skill Project 生成完整不可变 Snapshot 和带父版本关系的 ProjectRevision。Workspace 的“版本与基线”面板展示当前 revision、当前 snapshot、已阅基线、相对基线的文件变化和项目历史。用户只能把自己实际看到的当前 `revisionId + snapshotId` 设为已阅；若内容已变化则必须重新查看。此操作只移动人工比较基线，不修改 Skill、不创建 revision，也不代表批准任何尚未确认的 ChangeSet。

“撤销最近提交”不是移动 active revision 指针。工具读取当前 revision 的直接父 Snapshot，生成覆盖完整项目文件集合的 `project.restore` ChangeSet，并展示恢复后哪些文件新增、修改或删除。用户拒绝或返回历史时项目保持不变；确认时若 live 工作树、当前 Snapshot 或目标 Snapshot 与预览不一致则冲突终止。恢复成功后创建新的 `source: undo` revision，其 parent 仍是撤销前 revision，因此历史不可变且再次撤销可以恢复刚才的版本。`.git` 和系统元数据不进入 Snapshot，也不会被撤销过程删除。

### 5.4 RuntimeArtifact

每次运行、调试或 benchmark 开始时，从明确选中的 Skill Project active revision 生成不可变 RuntimeArtifact。运行中切换 Skill 或编辑任何项目都不影响当前任务。1.0 不生成多 Skill 联合 Artifact。

RuntimeArtifact 同时冻结启动时的初始变量，并记录项目内容 hash、输入 hash 和二者合成的运行指纹。指纹用于回答“这次运行究竟使用了哪份 Skill 和哪份输入”，不使用显示名称、当前选中项或运行时间参与计算；同一内容和同一 JSON 输入得到相同指纹，但每次运行仍有独立 runId 与 artifactId。测试页提供“运行事实”查看入口，展示冻结输入、当前运行输出和最近模型结构化回执。

active revision 在运行创建后发生变化时，旧运行继续读取旧 Artifact，页面必须明确提示使用新版本需要新建运行，不能静默热更新或自动重启。用户选择“新建当前版本运行”时可以沿用旧运行输入，但会生成新的 run、Artifact 和内容绑定指纹。

### 5.5 ExecutionTrace

ExecutionTrace 是可观测过程的事实记录，不是 Bug Report 本身。它绑定 workspaceId、skillId、projectId 和 RuntimeArtifact，可以被实时消费或事后回放，也可被投影为 benchmark 报告和脱敏 Bug Report。

首个有向图版本已经支持独立追加式 Trace 存储、按序号补拉、断线重连、图上实时状态、步进回放、速度控制和两次运行对比。页面实时展示与刷新后的展示读取同一份 Trace 事实；同时运行或切换多个 Skill 不改变任何运行归属。浏览器离线期间产生的事件在恢复连接后按最后已接收序号补齐，不要求用户手工刷新。

所有当前事件类型由统一注册表声明 domain、敏感度和可分发结构字段。运行对话与真实 Benchmark 在使用条件出口、节点文档和声明式项目事实时记录 `condition.evaluated`、`document.context` 与 `context.queried`，但不把原始变量、查询结果或文档正文复制进观察事件。未知事件保留信封以支持前向兼容，默认报告只保留已注册结构字段。

Trace 分析页支持切换实时与只读回放，通过前后步进、时间轴和播放速度查看任意历史事件状态；回放不会暂停、修复或重新执行原运行。用户可选择同一 Skill 的另一条运行，对比共同路径前缀、首个稳定节点 ID 偏差、revision、artifact、终态、变量和事件类型计数。变量和事件差异只展示持久化事实，不解释根因。旧运行节点不在当前运行图中时保留稳定 ID 并显示缺失占位，不按相似名称匹配。路径对比用于定位差异，不代表问题已经修复或验证通过。

### 5.6 Diagnosis

Diagnosis 不把“首个失败点”等同于根因。它要区分：

- 用户 skill 图设计问题；
- 文档或上下文不足；
- 模型偏离或结构化输出不合规；
- 通用包/外部 Agent 接入问题；
- 沙箱、工具或外部环境失败；
- Skill Designer 自身或内置 skill 缺陷；
- 证据不足。

每个诊断结论包含证据、推断、置信度、其他可能解释和建议验证方式。

建议验证方式使用结构化方法、步骤和成功证据表达。可观测规则把条件拒绝、文档上下文不可用、模型调用/协议错误、工具失败以及沙箱/Provider 环境失败分开归类；同一失败域的多条事件合并为一个候选但保留每条 seq 证据。条件候选要求被拒目标对应的已记录条件结果为 false，文档候选要求运行时读取状态为 missing 或 ambiguous；不能仅根据节点、边或文件静态字段推测根因。

首个实现从导入的 Bug Report 显式触发分析并保存 DiagnosisRecord。对于 `engine.reject`，工具只把“提交目标不在当前合法出口”列为高置信近因；若目标 nodeId 不在报告绑定图中，再列出“图中缺少目标”这一中等置信候选。它不会仅凭拒绝事件断言是模型走丢、用户操作错误或 Skill 设计错误。缺少 conversation、tool 或外部 Agent 观测时，页面必须单独展示证据边界；没有已定义症状时显示“证据不足”。重新分析生成新记录，不修改 Skill，也不产生“已验证”状态。

### 5.7 文档绑定与精确切片

节点使用 `doc` 关联项目内 Markdown，使用 `docAnchor` 选择完整标题路径（如 `Guide/macOS/Retry`）或显式 `#anchor`。界面必须展示实际将被读取的片段和行号；同名标题不得按出现顺序静默选择。文件树同时显示引用计数，便于用户判断修改影响。

图 ChangeSet 只有在文档和标题路径均存在且唯一时才能进入确认。编辑已被节点引用的文档时，服务器使用修改后的内容重新验证引用；删除或改名导致绑定失效时阻止提案并说明受影响节点，不自动替用户改写引用。确认期间图或文档发生变化时，原 ChangeSet 进入冲突状态，用户重新审阅后再提交。

### 5.8 Git 版本比较

ChangeSet diff 用于回答“这次提案将改什么”，仓库 Git diff 用于回答“当前 Skill 相对某个 Git 基准改了什么”，两者不得混为一谈。

- 当前 Skill Project 位于 Git 仓库时显示“Git 对比”按钮；Workspace 中不同 Skill 的 Git 状态分别计算；
- 默认比较当前工作树与 `HEAD`，允许选择已有 commit/tag；
- 只读取仓库差异，不替用户提交、切换分支或修改 Git 状态；
- Studio 自身的 Trace、快照和运行产物存放在项目目录之外，不污染仓库 diff。

当前实现默认比较 `HEAD`，也可从最多 50 个近期 commit 和 100 个 tag 中选择基准。界面传递的是服务端枚举并解析后的 commit OID，不把用户文本作为 Git 参数；相对旧基准时，文件列表同时包含旧基准到当前工作树之间已经提交和尚未提交的变化。已跟踪文本显示 patch，已跟踪和未跟踪二进制文件显示基准/当前字节大小摘要，最多 200 项并明确标记截断。比较始终限定到已授权 Skill 根目录；同一仓库其他目录、Studio 私有数据和 `.git` 均不会进入文件列表、patch 或摘要。页面固定说明这是只读仓库事实，不是待确认 ChangeSet。

---

## 6. 核心用户流程

### 6.1 创建与导入

```text
创建或选择 Workspace
-> 新建 Skill / 导入本机文件夹 / 原地打开 Git 项目
-> 检测本产品格式或外部 skill
-> 静态解析或 LLM 解析
-> 生成 draft + 导入候选（pre-project ChangeSet）
-> 用户审阅、修改、重新解析
-> 确认后 active
-> Workspace 成员转为 ready
```

每个新增 Skill 在确认前都显示目标 Workspace、稳定 skillId 和目录模式。“导入”默认将源 skill 复制到 Studio 管理项目目录，不原地改写用户源目录；用户也可明确选择“原地打开 Git 项目”，确认后的 ChangeSet 才写回该仓库。导入时未识别的文件必须保留；脚本默认不执行。

当前静态导入入口使用浏览器目录选择器，单次最多 500 个文件、单文件 2 MiB、总计 16 MiB；`.git` 元数据和系统 `.DS_Store` 不作为 Skill 资产导入。预检会先登记 `pending-import` 成员并显示原文件、生成文件和诊断，关闭或取消预检会移除该成员。原生 `skill.json + graph/main.json` 作为高置信候选；合法 manifest 明确声明 `content-only` 时优先于 Markdown 流程推断。外部 Markdown 只有在 `Workflow / Steps / 工作流 / 流程 / 执行流程 / 操作步骤 / 步骤` 明确章节下提取到至少两个有序子标题或编号/清单步骤时，才生成有向流程候选。证据不足时保守归为 `content-only` 单知识节点，不从普通段落臆造流程。已有保留路径文件无效时阻断确认，不静默覆盖。

格式预检区分原生 Skill Designer、带 frontmatter 的外部 Skill 和普通 Markdown Skill；文件清单至少区分 Markdown、JSON、YAML/TOML 配置、普通文本、脚本、可识别资产和未知文件。YAML、TOML、JSON frontmatter 均通过结构化解析器读取，统一提取名称、说明、版本、许可证、兼容性和 allowed-tools；未识别的顶层键及嵌套值仍保存在候选事实和原始 `SKILL.md` 中，不因生成 `skill.json` 而丢失或改写。

引用扫描覆盖所有导入 Markdown 的链接、图片、引用定义、看起来是项目路径的行内代码，以及 frontmatter 的 references/docs/files/includes/resources 字段。每项引用记录源文件、行号、原始目标、归一化目标和 `已解析 / 缺失文件 / 缺失锚点 / 外部 / 无效 / 越界` 状态。扫描只在上传资产清单和 Markdown 标题索引内解析，不访问外部 URL、不跟随文件系统符号链接，也不读取导入目录之外；缺失和越界作为明确警告交给用户处理，不自动创建文件或改写引用。

导入候选必须为格式、显示名称、说明、能力类型和图结构保存 provenance，包含来源路径、行区间、判断方法与置信度。由于确认前还不存在 active ProjectRevision，导入候选是带 status、digest、事实清单和预览的 pre-project ChangeSet，而不是伪造 baseRevision 的普通 `ProjectChangeSet`；确认成功后才创建初始 revision，之后的修改统一进入标准 ChangeSet。

解析预检必须同时展示候选节点和关系、逐项置信度、来源文件与 1-based 行号、原文片段、未决问题和实时图校验。候选图直接复用图谱工作台的 3D 立体/2D 平面力导向画布，不另做工作流卡片预览；用户可接受或拒绝每项候选，也可修改节点标题/类型和关系端点/类型。保存审阅只更新 Studio 私有导入候选与 digest，不创建 active 项目；拒绝造成入口、可达性或端点错误时阻断最终确认。

“重新解析”始终读取导入时冻结的原文件。候选从未人工修改时可直接刷新解析结果；一旦发生人工接受/拒绝或字段修改，新解析结果只保存为 `manual-vs-reparse` 冲突，当前人工版本继续作为有效审阅结果。产品不做自动合并，用户必须显式选择“保留人工修改”或“采用重新解析”；冲突未裁决、审阅未保存或 lint 有阻断错误时均不能确认导入。最终确认才复制原文件、按已裁决候选生成或改写图并把 Workspace 成员从 `pending-import` 转为 `ready`，冻结的 Markdown 原文不因图审阅而被改写。

用户可在静态审阅结果上显式启动“LLM 解析”。模型只能看到冻结文本文件清单、静态候选、已读取正文和读取反馈；需要更多证据时只能请求清单内的 Markdown/JSON/配置/文本文件。一次解析最多 4 次模型调用、8 次按需读取、每轮 4 个文件，单文件最多 20,000 字符、总正文最多 48,000 字符，图 lint 最多回灌修正一次。不变文件可按 SHA-256 跨运行复用带原始行号的私有摘要；摘要不足时模型必须再次请求同一路径，服务端才升级为完整正文。摘要不会放宽证据范围，节点和关系只能引用本轮模型实际看到的原始行，并继续按冻结原文的路径、行号和片段复核。Schema、证据或 lint 校验失败时只保留诊断，不改变候选。界面展示调用次数、token、读取记录、缓存命中/升级、修正次数和失败原因，并允许取消。LLM 结果仍是确认前候选；人工版本存在时必须进入与静态重解析相同的显式冲突裁决。

### 6.2 手工设计

```text
页面修改节点/边/文档
-> 本地服务器校验
-> 展示 diff
-> 用户确认
-> 事务提交
-> baseline 显示上次已阅后的变化
```

直接表单编辑可以使用简化确认，删除、跨文件修改、命令或自动条件必须显示完整 diff。

文档工作台支持按项目内路径即时检索、创建、编辑、安全预览、重命名和删除。`SKILL.md` 是根事实文件，不允许重命名或删除。关联文档重命名时，ChangeSet 同步改写所有节点的 `doc` 路径并保留 `docAnchor`；删除前必须列出受影响节点，确认后在同一 ChangeSet 中删除文档并清除这些节点的 `doc/docAnchor`，不能留下悬空绑定。预览同时展示文档前后内容、目标路径和图引用影响，确认前两个文件都不改变。

Workspace 的当前 Skill 操作区提供项目资产面板，管理 `assets/**` 下的图片、音频、视频、PDF 和其他二进制文件。上传、同路径替换和删除都必须先形成单资产 ChangeSet；预览只展示动作、路径、MIME、字节大小、SHA-256 及 Markdown 引用位置，不把 Base64 二进制复制进 diff。删除资产时引用仅作为影响清单，工具不自动改写文档；用户可返回文档工作台自行修正后再决定是否删除。首版单文件上限 2 MiB，路径必须使用 Windows、macOS、Linux 共同可用的 `assets/` 内 POSIX 相对路径。确认前文件字节不变，确认时若资产或引用事实漂移则进入冲突，确认后才生成新 Snapshot 和 Revision。页面只对 PNG/JPEG/GIF/WebP 做隔离的图片预览，其他类型仍可作为文件管理和导出资产。

图谱工作台采用原始图谱项目的观察语法，而不是流程编排器卡片：同一份 `SkillGraph` 可在“3D 立体”和“2D 平面”之间切换，首次进入默认 3D，之后记忆用户选择。两种模式都使用力导向关系布局、类型色节点、常显或随缩放渐显的标签、关系边、缩放、平移、搜索定位、节点类型与边类型筛选、双击聚焦两跳邻域，以及节点档案中的入边、出边和相邻跳转。3D 使用可旋转相机与节点飞行定位；2D 使用无限平面和拖拽布局。切换模式只替换渲染投影，不改变节点 ID、选择、筛选、草稿、ChangeSet 或运行事实。

1.0 的节点和边行为只来自 Engine 导出的核心类型注册表，图加载、运行、解析、导出和 UI 选择器复用同一事实源，不按名称或固定 ID 猜测行为。节点、边、图和 manifest 中未识别的普通 JSON 字段在加载、编辑、确认和导出时原样往返，但只是被动数据，不会因此获得执行语义；单个节点或边的未知字段与 `extensions` 合计最多 64 KiB。未注册类型继续由 lint 阻止进入 active workflow。项目级类型声明、`graph/_extensions.json` 和元模型属于 2.0，不在 1.0 中半实现。

1.0 图谱页的具体呈现直接沿用原始 `graph-engine-demo/graph-viewer` 风格：进入图谱即切换为沉浸式全窗口页面，隐藏 Studio 侧栏和全局顶栏，只保留返回入口与 46px 紧凑单行工具条；工具条按原版顺序承载搜索、2D/3D、全部/流程面/知识面、主题、变更、关系和适应视图，不增加独立的 Studio 编辑按钮。图谱铺满剩余空间并使用浅灰或深色径向背景；节点类型图例按原版固定类型顺序浮在左上，Lint/形状校验浮在左下，操作提示位于底部；节点或边被选中后才从右侧浮出圆角档案面板，未选中时不常驻占用画布。档案默认采用原版的标题、稳定 ID、类型/平面/连接数标签、关系 chip 和 Markdown 文档层级；编辑字段必须在用户明确点击“编辑节点/编辑关系”后出现。Studio 的新增、批量编辑与 ChangeSet 保存从“变更”浮层进入，不能把默认画布重新变成左右固定分栏的流程编辑器。导入解析等嵌入式候选图谱必须复用相同的 46px 标题/搜索/2D/3D/主题/适应工具条和径向渲染平面，不另造一套悬浮控件。自动适应、节点半径、辉光、标签、边宽、粒子、力参数和类型色以原项目源码为基准；2D 适应在正常图上保留原版尺度，只对超过 3.2x 的过度放大设置统一上限，避免稀疏知识图把一两个节点放大成占满画布的圆块。模式切换必须清除瞬时悬停态，不能让新模式中的非相邻节点被错误压暗。

“流程/知识”是图数据的语义平面，不是 2D/3D 模式。流程、条件、回流和继续边保留方向箭头，只有这些可执行边参与 Runtime；知识关系边用关系线和类型图例展示，不作为合法下一节点。内容型 Skill 可以只有知识/文档节点而没有伪造入口和流程边。有向工作流只是通用图谱中的一种关系子图，不能反过来把整个图谱页面设计成工作流编排器。

图编辑保留节点/边创建、修改、删除和批量操作，但从“变更”浮层或节点档案打开结构化表单，新增边通过明确选择起点和终点完成，不依赖流程卡片的 source/target handle。条件边必须填写封闭的结构化 Condition AST 并通过安全 DSL 校验；每种操作符和操作数只接受协议声明字段，任意代码、未知操作符、隐藏附加字段、不安全变量路径、非有限数字或超限数组都不能保存。持续 Lint 的每条问题都展示准确 path 并可定位到节点、边或字段；只有结果确定且不会自动提交的修复才能提供快捷入口，首批仅支持把自环边从当前草稿移除。上述修改仍统一进入 Graph ChangeSet，用户确认前项目文件不变。

节点数超过 200 时界面明确显示大图模式，2D/3D 均关闭粒子和持续动画、降低标签密度并保留聚焦搜索；设备不支持 WebGL 时 3D 明确降级到 2D，不显示空白画布。500 节点必须分别验证 2D 和 3D 的非空渲染、搜索、筛选、聚焦和模式切换，不能只用 DOM 节点数量代替 Canvas/WebGL 像素检查。

### 6.3 LLM 设计助手

```text
自然语言需求
-> 助手查询当前 schema/图/文档
-> 生成结构化 ChangeSet
-> 服务器预演 + lint + diff
-> 用户确认
-> 事务提交
```

助手不获得任意文件写入或任意 shell 权限。

助手可以读取 Workspace 成员摘要用于帮助用户选择 Skill，但一次会话必须锁定一个 projectId/skillId，只能为该 Skill 生成 ChangeSet。切换界面当前 Skill 不会隐式改变既有会话目标。

首个纵向版本把设计助手作为跨页面右侧工具抽屉，不增加主导航。用户明确点击后为当前就绪 Skill 创建会话；抽屉持续显示锁定目标、Provider 状态、模型 token、项目证据和待确认 ChangeSet。切换 Workspace 当前 Skill 时旧会话保持原目标并显示警告，只有用户点击“为当前 Skill 新建会话”才改变目标。

会话按 Workspace 私有持久化，抽屉打开或页面刷新后自动恢复当前 Skill 最近会话，也可从摘要列表切换历史会话或显式新建。历史列表只加载目标、时间、消息数和末条摘要，选择后才读取完整消息，不能跨 Workspace 展示。生成期间发送按钮切换为“停止生成”；取消信号直接传给 Provider，取消结果作为一轮明确消息保存，不生成 ChangeSet，也不记录虚假 token。

服务首轮只向模型提供成员摘要、当前 Skill 的 active revision、受限图上下文、文档/测试用例索引，以及由用户需求明确命中的少量正文；不扫描脚本、资产或其他成员文件。缺少项目事实时模型可返回 `read`，由服务执行绑定当前 projectId 的节点、邻域、图搜索、文档/切片或测试用例读取，再继续同一轮推理。整个请求最多 3 次模型调用、10 次只读操作，每轮最多 5 次；索引外目标被拒绝，项目版本变化立即中止，正文不写入会话历史。工具结果和项目内容都按不可信输入处理，不能要求扩大权限。

最终模型只能返回 `clarify` 或 `propose`：用户目标仍有歧义时必须追问且操作为空；提案必须引用当前上下文中存在的稳定 ref，并以 JSON 字符串承载白名单操作 value。服务端重新解析 evidence 和操作，再调用统一 ChangeSet 预演、路径边界和 lint。页面展示读取记录、聚合 token 和 diff 后由用户确认；模型回复、读取、生成提案和预览本身都不能修改项目。

### 6.4 测试用例编写与调试

测试用例可以由用户手工编写、用户的外部 Agent 编写、Studio 设计助手生成，或由 Trace/Bug Report 转换为候选用例。

```text
draft -> ready -> running -> needs-review
                              -> passed
                              -> failed
                              -> inconclusive
```

- 工具自动提供路径、终态、文件 diff、工具结果和禁止副作用等证据；
- 用户最终确认 `passed / failed / inconclusive`；
- 缺少预设用户回答时标记 `fixture-incomplete`，不得自动算作通过；
- 测试用例本身的修改也走 ChangeSet 和确认。

首批实现把“用例编写”和“手动运行”放在测试工作台的两个独立模式中。用例以 `benchmarks/cases/<caseId>.json` 保存，支持 draft/ready、初始变量、确认节点回答脚本、默认 `subsequence`/可选 `exact` 路径、终态、变量、产物、工具结果、禁止副作用、标签和备注。路径节点必须存在，`exact` 相邻节点必须有直接流程边；创建、修改和删除均先生成 ChangeSet。该页面只证明用例已通过静态校验，不显示“Benchmark 成功”。

用户可以从同一 Skill 的 `completed/stopped` 手动运行直接生成用例候选。工具读取该运行的冻结输入、Trace 节点路径、终态和最终变量，形成可编辑 draft；这些字段只是“观察到的事实”，不是系统认定的正确期望。页面必须持续提示用户审阅，候选只有经过普通用例编辑器的 lint、ChangeSet 预览和确认后才进入项目。来源 runId、artifactId、workspaceId 和 projectId 只存在于 Studio 私有候选响应，不写入 BenchmarkCase 或通用导出包。用例列表支持按标题、caseId 和标签搜索，并在百用例规模下使用独立滚动区。

人工结果判定属于一次真实 Benchmark 运行，而不是用例输入。每次判定以追加 review 保存 `passed / failed / inconclusive + note + createdAt`，保留历史；修改用例不能伪造、覆盖或预设运行判定。

“手动运行”中的模型调试面板供尚未把 Skill 接入外部 Agent 的用户使用。每个对话固定绑定一个 run 和不可变 RuntimeArtifact，只向模型提供当前节点、引擎计算的合法出口、skill 变量、当前节点在冻结 revision 中绑定的文档切片、该节点声明且有总字符上限的项目事实查询结果及本 run 最近 12 条对话。查询结果在页面逐项展示命中、空结果、显式降级、歧义或缺失。模型每轮只能结构化返回“回复并等待 / 提交下一节点 / 停止运行”，不能修改项目、生成 ChangeSet、执行工具或命令。提交下一节点仍由确定性引擎校验；非法目标会留下 `engine.reject`，当前节点和步骤不变，界面列出合法出口，不自动重试或改走其他路线。

运行命令响应直接返回 `accepted`、本次新增事件序号及可选 `rejection(code/message/requestedNodeId)`；同一拒绝事实同时写入 Trace 的 `engine.reject`，其中保留合法出口 ID。页面以持久化 Trace 为恢复后的事实源，显示稳定错误码、提交目标和合法出口，不把 HTTP 成功误写成“跳转已接受”。Studio 与通用导出 CLI 对条件使用完全相同的 `skill.* / runtime.*` 命名空间；导出后不能因变量根对象不同而改变分支。

手动运行页同时提供运行列表、当前节点、合法出口、启动/暂停/继续/停止、Trace 实时视图与输入输出检查。Workspace 内切换当前 Skill 只切换页面查询作用域，不转移或复制后台运行；返回原 Skill 后仍按 projectId 读取原运行。项目版本漂移不影响正在查看或执行的旧运行，只有用户显式新建运行才使用新的 active revision。

用户消息、模型请求元数据、结构化回执、引擎决定和最终可见回复按顺序写入同一 ExecutionTrace；API Key、原始请求和私有思维链不进入记录。模型调用固定零自动重试，用户可取消当前生成；取消与错误都形成明确消息且不改变运行。页面提供显式“重试”，它会建立一轮新的模型请求和 Trace，保留旧失败事实。模型生成期间若人工推进或其他事件改变 run，eventSeq/currentNode 乐观锁会拒绝陈旧决策。

### 6.5 Trace 与 Bug Report

```text
运行中持续追加 Trace
-> 页面可选实时亮灯
-> 结束/失败/取消后封存
-> 事后回放
-> 导出脱敏 Bug Report（项目名 + 时间戳）
```

Bug Report 只承诺覆盖 Skill Designer 引擎、对话框和工具实际观测到的过程，不承诺记录外部 Agent 绕过引擎的行为。

已结束运行可生成版本化 Bug Report。用户先选择默认、严格或关闭可选脱敏，填写现象说明，再同时检查不下载的本地原始投影与实际导出预览；确认后才从同一份冻结、已脱敏内容生成 JSON 和 Markdown。Markdown 包含便于阅读的摘要和完整脱敏 JSON，不会重新读取原始 Trace。即使选择关闭可选脱敏，密钥、令牌、密码和授权字段仍不可导出。报告中的拒绝和停止属于事实症状，不自动转换为根因、修复建议或“已验证”结论。

报告弹窗按当前 Workspace 和 Skill Project 展示历史记录，允许重新选择、下载两种格式或删除源报告。删除源报告不级联删除已经加入诊断的独立导入副本，也不影响来源运行、Trace、RuntimeArtifact 或 Skill 项目。1.0 不接受任意附件；附件只有在后续定义明确类型、数量和大小上限、路径边界以及逐类型脱敏规则后才能进入报告协议，不能以未检查文件绕过脱敏预览。

用户可通过文件多选或拖放把 JSON 报告批量导入 Workspace 的诊断页；单份与单批均受大小和数量限制，每份独立校验，部分失败不会撤销已经成功的导入。相同 reportId 和相同内容幂等复用已有副本，相同 reportId 对应不同内容时作为身份冲突拒绝。工具只按 skillId 精确查找成员，再比较内容指纹；skillId 缺失、目标不可用或指纹不同均显示不同警告，并使用报告自带图复现。导入后默认停在首个事实症状，用户可逐症状跳转或沿时间轴查看节点和事件；不存在的节点保留原 ID 占位，不映射到同名或相似名称节点。新生成报告显示完整 RuntimeArtifact 指纹，Benchmark 报告同时显示模型与沙箱执行指纹；早期 1.0 报告没有该字段时明确显示“未记录”，不伪造环境事实。

用户删除导入副本时，同时清理绑定该副本的诊断、修复记录、确定性夹具和候选用例草稿。删除不反向撤销已经确认应用的 ChangeSet，也不影响来源运行、Trace、RuntimeArtifact、源报告或 Skill 文件。

导入报告可以复用为两种明确分离的资产。第一种是 Studio 私有的确定性引擎夹具：按报告自带图重放命令并逐项比较事件签名，只用于验证工具引擎，不调用模型、工具或沙箱，也不计入 Skill Benchmark。第二种是带 reportImportId、reportId 和 sourceRunId 来源链的 draft BenchmarkCase 候选：工具提取实际节点路径，用户补充业务意图、期望路径和终态，先检查 ChangeSet 差异，再明确确认、拒绝或处理冲突。拒绝或冲突后可从原报告重新生成候选，但不能复用旧 ChangeSet；生成候选不等于加入项目，加入项目也不等于 Benchmark 通过，真实结果仍由后续模型与沙箱运行决定。

### 6.6 诊断与辅助修复

```text
detected
-> diagnosed
-> proposal-created
-> user-approved
-> applied
-> verified
```

- 工具显示症状与候选原因，不把推断写成事实；
- 用户必须主动点击分析；结果按报告保存，刷新后仍可查看，重新分析不会覆盖历史事实报告；
- 修复方案以 ChangeSet 形式提交；
- 用户可以接受、拒绝、手工修改或用自己的方案处理；
- 回放只能帮助定位原因，不能证明修复成功；修复后必须创建新 RuntimeArtifact 并在原问题对应的运行方式中重新执行。
- 运行验证要记录级别：`runtime` 或 `benchmark`。只有原问题属于 Benchmark 时，才必须由新的真实 Benchmark 标记为已验证。

首个辅助修复只处理一类证据充分的情况：被拒绝目标是报告图中已有节点，但当前节点没有通向它的直接边。工具可以据此生成“新增流程边/回退边”的 ChangeSet；目标节点缺失、报告指纹已过期或候选只有文本建议时，不提供一键提案。用户必须在图 diff 中明确确认，应用后状态仍为“未验证”。普通 Runtime 报告要求用户创建修复版本的新运行，实际经过新增边并到达完成终态后再选择该 run 验证；旧 revision、未经过修复边或只做报告回放均不能标记成功。

修复记录分别维护“提案状态”和“验证状态”：提案可以是 `proposed / rejected / applied / conflicted`，验证仍是 `unverified / verified / failed`。拒绝提案不是验证失败，因为项目并未修改、也没有产生可验证的新版本；页面应保留诊断与证据，明确显示“提案已拒绝”，并提供生成新修复提案的入口。只有提案成功应用后，运行验证才可以改变验证状态。

Benchmark 来源报告不允许用户从普通运行记录中随意选择一个结果冒充验证。诊断页从已应用修复发起专用 `post-repair` 运行，记录来源 Benchmark、repairId、changeSetId 和 appliedRevision。启动前活动版本必须仍是 appliedRevision；一旦运行已冻结 Artifact，后续验证不受项目之后的编辑影响。判定要求父子运行属于同一 Workspace、Skill 和 case，父子 Artifact 均真实存在且不同，子运行使用 appliedRevision 并技术完成；自动断言和最新人工判定都必须是明确的 passed 或 failed。两者均通过才是 verified，任一失败即为 failed，blocked、技术失败或 inconclusive 都不能成为修复结论。

### 6.7 通用导出

```text
active revision
-> lint 通过
-> 用户检查导出内容
-> 生成通用包
-> 在外部 Agent 中由用户按对方方式安装/引用
```

1.0 每次只导出一个 Skill Project，不提供 Workspace 聚合导出。

`generic/1` 导出从用户实际预览的不可变 Snapshot 读取文件，确认时若 active revision 或 snapshot 已变化则返回冲突。ZIP 包附带 `export-manifest.json`、`engine/README.md` 和零 npm 依赖的 `engine/skill-engine.mjs`。CLI 提供结构检查、条件感知的出边查询，以及由外置状态文件承载的开始、查看、推进、暂停、恢复和停止；它只运行通用图状态机，不执行包内业务脚本或网络调用。

Studio 保存当前 Skill 的导出历史，区分待确认、可下载和已冲突记录。用户可以重新下载已生成 ZIP，也可以显式删除某条导出记录；清理只删除 Studio 私有导出记录、生成目录和 ZIP，不删除 Skill 文件、Revision 或 Snapshot。导出历史不进入通用包。

---

## 7. 功能模块

| 模块 | 1.0 范围 |
|---|---|
| M1 Workspace 与 Skill Project | 多 Skill 清单、摘要状态、当前 Skill 切换、添加/移除引用、复制导入/原地打开、稳定 skillId |
| M2 通用图谱编辑器 | 同图 3D 立体/2D 平面视图，流程与知识关系分层，节点/边 CRUD，schema 动态表单，lint，搜索定位、关系筛选、两跳聚焦和大图模式 |
| M3 文档与资产管理 | 文档树、编辑、引用计数、精确标题路径切片、节点绑定、资产上传/替换/删除与引用影响 |
| M4 ChangeSet、基线与 Git 对比 | 预演、diff、确认即原子应用、撤销、snapshot/baseline、只读仓库 diff |
| M5 Skill 导入与解析 | 格式检测、静态解析、LLM 解析、重载、人工确认、未识别资产保留 |
| M6 LLM 设计助手 | 查询项目、生成 ChangeSet，不直接写文件 |
| M7 测试用例 | 手工/外部 Agent/助手编写，schema 校验，人工判定 |
| M8 ExecutionTrace | 追加写、实时消费、断线续传、回放和运行时指纹 |
| M9 Bug Report | 生成、脱敏预览、导出、导入、回放和候选用例转换 |
| M10 Diagnosis | 证据关联、候选原因、置信度、修复建议和 ChangeSet |
| M11 通用导出 | 通用目录、零 npm 引擎、资产保留、导出预览 |
| M12 真实沙箱 Benchmark | 1.0 最后实现；真实模型、真实工具、隔离工作区、产物断言和人工判定 |

1.0 的图谱工作台必须同时提供原始项目语义一致的 3D 立体与 2D 平面视图；3D 不是装饰预览，也不是可延后的营销效果。运行、回放和诊断复用同一图谱状态适配层；在狭窄移动端默认 2D 以保证操作，但用户仍可显式切换 3D。

---

## 8. Benchmark 与测试分层

| 层级 | 真实模型 | 真实沙箱 | 是否称为 Skill Benchmark |
|---|---:|---:|---:|
| 引擎/Trace/渲染单元测试 | 否 | 否 | 否 |
| 协议与 Provider 集成测试 | 可选 | 否 | 否 |
| Trace Replay | 否 | 否 | 否 |
| Skill Benchmark | 是 | 是 | 是 |

Skill Benchmark 必然消耗 token。Skill Designer 不设置金额红线，但要从工具层减少无效消耗：

- 按需读取文档；
- 缓存不变内容；
- 增量上下文；
- 记录每次模型调用的 token、缓存、重试原因和停止原因；
- 提供取消、技术性最大步数和断点保存，防止无限循环。

Benchmark 结果必须冻结并记录：精确模型、参数、提示模板、LLM Provider、通用运行协议、沙箱策略和 RuntimeArtifact。单次通过不等于稳定性结论。

1.0 的 Provider 接口保持厂商无关，首个落地适配器使用 OpenAI Responses API。模型设置统一放在项目目录之外；API Key 可由服务端环境变量提供，也可从设置页写入系统凭据库，但任何读取接口都只返回“是否配置”和来源，不回传明文、后缀或可识别掩码。运行页面显示 Provider、请求模型和 API 实际返回模型。每次模型调用要求严格结构化节点决策并记录真实 usage；测试替身只能验证协议，不能进入用户 Benchmark 统计。模型只能选择引擎列出的合法下一节点或停止，不能直接构造工具命令。

设置页允许修改模型 ID 和请求超时、测试连通性、替换或删除本地密钥。连通性检查查询模型元数据，不发起生成、不产生模型 token，并可对网络或服务端暂时故障受控重试一次；真实模型生成不自动重试，避免重复消费。环境变量存在时优先级最高，页面明确显示覆盖状态且不能读取或修改环境变量值。

真实 Benchmark 采用单并发队列。单用例可直接入队；批量运行必须由用户显式勾选 ready 用例，服务先校验整批再按选择顺序建立独立运行记录，不把批量结果合并成一个不可追踪的总状态。每次启动都先检查 ready 用例、Provider、固定 runner 镜像和沙箱自检；条件不足时仍保存 blocked 记录供诊断，但不得调用模型、消耗 token、冻结 Artifact 或显示自动通过。条件满足后才冻结当前 revision 的 case、图和按节点引用的文档，容器先读取该 snapshot，随后宿主 Provider 按节点决策；路径、终态、变量、产物、工具结果和禁止副作用分别断言，技术状态与自动断言结论分开保存。

自动断言和人工判定是两个独立事实。人工结论不能覆盖、重写或伪装自动结果；每次 `passed / failed / inconclusive` 复核连同备注与时间只追加保存，后续复核保留旧记录。只有技术执行完成的运行可人工判定，条件阻断或技术失败必须先解决执行条件，不能手工改成通过。

失败或待定的 Benchmark 可以生成与普通运行兼容的 Bug Report，但必须保留 `source.kind=benchmark`、真实 benchmarkRunId、caseId、RuntimeArtifact、revision 和 RuntimeFingerprint。报告使用通用 Trace 事件信封承载模型、沙箱、工具、引擎和断言事件；自动断言失败、证据不足和技术失败分别形成事实症状。用户检查脱敏预览并确认 digest 后，可以下载 JSON，或把同一报告直接加入当前 Workspace 的诊断列表。preflight 阶段阻断且未冻结 Artifact 的记录不能生成 Skill 问题报告。

用户可以从一条已结束记录按同一 case、模型和 reasoning 配置发起重跑；新记录必须显式保存父 benchmarkRunId，而不是依赖创建时间推断。对比至少展示技术状态、自动断言、Artifact 和 token。普通 `rerun` 只表示运行关系，不表示发生过修复；只有从已应用 DiagnosisRepair 发起的新运行才可标记为后续 `post-repair` 关系，并在真实执行后参与修复验证。

`post-repair` 运行不是普通重跑的显示别名。它必须同时绑定 repairId、changeSetId、appliedRevision 和报告来源 parentBenchmarkRunId。启动阶段拒绝当前版本漂移；验证阶段重新读取父子 RuntimeArtifact，校验报告来源、指纹、修复版本和独立 Artifact 身份，并要求 completed 技术状态、自动断言和最新人工判定均给出明确结论。项目在运行冻结后继续编辑不会抹掉这份历史证据，也不会让新编辑版本冒充已验证版本。

1.0 的 Windows/macOS 客户端统一使用 Docker Desktop `desktop-linux` Linux VM 作为真实执行后端。默认策略为固定 digest runner 镜像、根文件系统只读、snapshot 输入只读、产物目录独立可写、宿主其他路径拒绝、网络完全禁用、丢弃 capabilities、禁止提权、非 root 用户以及 PID/内存/CPU/超时限制。模型 API 由宿主 Provider 调用，不把模型凭据放入容器。macOS `sandbox-exec`、Windows Sandbox 和裸宿主进程都不作为自动降级路径；Docker 不存在、context 指向远程、不是 Linux containers 模式或生命周期自测未通过时，页面必须显示不可用且禁止启动 Benchmark。

沙箱可用性由固定协议的真实隔离自检决定，而不是由“检测到 Docker”决定。自检覆盖只读输入、无网络、独立产物、源 snapshot 不变、长任务取消、容器无残留和临时副本清理；结果及 handle 作为本地审计记录持久化。当前开发机缺少 Docker CLI 时，界面只允许查看能力与运行自检并明确返回不可用，不提供裸宿主执行或伪造通过结果。

期望路径默认使用 `subsequence` 匹配，同时始终展示多走、漏走和换序差异；用户可切换为 `exact`。路径只是一类诊断证据，不能取代终态、产物和禁止副作用断言。

---

## 9. 基础安全和隐私

完整沙箱在 1.0 最后阶段实现，但以下基础边界随本地服务器一起实现，不得后置：

1. 服务器只监听 loopback，启动时生成随机访问令牌并校验 Origin。
2. Workspace 中每个 Skill 根目录都要单独授权并映射为不透明 projectId；加入 Workspace 不等于授权共同父目录。
3. 所有路径在解析真实路径后必须仍位于当前 Skill Project 根内，防止 `..`、符号链接逃逸和跨成员访问。原地项目不能只在加入时检查：每次读取文档、图、用例或生成变更预览前都要从已授权根逐级检查现存路径组件，任何符号链接替换都应拒绝并在页面明确报错；外部工具对普通文件的修改仍由 revision、content hash 和 Snapshot 一致性发现，不能静默混入已冻结运行。
4. LLM 设计助手只能生成白名单内的结构化操作。
5. 导入脚本只保留、展示和审阅，沙箱完成前不执行。
6. API Key 存在项目和导出目录之外，前端不读回明文，Trace/日志/报告不记录。
7. 外来 Markdown 禁止原始 HTML 或经严格清洗后渲染。
8. Bug Report 按字段敏感级别导出，导出前展示实际内容和脱敏预览。

---

## 10. 非功能需求

- **本地优先**：除模型 API 和用户明确允许的沙箱网络访问外，全部在本地运行。
- **单人使用**：1.0 不实现团队权限；仍需防止同一用户的多窗口过期覆盖。
- **规模目标**：单个 Skill 500 节点、100 篇文档；500 节点仍可搜索定位、筛选、查看关系、缩放和平移，Workspace 概览只加载成员摘要，不合并加载全部图。
- **平台**：Studio 正式验收 Windows/macOS；核心引擎和通用导出物的文本、路径和 hash 在 Windows/macOS/Linux 上保持一致。
- **可访问性**：状态使用颜色 + 形状/线型双重编码，动画尊重系统“减少动效”设置。
- **可取消**：解析、重载、诊断、导出和 benchmark 等长任务必须可查询状态和取消。
- **数据生命周期**：Trace、运行记录、备份和诊断结果可配置保留数量，提供手动清理。

---

## 11. 1.0 验收标准

1. 将用户提供的 3-5 个真实 skill 加入同一 Workspace，覆盖正常流程、多文档、知识节点和带脚本但默认不执行的场景。
2. 完整走通：导入 -> 修改 -> ChangeSet 确认 -> 测试 -> Trace -> 诊断 -> 修复 -> 导出。
3. 对未提取到流程的 skill 不臆造流程，界面明确报告能力边界。
4. Windows 和 macOS 完成 Studio 端到端验收。
5. 至少一个通用导出包在外部 Agent 中由用户手工接入并成功运行。
6. 所有 LLM 修改不得绕过 ChangeSet 和用户确认。
7. Trace 在运行中追加并可从中断处续传；Bug Report 可脱敏导出并导入回放。
8. 修复建议必须引用证据，修改后的验证状态不得误导用户。
9. 真实沙箱 Benchmark 作为 1.0 最后验收阶段，验证真实模型、工具、文件产物和人工判定闭环。
10. Workspace 能同时展示所有验收 Skill 的摘要状态并切换；切换、重命名或移动目录不会混淆 skillId、Trace、报告和 Git 状态。

---

## 12. 实施优先级

```text
P0  Workspace、Skill 身份、统一协议和基础模型
P1  多 Skill 管理、节点、边、文档、页面图和导入
P2  ChangeSet、设计助手、解析确认和通用导出
P3  ExecutionTrace、Bug Report、回放、Diagnosis 和辅助修复
P4  真实沙箱 Skill Benchmark
P5  Windows/macOS 及外部 Agent 端到端验收与发布
```

---

## 13. 已确认决策

1. `graph-engine-demo` 只学习思想和已验证机制，不迁移业务内容。
2. 1.0 是不绑定业务域的有向流程图版本；多层元模型放到 2.0。
3. 产品不承诺强制外部 Agent 按图运行，只记录工具可观测的事实。
4. 用户 skill 不做未经确认的自动修复；工具必须能分析、建议并经确认后辅助修复。
5. 节点行为按类型决定，节点 ID 只是实例标识，引擎不写死节点名称。
6. 1.0 不实现运行时自动改图、自动重新接回流程或自动修复。
7. 解析、重载和修复结果必须人工确认后生效。
8. Studio 内所有写入归本地服务器；导出包在对方环境中由零依赖引擎记录运行 Trace。
9. 基础安全随服务器和工作区实现；完整沙箱放在 1.0 最后。
10. Skill Benchmark 必须使用真实模型和真实沙箱，必然消耗 token；内部确定性测试不得冒充 benchmark。
11. 测试用例可由用户、外部 Agent 或 Studio 助手编写，最终成功与否由用户根据证据确认。
12. 1.0 只导出通用包，Claude/Codex 等专用适配放到 2.0。
13. Studio 1.0 面向中文用户，正式支持 Windows 和 macOS，以单人使用和 500 节点/100 文档为规模目标。
14. 采用原 demo 的 snapshot/baseline 人工已阅思路；完整数据格式迁移系统不进入 1.0。
15. Skill 版本差异读取其 Git 仓库 diff，并在界面“Git 对比”中展示；Studio 不自动执行 Git 写操作。
16. Workspace 是产品根对象，可同时管理和查看多个 Skill；1.0 的 ChangeSet、revision、运行、报告、Benchmark 和导出仍以单个 Skill Project 为原子边界。
17. 每个 Skill 使用不依赖名称和路径的稳定 skillId；跨 Skill 引用、联合图谱与联合运行放到 2.0。
18. 跨 Skill 依赖属于 Skill 自身的可移植元数据；Workspace 只负责成员组织、依赖解析和状态展示，不成为依赖事实源，也不得按名称模糊关联。

---

*Skill Designer 1.0 产品设计基线，2026-07-27*
